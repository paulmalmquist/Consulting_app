# Stargate streaming tooling — its own venv, deliberately NOT backend/requirements.txt
# (the Railway backend image must not grow a Kafka client for a demo lane).
#
#   python -m venv .venv && .venv\Scripts\pip install -r requirements.txt
confluent-kafka[protobuf,schemaregistry]>=2.5,<3
protobuf>=5.26,<6
grpcio-tools>=1.62        # protoc for regenerating proto_gen bindings
numpy>=1.26
fastapi>=0.115
uvicorn>=0.30
httpx>=0.27               # bridge test client
