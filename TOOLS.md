# Tool Notes

- Use `rg` for repo search.
- Quote paths that contain brackets when using `zsh`.
- Prefer the repository root as the command starting point unless a subproject requires a narrower working directory.
