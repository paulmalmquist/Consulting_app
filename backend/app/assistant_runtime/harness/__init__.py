"""Winston agent harness layer.

Wraps the existing assistant runtime with loop control, quality gates,
lifecycle checkpoints, and structured audit logging. Extends rather than
replaces the canonical skill/dispatch/receipt architecture.
"""
