fastapi==0.115.6
uvicorn[standard]==0.33.0
psycopg[binary]==3.2.4
psycopg-pool>=3.2
python-dotenv==1.0.1
# Control Tower — Ed25519 signed/hash-chained decision receipts (was transitive; now a direct dep)
cryptography>=42.0
pydantic==2.10.4
httpx==0.28.1

# Direct imports that were only present transitively (deptry DEP003). Floors (not ==)
# so they don't fight the transitive resolver; pinned to the versions resolved at
# declaration time, no opportunistic upgrade. anyio: stargate_bridge route + service
# (top-level); requests: receipt_intake.py (lazy); websockets: hr_polymarket/worker.py (lazy).
anyio>=4.13          # resolved 4.13.0
requests>=2.33       # resolved 2.33.1
websockets>=16.0     # resolved 16.0
# Second DEP003 batch: starlette is imported directly (BaseHTTPMiddleware) in auth/middleware.py +
# middleware/request_logging.py; floor = FastAPI's own starlette floor (FastAPI 0.115.6 pins
# starlette>=0.40,<0.42), so this can never fight FastAPI's range. pyarrow (cre_egress.py parquet,
# lazy) + scipy (hr_ml_demo/trainers.py cluster.hierarchy, lazy) get conservative floors well below
# the resolved versions so they declare the dep without forcing a CI upgrade.
# Skipped: vertexai (DEP003) — not installed here, a lazy optional heavy cloud SDK; pinning would
# force-install it and change the footprint. Left transitive on purpose.
starlette>=0.40      # resolved 0.41.3 (FastAPI-coupled; floor matches FastAPI's own)
pyarrow>=14          # resolved 24.0.0 (conservative floor; via google-cloud-bigquery/pandas)
scipy>=1.10          # resolved 1.17.1 (conservative floor; via scikit-learn/pandas)

# RS Demo streaming slice: ISS Lightstreamer live adapter (lazy-imported; capture mode needs no network)
lightstreamer-client-lib==2.2.2
scikit-learn==1.6.1
pandas>=2.2

# Test dependencies
pytest==8.3.2
respx==0.22.0
ruff==0.9.6

pypdf==5.1.0
jsonschema==4.23.0
openpyxl==3.1.5
python-docx==1.1.2
reportlab==4.2.5
python-multipart==0.0.20

# Enterprise OIDC (Okta + Entra) — JWKS-backed JWT validation
PyJWT[crypto]>=2.8,<3

# CRE Intelligence connectors
pyshp>=2.3.0
usaddress>=0.5.10
rapidfuzz>=3.0
networkx>=3.0

# OCR / Invoice processing
pytesseract>=0.3.10
Pillow>=10.0.0

# AI Gateway + RAG
openai>=1.30
anthropic>=0.95.0
tiktoken>=0.7
numpy>=1.26
pgvector>=0.3
cohere>=5.0
langfuse>=2.0

# Trade execution layer
celery>=5.4
redis>=5.0
ib_insync>=0.9.86

# Event streaming — BigQuery sink (Phase 2+). BQ_ENABLED defaults to false so
# the sink is inert unless explicitly enabled.
google-cloud-bigquery>=3.11
# AI dispatch — Gemma-on-Vertex adapter mints ADC tokens via google.auth (imported
# lazily; already present transitively via google-cloud-bigquery, declared here because
# app/services/ai_dispatch/providers/gemma_vertex_provider.py uses it directly).
google-auth>=2.0
# Event streaming — Kafka transport (Phase 3B+). Lazy-imported in transport.py:
# absent or unconfigured → get_transport() falls back to NoopTransport, so its
# presence here never changes default-off behavior. Needed once a real broker
# (local Redpanda or Confluent Cloud) is exercised.
confluent-kafka>=2.3

# Podcast Intelligence (migration 425)
yt-dlp>=2024.0.0
feedparser>=6.0
# faster-whisper is optional — LocalWhisperBackend lazy-imports it.
# Install with `pip install faster-whisper` on machines with enough RAM.
