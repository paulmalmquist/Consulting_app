fastapi==0.115.6
uvicorn[standard]==0.33.0
psycopg[binary]==3.2.4
psycopg-pool>=3.2
python-dotenv==1.0.1
pydantic==2.10.4
httpx==0.28.1

# RS Demo streaming slice: ISS Lightstreamer live adapter (lazy-imported; capture mode needs no network)
lightstreamer-client-lib==2.2.2
scikit-learn==1.6.1

# Test dependencies
pytest==8.3.2
respx==0.22.0
ruff==0.9.6

pypdf==5.1.0
jsonschema==4.23.0
openpyxl==3.1.5
python-docx==1.1.2
reportlab==4.2.5
python-multipart==0.0.20

# Enterprise OIDC (Okta + Entra) — JWKS-backed JWT validation
PyJWT[crypto]>=2.8,<3

# CRE Intelligence connectors
pyshp>=2.3.0
usaddress>=0.5.10
rapidfuzz>=3.0
networkx>=3.0

# OCR / Invoice processing
pytesseract>=0.3.10
Pillow>=10.0.0

# AI Gateway + RAG
openai>=1.30
anthropic>=0.95.0
tiktoken>=0.7
numpy>=1.26
pgvector>=0.3
cohere>=5.0
langfuse>=2.0

# Trade execution layer
celery>=5.4
redis>=5.0
ib_insync>=0.9.86

# Event streaming — BigQuery sink (Phase 2+). BQ_ENABLED defaults to false so
# the sink is inert unless explicitly enabled. confluent-kafka is optional (lazy
# import in transport.py) and is NOT listed here; install separately for local
# Kafka/Redpanda testing.
google-cloud-bigquery>=3.11

# Podcast Intelligence (migration 425)
yt-dlp>=2024.0.0
feedparser>=6.0
# faster-whisper is optional — LocalWhisperBackend lazy-imports it.
# Install with `pip install faster-whisper` on machines with enough RAM.
