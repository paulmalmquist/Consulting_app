import { OperatorModePlaceholder } from "../_placeholder";

export default function ProductPage() {
  return (
    <OperatorModePlaceholder
      activeKey="product"
      title="Product"
      purpose="Winston + internal tooling roadmap, ship log, and usage signals."
      forwardNote="First build targets the ship log + active-initiative tracker."
    />
  );
}
