import { OperatorModePlaceholder } from "../_placeholder";

export default function TasksPage() {
  return (
    <OperatorModePlaceholder
      activeKey="tasks"
      title="Tasks"
      purpose="Cross-mode task queue, ownership, and completion pacing."
      forwardNote="First build targets a unified queue across Pipeline, Engagements, and Accounting."
    />
  );
}
