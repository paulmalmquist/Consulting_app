import { OperatorModePlaceholder } from "../_placeholder";

export default function CapitalRaisingPage() {
  return (
    <OperatorModePlaceholder
      activeKey="capital-raising"
      title="Capital Raising"
      purpose="Pipeline of LP commitments, close cadence, and fund readiness."
      forwardNote="First build targets the LP pipeline kanban and close calendar."
    />
  );
}
