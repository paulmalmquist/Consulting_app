import { OperatorModePlaceholder } from "../_placeholder";

export default function ResearchPage() {
  return (
    <OperatorModePlaceholder
      activeKey="research"
      title="Research"
      purpose="Thesis development, competitor intel, and signal capture."
      forwardNote="First build targets the signal inbox + thesis draft workspace."
    />
  );
}
