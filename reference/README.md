# Reference Only

These imported repositories are quarantined reference material.

- They are non-canonical.
- They must not be used as Winston runtime source-of-truth.
- The deterministic assistant runtime is canonical in `backend/app/assistant_runtime/`.

