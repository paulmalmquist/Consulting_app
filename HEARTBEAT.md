# Heartbeat

- Resolve the git root, branch, HEAD, dirty state, and worktrees.
- Reconstruct active work from plans, PRs, ADO, tests, and deploy evidence.
- Use a dedicated worktree before mutation; never alter the shared checkout.
- Prefer Winston / Business Machine tasks unless the user explicitly switches context.
