# Stone PDS — Planning 2026-03-25

## Starting Point (from yesterday's health check)

**Incoming status: DEGRADED → NEAR-HEALTHY after yesterday's session**

Yesterday's session fixed Resources and Timecards null guards (both verified deployed), seeded 580 satisfaction snapshot rows, and committed a follow-up fix for the Client Satisfaction page (`npsSummary.trend` render-time guard). That fix is in the repo but **awaiting Vercel deploy** — it needs verification as the first task this morning.

Carry-forward bugs from yesterday: Schedule Health redirect, empty `pds_leader_coverage`, satisfaction score data quality, two Coming Soon stubs (Backlog, Fee Variance).

**New bugs found during this morning's code tour (not in yesterday's report):**
- Tech Adoption page has the same null guard pattern gap as Resources/Timecards/Satisfaction — will crash on real data
- Financials page has the same pattern gap — `setVarianceData` and `setWaterfallData` unguarded
- NPS data quality root cause identified: the service reads `pds_nps_responses`, not `pds_client_satisfaction_snapshot` — yesterday's seed targeted the wrong table

---

## Today's Page Status (pre-coding-session baseline)

| Page | Status | Notes |
|---|---|---|
| Home (`/pds`) | ✅ PASS | Stable |
| Markets (`/pds/markets`) | ✅ PASS | Stable |
| Accounts (`/pds/accounts`) | ✅ PASS | Stable |
| Projects (`/pds/projects`) | ✅ PASS | Stable |
| Pipeline (`/pds/pipeline`) | ✅ PASS | Empty state, loads correctly |
| Exec Briefing (`/pds/ai-briefing`) | ✅ PASS | Stable |
| Delivery Risk (`/pds/risk`) | ✅ PASS | Stable |
| Revenue & CI (`/pds/revenue`) | ✅ PASS | Stable |
| Forecast (`/pds/forecast`) | ✅ PASS | Stable |
| Closeout (`/pds/closeout`) | ✅ PASS | Stable |
| Utilization (`/pds/utilization`) | ✅ PASS | PdsWorkspacePage shell — stable |
| Strategic Accounts (`/pds/strategic-accounts`) | ✅ PASS | PdsWorkspacePage shell — stable |
| Project Status (`/pds/project-status`) | ✅ LIKELY PASS | PdsWorkspacePage shell — not yet crash-tested |
| Relationship Health (`/pds/relationship-health`) | ✅ LIKELY PASS | PdsWorkspacePage shell — not yet crash-tested |
| Operational Signals (`/pds/operational-signals`) | ✅ LIKELY PASS | PdsWorkspacePage shell — not yet crash-tested |
| Process Compliance (`/pds/process-compliance`) | ✅ LIKELY PASS | PdsWorkspacePage shell — not yet crash-tested |
| Resources (`/pds/resources`) | ✅ PASS | Fixed yesterday |
| Timecards (`/pds/timecards`) | ✅ PASS | Fixed yesterday |
| Client Satisfaction (`/pds/satisfaction`) | ⏳ PENDING VERIFY | Fix committed yesterday, awaiting Vercel deploy |
| Tech Adoption (`/pds/adoption`) | ⚠️ AT RISK | Missing null guards on all 4 state setters — preemptive fix needed |
| Financials (`/pds/financials`) | ⚠️ AT RISK | Missing null guards on `setVarianceData` / `setWaterfallData` |
| Schedule Health (`/pds/schedule`) | ⚠️ REDIRECT | Silently redirects to Delivery Risk — open P1 |
| Backlog (`/pds/backlog`) | 🚧 STUB | Coming Soon — data ready |
| Fee Variance (`/pds/fee-variance`) | 🚧 STUB | Coming Soon — data ready |
| Capacity Planning (`/pds/capacity`) | 🚧 STUB | Coming Soon — not previously tracked |

**Legacy redirects (not in nav, correct behavior):**
- `/pds/executive` → `/pds/ai-briefing` ✅
- `/pds/subcontractors` → `/pds/resources` ✅

---

## Priority Queue

### P0 — Critical (do first, blocks demo readiness)

**1. Verify Client Satisfaction page after deploy**
- The `npsSummary.trend ?? []` guard is confirmed in the repo (`satisfaction/page.tsx` line ~136 and ~171).
- Action: Load `/pds/satisfaction` in the Stone environment. Confirm no TypeErrors. Confirm NPS gauge and "No trend data available" empty state render cleanly.
- If Vercel hasn't built yet, trigger a deploy manually from the Railway/Vercel dashboard.
- Expected result: Page loads, NPS gauge shows, trend chart shows "No trend data available" until `pds_nps_responses` is seeded.

**2. Fix Tech Adoption null guards** (`/pds/adoption`)
- Same pattern as Resources/Timecards fixes. All 4 state setters lack `?? []`:
  - `setTools(overviewRes.tools)` → `setTools(overviewRes.tools ?? [])`
  - `setHealthScores(healthRes.accounts)` → `setHealthScores(healthRes.accounts ?? [])`
  - `setTrends(trendsRes.rows)` → `setTrends(trendsRes.rows ?? [])`
  - `setTrendToolNames(trendsRes.tool_names)` → `setTrendToolNames(trendsRes.tool_names ?? [])`
- File: `repo-b/src/app/lab/env/[envId]/pds/adoption/page.tsx` — fetchData callback, lines ~100–107

**3. Fix Financials null guards** (`/pds/financials`)
- Two state setters unguarded:
  - `setVarianceData(varRes.rows)` → `setVarianceData(varRes.rows ?? [])`
  - `setWaterfallData(wfRes.steps)` → `setWaterfallData(wfRes.steps ?? [])`
- File: `repo-b/src/app/lab/env/[envId]/pds/financials/page.tsx` — fetchData callback, lines ~78–81

### P1 — Bugs (fix today)

**4. Fix NPS / satisfaction score data** (`/pds/satisfaction`)
- Root cause confirmed: `pds_satisfaction_analytics.py` reads exclusively from `pds_nps_responses` (not `pds_client_satisfaction_snapshot`). Yesterday's seed targeted the wrong table.
- Action: Check row count in `pds_nps_responses` for env `a2ac9edd-fa26-4ca0-bf96-f64f1fee91f2` — likely 0 rows.
- Fix: Seed `pds_nps_responses` with realistic NPS responses (scores 1–10) for the 68 Stone accounts. Target avg NPS around +25–35 (realistic for a JLL-type PDS operation).
- Schema reference: `repo-b/db/schema/370_pds_analytics_schema.sql` line 175 — `CREATE TABLE IF NOT EXISTS pds_nps_responses`

**5. Seed `pds_leader_coverage`** (Business Line filter)
- 0 rows persists from yesterday. 9 business lines × 3 markets × known leaders (Sam Rivera, Jordan Hale, Avery Cole, env-specific resources) = ~9 rows minimum to unblock filter.
- Schema: `repo-b/db/schema/413_pds_leader_coverage.sql` — references `pds_resources.resource_id`, so must join against existing resource rows for the Stone env.
- Query to get resource IDs: `SELECT resource_id, name FROM pds_resources WHERE env_id = 'a2ac9edd-fa26-4ca0-bf96-f64f1fee91f2' LIMIT 20`
- Apply via Supabase MCP (`mcp__045ccf44` project `ozboonlsplroialdwuxj`)

**6. Fix Schedule Health nav redirect** (`/pds/schedule`)
- Current: Nav link "Schedule Health" fires `/pds/schedule` which server-redirects to `/pds/risk`.
- Fastest fix: Remove "Schedule Health" entry from `PdsEnterpriseShell.tsx` nav (line 58 — `{ href: \`${base}/schedule\`, label: "Schedule Health" }`). Add a comment noting schedule data surfaces inside Delivery Risk.
- Better fix (if time): Build a real Schedule Health page using PdsWorkspacePage with `sections={["deliveryRisk"]}` and a schedule-specific title. The `deliveryRisk` section already renders schedule-adjacent data.
- File: `repo-b/src/components/pds-enterprise/PdsEnterpriseShell.tsx` line 58

### P2 — Enhancements (build if P0/P1 complete)

**7. Build Backlog page** (`/pds/backlog`)
- Replace `PdsPlaceholderPage` with real content.
- Data available: `pds_backlog_fact`, `pds_forecast_snapshot` (2,040 rows), `pds_accounts` (68 rows).
- Suggested layout: Backlog composition donut by market, aging table (0–30/31–60/61–90/90+ days), conversion rate trend line, total backlog KPI strip.
- Backend: Add `/api/pds/v2/backlog/summary` endpoint to `pds_v2.py` or a new `pds_backlog.py` route file.
- Frontend: `repo-b/src/app/lab/env/[envId]/pds/backlog/page.tsx`

**8. Build Fee Variance page** (`/pds/fee-variance`)
- Replace `PdsPlaceholderPage` with real content.
- Data available: `pds_fee_revenue_actual`, `pds_fee_revenue_plan`, `pds_revenue_entries` (18,720 rows).
- Suggested layout: Variance waterfall by market, fee-at-risk table by account, period toggle (MTD/QTD/YTD), concentration risk heatmap.
- Backend: Add `/api/pds/v2/fee-variance/*` endpoint.
- Frontend: `repo-b/src/app/lab/env/[envId]/pds/fee-variance/page.tsx`

**9. Seed `pds_business_line_performance_snapshot`**
- 0 rows — Business Line operating view returns empty panels when BL filter is active.
- Can be generated from `pds_revenue_entries` GROUP BY `business_line_id` for the Stone env.
- Schema reference: `repo-b/db/schema/416_pds_business_line_snapshot.sql`

### P3 — New Features (backlog)

**10. Capacity Planning page** (`/pds/capacity`)
- Currently a `PdsPlaceholderPage` stub — not previously tracked.
- Not blocking demos but visible in the nav. Add to the build queue after Backlog and Fee Variance.
- Data likely available: `pds_resources`, `pds_utilization_heatmap`, `pds_forecast_snapshot` could feed demand-vs-capacity view.

**11. Winston AI chat response quality in PDS context**
- Carried: Winston returns "Found 1 results." without rendering prose value. Streaming hangs.
- Owning surface: `agents/ai-copilot.md`. Visible on the `/pds/ai-query` Custom Query page.
- Not a Stone-specific bug — part of global Bug 1 (META_PROMPT_CHAT_WORKSPACE.md).

---

## Recommended Session Sequence

1. **[15 min]** Verify Client Satisfaction deploy. If not deployed, trigger it.
2. **[20 min]** Apply Adoption + Financials null guard fixes (P0 #2 and #3) — same pattern, 4+2 one-liners.
3. **[30 min]** Seed `pds_nps_responses` via Supabase MCP (P1 #4) — confirms NPS gauge populates on Satisfaction page.
4. **[20 min]** Seed `pds_leader_coverage` via Supabase MCP (P1 #5) — 9 rows, unblocks Business Line filter.
5. **[15 min]** Fix Schedule Health nav or build minimal page (P1 #6).
6. **[60–90 min]** Build Backlog page if bandwidth remains (P2 #7).
7. **[60–90 min]** Build Fee Variance page (P2 #8).

After steps 1–5, the environment should reach **HEALTHY** (all nav pages loading, data meaningful, no misleading nav links). Steps 6–7 elevate it to **COMPLETE** — all nav pages functional.

---

## Key Files for Coding Agent

**Null guard fixes (P0):**
- `repo-b/src/app/lab/env/[envId]/pds/adoption/page.tsx` — lines ~100–107
- `repo-b/src/app/lab/env/[envId]/pds/financials/page.tsx` — lines ~78–81
- Pattern reference: `repo-b/src/app/lab/env/[envId]/pds/resources/page.tsx` (fixed version — copy the `?? []` pattern)

**Nav fix (P1):**
- `repo-b/src/components/pds-enterprise/PdsEnterpriseShell.tsx` — line 58

**Data seeding (P1):**
- `repo-b/db/schema/370_pds_analytics_schema.sql` — `pds_nps_responses` table definition (line 175)
- `repo-b/db/schema/413_pds_leader_coverage.sql` — `pds_leader_coverage` table definition
- Supabase project: `ozboonlsplroialdwuxj` / env_id: `a2ac9edd-fa26-4ca0-bf96-f64f1fee91f2`

**Stub pages to build (P2):**
- `repo-b/src/app/lab/env/[envId]/pds/backlog/page.tsx`
- `repo-b/src/app/lab/env/[envId]/pds/fee-variance/page.tsx`
- `backend/app/routes/pds_v2.py` — add backlog and fee-variance API endpoints
