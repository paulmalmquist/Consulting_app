# Stone PDS Health — 2026-03-22

## Overall: DEGRADED

The PDS environment is partially functional. The core navigation shell and several sub-pages load correctly, but a missing `pds_business_lines` table causes errors on Home, Markets, and Projects pages. Today's coding session focused on intent classification in `repe_intent.py` (not PDS-specific), so no PDS-specific fixes were deployed today.

## Page Load

- **Home** (`/pds`): FAIL — Red error banner: `relation "pds_business_lines" does not exist LINE 1: SELECT * FROM pds_business_lines WHERE env_id = $1::uuid AND...`
- **Exec Briefing** (`/pds/exec-briefing`): FAIL — 404 Not Found
- **Markets** (`/pds/markets`): FAIL — Same `pds_business_lines` error
- **Accounts** (`/pds/accounts`): PASS — KPI cards render ($781,791 revenue, -77.9% change, 35.1% metric), Top 5 by Revenue and Top 5 At Risk lists populated. Minor: 4th card shows "NaN accounts"
- **Projects** (`/pds/projects`): FAIL — Same `pds_business_lines` error
- **Pipeline** (`/pds/pipeline`): NOT TESTED
- **Revenue & CI** (`/pds/revenue`): FAIL — "Something went wrong" error dialog
- **Forecast** (`/pds/forecast`): PASS — Deal Funnel chart renders with $523,990 Unweighted Value. Minor: one value shows "NaN"
- **Delivery Risk** (`/pds/delivery-risk`): FAIL — 404 Not Found
- **Resources** (`/pds/resources`): FAIL — "Something went wrong" error dialog
- **Closeout** (`/pds/closeout`): NOT TESTED
- **Schedule Health** (`/pds/schedule-health`): NOT TESTED
- **Project Status** (`/pds/project-status`): NOT TESTED
- **Timecards** (`/pds/timecards`): NOT TESTED
- **Utilization** (`/pds/utilization`): NOT TESTED

### Summary: 2 PASS, 6 FAIL (3 pds_business_lines errors, 2 "Something went wrong", 2 404s), 5 NOT TESTED

## Data Health

- PDS tables present in Supabase: **79** (comprehensive set including exec_, analytics_, and financial tables)
- **MISSING: `pds_business_lines`** — This table is queried on Home, Markets, and Projects but does not exist in the database. This is the root cause of most page failures.
- Data populating in UI: **PARTIAL** — Accounts and Forecast pages show real data; most other pages blocked by the missing table

## Today's Fix Verification

- **What was fixed today:** Intent classification in `repe_intent.py` — chart-keyword queries ("Show me top 5 assets by NOI") were routing to empty dashboard composer instead of SQL analytics agent. Score thresholds adjusted.
- **Verified working:** NOT APPLICABLE TO PDS — The fix was for REPE fast-path intent classification, not PDS-specific. The fix would apply to Winston AI chat queries across all environments, but we could not verify via PDS because the home page errors prevent normal chat access.
- **CI status:** GREEN (CI #314 passed)

## New Issues Found

1. **CRITICAL: Missing `pds_business_lines` table** — This table does not exist in the database but is queried by the Home, Markets, and Projects pages. A migration needs to be created to add this table with appropriate columns and seed data. This blocks 3+ pages.
2. **NaN rendering on Accounts page** — 4th KPI card shows "NaN accounts" instead of a proper count
3. **NaN rendering on Forecast page** — One summary value shows "NaN" instead of a number
4. **404 pages** — Exec Briefing (`/pds/exec-briefing`) and Delivery Risk (`/pds/delivery-risk`) return 404, suggesting these routes may not be implemented yet or have incorrect paths
5. **"Something went wrong" on Revenue & CI and Resources** — These pages hit unhandled errors (likely also related to missing tables or data issues)

## Recommendations for Tomorrow

1. **P0: Create `pds_business_lines` migration** — Add the `pds_business_lines` table with proper schema and seed data. This single fix will likely unblock Home, Markets, and Projects pages.
2. **P1: Audit all PDS page queries** — Check which other tables are missing or have schema mismatches causing the "Something went wrong" errors on Revenue & CI and Resources.
3. **P1: Fix NaN rendering** — Investigate null/undefined values causing NaN display on Accounts (account count) and Forecast (deal value).
4. **P2: Verify Exec Briefing and Delivery Risk routes** — Determine if these are unimplemented features or routing errors.
5. **P2: Re-verify today's intent classification fix** — Once PDS home page is fixed, test a Winston AI query like "Show me top 5 projects by revenue" to confirm the repe_intent.py fix works in this environment.
