# Stone PDS Health — 2026-03-23

## Overall: DEGRADED (improved from BROKEN)

Major progress today: the critical `pds_business_lines` migration (commit `286dea0f`) was deployed and resolved the P0 blocker. Home, Markets, and Projects now fully load with real data. Delivery Risk and Exec Briefing (previously 404) are now live. Two pages (Revenue & CI, Resources) remain broken with JS runtime errors.

## Page Load

- **Home** (`/pds`): PASS — Full data render: Fee Revenue $2,517,000, GAAP $2,391,150, CI $377,550, Backlog $4,705,800, Forecast $8,008,020. Market Operating View, Staffing Issues panel, Forecast Trend, and Exec Briefing all populated. No error banners.
- **Markets** (`/pds/markets`): PASS — All three markets render (Northeast Healthcare, Mid-Atlantic Public Sector, South Florida) with revenue, backlog, forecast, and utilization data. Staffing Issues and Forecast Trend panels populated.
- **Projects** (`/pds/projects`): PASS — Project Operating View renders two projects (Riverside Mixed Use Phase II, Downtown Tower Renovation) with fee/GAAP/CI variance and reason codes. Client satisfaction and closeout panels populated.
- **Exec Briefing** (`/pds/ai-briefing`): PASS — Page now resolves (was 404 yesterday). Content loads: market view, KPI cards, forecast table, management intelligence brief.
- **Delivery Risk** (`/pds/risk`): PASS — Page now resolves (was 404 yesterday). Shows 252 active projects, avg health score 78.4, and projects ranked by health score (worst first). Full EVM data present.
- **Revenue & CI** (`/pds/revenue`): FAIL — "Something went wrong: Cannot read properties of undefined (reading 'map')" — JS error in `pds/revenue/page-47f8e29ef92b5a3e.js:1:6052`. API response returns undefined where an array is expected.
- **Resources** (`/pds/resources`): FAIL — "Something went wrong: Cannot read properties of undefined (reading 'length')" — JS error in `pds/resources/page-f3f6b27f03f7d3c7.js:1:3341`. Same pattern — component calling `.length` on undefined.
- **Accounts** (`/pds/accounts`): PASS (carried from yesterday — not re-tested today)
- **Forecast** (`/pds/forecast`): PASS (carried from yesterday — not re-tested today)

### Summary: 6 PASS, 2 FAIL (Revenue & CI, Resources)

## Data Health

- PDS tables present in Supabase: **83** (up from 79 yesterday)
- **NEW tables added today:** `pds_business_lines` (9 rows), `pds_business_line_performance_snapshot` (0 rows), `pds_leader_coverage` (0 rows), `pds_leader_coverage` bridge table (0 rows)
- `pds_business_lines`: 9 rows ✅
- `pds_leader_coverage`: 0 rows — table exists but no seed data yet
- `pds_business_line_performance_snapshot`: 0 rows — table exists but no data yet
- `pds_revenue_entries`: 18,720 rows — total fee revenue $14,445,565.58
- `pds_markets`: 3 rows, `pds_projects`: 2 rows, `pds_accounts`: 68 rows
- `pds_resources`: 6 rows, `pds_timecards`: 24 rows
- `pds_forecast_snapshot`: 2,040 rows
- Data populating in UI: **YES** for all passing pages — real dollar values, market leaders, project names all visible

## Today's Fix Verification

- **What was fixed:** `pds_business_lines` table missing — blocked Home, Markets, and Projects pages with `relation "pds_business_lines" does not exist` error. Fix: 5 migrations added (412–416) creating the table, leader coverage bridge, business_line_id columns on related tables, indexes, and snapshot table.
- **Verified working:** YES — Home, Markets, and Projects all load with full data. The pds_business_lines error is completely gone.
- **Additional fixes today:** Exec Briefing (`/pds/ai-briefing`) and Delivery Risk (`/pds/risk`) routes now resolve — were 404 yesterday, both fully functional today. These appear to have been fixed as part of the same deployment.

## Winston AI Chat

- Query tested: "What is the total fee revenue?"
- Result: AI parsed intent correctly, generated valid SQL (`SELECT SUM(r.recognized_revenue) FROM pds_revenue_entries`), executed query, returned "Found 1 results."
- **Issue observed:** Response rendering appears incomplete — result value not displayed in final prose narrative, and Stop Claude button persisted beyond expected completion time (~60 seconds). This matches the known chat rendering issue (Bug 1 from prior reports).
- Chat backbone: FUNCTIONAL (intent parsing, SQL generation, and execution all working)
- Chat rendering: DEGRADED (result value not surfaced in response text)

## New Issues Found

1. **Revenue & CI page (`/pds/revenue`) — persistent FAIL** — `TypeError: Cannot read properties of undefined (reading 'map')` at `pds/revenue/page.js:6052`. The page component is calling `.map()` on a value that the API returns as `undefined` or `null` instead of an array. Likely the revenue breakdown endpoint returns an empty/null array when no data matches. Needs null guard: `(data?.items ?? []).map(...)`.
2. **Resources page (`/pds/resources`) — persistent FAIL** — `TypeError: Cannot read properties of undefined (reading 'length')` at `pds/resources/page.js:3341`. Same pattern — component calling `.length` on undefined. Needs null guard.
3. **`pds_leader_coverage` empty** — The new bridge table exists but has 0 rows. If any page queries this table expecting data, it may render empty panels. Needs seed data for the Stone env.
4. **`pds_business_line_performance_snapshot` empty** — New snapshot table has 0 rows. Business Line filter button exists in the UI toolbar but may return empty results. Needs seed data or a scheduled snapshot job.
5. **Winston AI chat response rendering incomplete** — "Found 1 results" shows but the actual value ($2,517,000 or similar) is not rendered in the response prose. The streaming session appears to hang rather than complete cleanly.

## Recommendations for Tomorrow

1. **P0: Fix Revenue & CI page null guard** — Add `?? []` / `?? 0` defensive checks before `.map()` calls in `pds/revenue/page.tsx`. This is a one-line fix per occurrence. Should unblock the last major failing page.
2. **P0: Fix Resources page null guard** — Same fix: add `?? []` before `.length` check in `pds/resources/page.tsx`.
3. **P1: Seed `pds_leader_coverage` and `pds_business_line_performance_snapshot`** — These tables have 0 rows. The Business Line filter and leader coverage panels on Home/Markets will be empty until seeded.
4. **P1: Investigate Winston AI chat response rendering** — The response value is fetched (SQL executes, "Found 1 results") but the narrative doesn't complete or display the number. May be a streaming completion signal issue.
5. **P2: Verify remaining untested pages** — Pipeline, Backlog, Fee Variance, Closeout, Schedule Health, Project Status, Timecards, Utilization, Capacity Planning, Client Satisfaction, Strategic Accounts, Relationship Health, Tech Adoption, Process Compliance, Operational Signals, Reports, Documents, Audit, Configuration were not tested today.
