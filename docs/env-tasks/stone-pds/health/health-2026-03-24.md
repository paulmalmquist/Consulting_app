# Stone PDS Health — 2026-03-24

## Overall: DEGRADED (stable from yesterday)

Revenue & CI page is now fixed (was FAIL yesterday). However, three pages remain broken with the same null guard pattern, and two pages are still "Coming Soon" stubs. Client satisfaction data quality issue newly identified — all accounts except one show Score 0.0.

## Page Load Results

- **Home** (`/pds`): PASS — Full data render: Fee Revenue $2,517,000, GAAP $2,391,150, CI $377,550, Backlog $4,705,800, Forecast $8,008,020. Staffing issues panel, Forecast Trend, Exec Briefing all populated. No errors.
- **Markets** (`/pds/markets`): PASS (carried from 2026-03-23)
- **Accounts** (`/pds/accounts`): PASS (carried from 2026-03-23)
- **Projects** (`/pds/projects`): PASS (carried from 2026-03-23)
- **Exec Briefing** (`/pds/ai-briefing`): PASS (carried from 2026-03-23)
- **Delivery Risk** (`/pds/risk`): PASS — 252 active projects, avg health score 78.6, EVM data present
- **Revenue & CI** (`/pds/revenue`): ✅ **FIXED TODAY** — was FAIL yesterday. Full data: variance bridge chart, by-market table, revenue risk items all render correctly.
- **Forecast** (`/pds/forecast`): PASS (carried from 2026-03-23)
- **Closeout** (`/pds/closeout`): PASS — Renders with project closeout data and exec briefing panel
- **Utilization** (`/pds/utilization`): PASS — Resource operating view with market table and staffing alerts (5 total: 2 critical, 3 warning)
- **Strategic Accounts** (`/pds/strategic-accounts`): PASS — Account operating view, forecast table, client health panels
- **Pipeline** (`/pds/pipeline`): PASS — Empty state (no deals seeded yet) — page loads and CTA buttons work
- **Schedule Health** (`/pds/schedule`): ⚠️ REDIRECT — Silently redirects to `/pds/risk`. Nav link misleads users.
- **Resources** (`/pds/resources`): FAIL — `TypeError: Cannot read properties of undefined (reading 'length')` — same as yesterday, null guard on `heatmapMonths` not applied
- **Timecards** (`/pds/timecards`): FAIL — `z is not iterable` — NEW FAIL (not tested yesterday). `bench` state receives undefined; spread `[...bench]` crashes.
- **Client Satisfaction** (`/pds/satisfaction`): FAIL — `TypeError: Cannot read properties of undefined (reading 'length')` — same null guard pattern as Resources
- **Backlog** (`/pds/backlog`): STUB — "Coming Soon" placeholder
- **Fee Variance** (`/pds/fee-variance`): STUB — "Coming Soon" placeholder

### Summary: 10 PASS, 3 FAIL (Resources, Timecards, Client Satisfaction), 1 REDIRECT, 2 STUB

## Data Health

- PDS tables present in Supabase: **83** (unchanged from yesterday)
- `pds_business_lines`: 9 rows ✅
- `pds_leader_coverage`: 0 rows ⚠️ — still empty, no seed data
- `pds_business_line_performance_snapshot`: 0 rows ⚠️ — still empty
- `pds_revenue_entries`: 18,720 rows ✅
- `pds_markets`: 3 rows ✅ / `pds_projects`: 2 rows ✅ / `pds_accounts`: 68 rows ✅
- `pds_forecast_snapshot`: 2,040 rows ✅

## New Issues Found Today

1. **Timecards page (`/pds/timecards`) — NEW FAIL** — `z is not iterable` at `[...bench].sort()`. The `bench` state is `undefined` because `benchRes.employees` from `/api/pds/v2/utilization/bench` returns undefined (no null guard on state setter). Fix: `setBench(benchRes.employees ?? [])`.

2. **Client Satisfaction page (`/pds/satisfaction`) — CONFIRMED FAIL** — Not tested yesterday, now confirmed broken with same null guard pattern as Resources.

3. **Schedule Health redirect is a UX bug** — `/pds/schedule` silently redirects to Delivery Risk without any indication. The nav label "Schedule Health" is misleading. Either build the page or remove the nav entry.

4. **Client satisfaction scores all 0.0** — On Home, Closeout, and Strategic Accounts pages, all 68 strategic accounts show Score 0.0 except City Civic Infrastructure (4.5). This suggests `pds_client_satisfaction_snapshot` has no rows for this env. Needs seed data.

## Yesterday's P0 Status

- **Revenue & CI null guard**: ✅ FIXED — page now fully loads
- **Resources null guard**: ❌ STILL OPEN — unchanged from yesterday
- **`pds_leader_coverage` empty**: ❌ STILL OPEN — 0 rows
- **Winston AI chat response rendering**: Partially open — not retested today

## Recommendations for Tomorrow

1. **P0: Fix Resources, Timecards, Client Satisfaction null guards** — All three share the same root cause: array state setters receiving undefined from API. One-line fix per setter: add `?? []`. This likely unblocks all three pages in a single small PR.
2. **P1: Fix Schedule Health nav redirect** — Remove the misleading nav entry or build a real page.
3. **P1: Seed `pds_leader_coverage`** — 0 rows blocks Business Line filter across the whole environment.
4. **P1: Investigate/seed client satisfaction scores** — All accounts at 0.0 is a data quality problem that undermines the entire Client Health section.
5. **P2: Build Backlog and Fee Variance pages** — Both are confirmed stubs with underlying data available.
