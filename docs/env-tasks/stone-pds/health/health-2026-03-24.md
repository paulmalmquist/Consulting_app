# Stone PDS Health — 2026-03-24

## Overall: DEGRADED (improving — 2 of 3 P0 null guard fixes verified, 1 still broken pre-deploy)

Resources and Timecards null guard fixes deployed and verified PASS. Client Satisfaction fix was **incomplete** — the original fix guarded array state setters but missed `npsSummary.trend` accessed directly in the render path. A follow-up fix was applied during this afternoon health check and committed. Satisfaction page will be PASS after the next Vercel deploy.

Satisfaction snapshot data was seeded today (580 rows), resolving the 0-row data gap. However, average scores remain near 0.0 — likely a seed quality issue, not a code bug.

## Page Load Results

| Page | Status | Notes |
|---|---|---|
| Home (`/pds`) | ✅ PASS | Full data — Fee Revenue, KPIs, staffing panel, forecast, exec briefing all populated. No console errors. |
| Markets (`/pds/markets`) | ✅ PASS | Carried from morning check |
| Accounts (`/pds/accounts`) | ✅ PASS | Carried from morning check |
| Projects (`/pds/projects`) | ✅ PASS | Carried from morning check |
| Exec Briefing (`/pds/ai-briefing`) | ✅ PASS | Carried from morning check |
| Delivery Risk (`/pds/risk`) | ✅ PASS | Carried from morning check |
| Revenue & CI (`/pds/revenue`) | ✅ PASS | Fixed yesterday, still working |
| Forecast (`/pds/forecast`) | ✅ PASS | Carried from morning check |
| Closeout (`/pds/closeout`) | ✅ PASS | Carried from morning check |
| Utilization (`/pds/utilization`) | ✅ PASS | Carried from morning check |
| Strategic Accounts (`/pds/strategic-accounts`) | ✅ PASS | Carried from morning check |
| Pipeline (`/pds/pipeline`) | ✅ PASS | Empty state, page loads correctly |
| Schedule Health (`/pds/schedule`) | ⚠️ REDIRECT | Still silently redirects to `/pds/risk` — open P1 bug |
| Resources (`/pds/resources`) | ✅ **FIXED** | Was FAIL this morning. Now renders "No heatmap data available" gracefully — null guard working. No console errors. |
| Timecards (`/pds/timecards`) | ✅ **FIXED** | Was FAIL this morning. Now renders "No distribution data / No one on bench" — null guard working. No console errors. |
| Client Satisfaction (`/pds/satisfaction`) | ❌ STILL BROKEN (fix pending deploy) | Today's null guard fix missed `npsSummary.trend` — accessed directly in render at line 171. Error: `TypeError: Cannot read properties of undefined (reading 'length')`. Follow-up fix committed this afternoon — will be PASS after next Vercel deploy. |
| Backlog (`/pds/backlog`) | 🚧 STUB | "Coming Soon" placeholder |
| Fee Variance (`/pds/fee-variance`) | 🚧 STUB | "Coming Soon" placeholder |

### Summary: 12 PASS, 1 FAIL (satisfaction — fix pending deploy), 1 REDIRECT, 2 STUB

*Morning baseline was: 10 PASS, 3 FAIL, 1 REDIRECT, 2 STUB*

## Data Health

- PDS tables present in Supabase: **83** ✅ (unchanged — all expected tables present)
- `pds_business_lines`: 9 rows ✅
- `pds_leader_coverage`: 0 rows ⚠️ — still empty, no seed data
- `pds_business_line_performance_snapshot`: 0 rows ⚠️ — still empty
- `pds_revenue_entries`: 18,720 rows ✅
- `pds_markets`: 3 rows ✅ / `pds_projects`: 2 rows ✅ / `pds_accounts`: 68 rows ✅
- `pds_forecast_snapshot`: 2,040 rows ✅
- `pds_client_satisfaction_snapshot`: **580 rows** ✅ (was 0 this morning — data seeded today)
  - BUT: avg score 0.38, min 0.0, max 4.5 — most scores are 0.0. Seed data quality issue.

## Today's Fix Verification

### Resources null guard — ✅ VERIFIED WORKING
- What was fixed: `setHeatmap/setHeatmapMonths/setCapacityDemand ?? []` null guards
- Verified: Page loads without crash, renders "No heatmap data available" gracefully
- Deployed commit: `32e0609c` / `04d5639f`

### Timecards null guard — ✅ VERIFIED WORKING
- What was fixed: `setDistribution/setOvertime/setBench ?? []` null guards
- Verified: Page loads without crash, renders "No distribution data / No one on bench" gracefully
- Deployed commit: `32e0609c` / `04d5639f`

### Client Satisfaction null guard — ❌ INCOMPLETE (follow-up fix applied this afternoon)
- What was fixed in original commit: `setDrivers/setAtRisk/setVerbatims ?? []` null guards
- **What was missed**: `npsSummary.trend` is accessed as `.length` directly in the render at line 171. The API returns `npsSummary` with `trend: undefined`, causing a crash at render time — not at the state setter level.
- Follow-up fix applied:
  - `setNpsSummary(npsRes ? { ...npsRes, trend: npsRes.trend ?? [] } : null)` — normalize on ingest
  - `{npsSummary && (npsSummary.trend ?? []).length > 0 ?` — defensive guard in render
- Status: Fix committed, awaiting Vercel deploy to verify

## New Issues Found

1. **Client Satisfaction fix was incomplete** — The original null guard commit missed the render-time `npsSummary.trend.length` access. The array state setters were guarded but the NPS summary object's `trend` field was not normalized before being stored in state. Follow-up fix committed.

2. **Satisfaction snapshot data quality** — `pds_client_satisfaction_snapshot` now has 580 rows (seeded today) but avg score is 0.38 and most values are 0.0. Seed likely inserted rows with zeroed `average_score`. Needs investigation to determine if the NPS API reads from this table or `pds_nps_responses`.

3. **`pds_leader_coverage` still empty** — Persists from morning. Business Line filter remains broken across Home/Markets panels.

## Recommendations for Tomorrow

1. **P0: Verify Client Satisfaction page after next deploy** — Fix is committed; confirm the page loads clean after Vercel builds.
2. **P1: Fix Schedule Health nav redirect** — Quick win: either build the page or remove the misleading nav entry.
3. **P1: Seed `pds_leader_coverage`** — 9 business lines, 3 market leaders. Small insert unblocks the Business Line filter across the whole environment.
4. **P1: Investigate satisfaction score data quality** — Check which tables the NPS summary API reads. Determine if seed data needs correcting or if `pds_nps_responses` also needs rows.
5. **P2: Build Backlog and Fee Variance pages** — Stubs with data ready underneath.
