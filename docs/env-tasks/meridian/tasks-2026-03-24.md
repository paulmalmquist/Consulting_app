# Meridian Capital Tasks — 2026-03-24

## Environment Status

**DEGRADED** — Core pages load with real fund/asset data. Navigation fully functional. AI chat (Winston) broken for data-query pipeline: SQL generation fails on all NOI/analytics queries. Distributions page empty (no seed data). Variance page empty. Fund detail KPIs partially blank (Paid-In, Distributed, Active Assets, Markets, Occupancy, Avg LTV). Several asset records have bad size data ("0K SF", "76294 units").

---

## Priority Queue (for today's coding session)

### P0 — Critical

**1. Fix SQL generation failure in `repe_fast_path` analytics lane**
- **What's broken:** All data-oriented queries routed to the analytics lane return "I couldn't generate a SQL query from your request" in 39ms. Intent routing fix (2026-03-22) is confirmed working — queries now reach the SQL generation stage but fail there. Affects Tests 2, 3, 4 in AI test suite.
- **Observed on:** `/re/winston` — "Show me top 5 assets by NOI" → 39ms failure. Dashboard shell creates but 0 tools called, 0 tokens generated.
- **Root cause:** `repe_fast_path` SQL prompt likely lacks schema context or the schema field names don't match what the LLM expects (e.g., `noi` vs `net_operating_income`). May also be a missing system prompt update post-routing fix.
- **Impact:** Blocks 4/6 AI test cases. Core demo capability. Unlocking this should bring pass rate from 33% → ~67%.
- **Files to check first:**
  - `backend/app/services/ai_gateway.py` — `repe_fast_path` SQL generation prompt and schema injection
  - `backend/app/routes/nv_ai_copilot.py` — Lane F handler, schema context passed to SQL model
  - `backend/app/services/repe_intent.py` — confirm routing is stable post-fix
  - `repo-b/db/schema/265_repe_object_model.sql` — verify actual column names passed to LLM

---

### P1 — Bugs

**2. Fund detail page: Paid-In, Distributed, Active Assets, Markets, Occupancy, Avg LTV KPIs are blank**
- **What's broken:** On `/re/funds/{fund_id}` (confirmed on Meridian Real Estate Fund III), the Capital section shows "Paid-In" and "Distributed" with no values. Portfolio Snapshot shows "Assets", "Markets", "Occupancy", "Average LTV" all blank. Performance Drivers show "— IRR" (dash instead of value).
- **Root cause:** Investment-level records for MRF III show "No type", "No market", "No valuation", "No operating data" — the investment objects are present but their sub-records (property type, geography, valuation, operating data) are not seeded.
- **Files to check:**
  - `repo-b/src/app/lab/env/[envId]/re/funds/[fundId]/page.tsx` — null-guard rendering for blank KPIs
  - Supabase seed data for `a1b2c3d4-0001-0010-*` investment records — missing property_type, market, valuation, operating_data links
  - `backend/app/services/repe_finance.py` — fund KPI aggregation queries

**3. Distribution Operations page has no data (no seed records)**
- **What's broken:** `/re/distributions` — all KPIs blank (Total Declared, Total Paid, Pending Amount, Paid This Quarter, Pending Recipients). All chart panels show placeholder text. "Seed Demo Data" button exists on the page.
- **Root cause:** Distribution events have not been seeded for this environment. Known gap per LATEST.md.
- **Fix:** Run or build distribution seed data for Meridian. Use the existing "Seed Demo Data" button if it's wired up, otherwise add distribution records to the seed script.
- **Files to check:**
  - Supabase seed scripts for `distribution_event` table, env `a1b2c3d4-0001-0001-0003-000000000001`
  - `repo-b/src/app/lab/env/[envId]/re/distributions/page.tsx` — confirm Seed Demo Data button is wired

**4. Asset table: "0K SF" and bad unit count data on multiple assets**
- **What's broken:** `/re/assets` — multiple assets display "0K SF" in the Units/SF column (Bellmont Residential, Midtown Towers, Riverdale Multifamily, Riverside Park, Stratford Village, Summit Heights, Vertex Multifamily, Westridge Commons). One asset ("Phoenix Gateway Medical Office") shows "76294 units" — clearly a data corruption / unit-type mismatch.
- **Root cause:** Assets missing `square_footage` values in the DB, or a units/SF display logic issue where residential-unit assets fall through to SF calculation showing 0. The 76294 case suggests a units field stored in a SF column.
- **Files to check:**
  - `repo-b/src/app/lab/env/[envId]/re/assets/page.tsx` — units/SF display logic
  - Asset seed data for the affected asset IDs
  - `backend/app/services/repe_finance.py` — asset size field mapping

**5. Raw fund UUIDs exposed in Winston chat responses (Tool lane)**
- **What's broken:** When Winston uses the `tool` lane (Lane B) for fund lookups, internal UUIDs (e.g., `a1b2c3d4-0003-0030-0001-000000000001`) are visible in the response text.
- **Fix:** Strip or mask `fund_id` / `asset_id` UUID fields from MCP tool response text before rendering. Show human-readable names only.
- **Files to check:**
  - MCP tool response formatting in `backend/app/mcp/` — fund/asset lookup tool response shape
  - `repo-b/src/components/winston/` — response renderer, check if IDs are being surfaced from raw tool output

---

### P2 — Enhancements

**6. Fund detail: Sector and Geographic Exposure panels show "No data available"**
- **What's broken:** On fund detail pages, "Sector Allocation" and "Geographic Exposure" panels display placeholder messages. Portfolio Allocation section also shows "Allocation will populate after sector or market rollups are available."
- **Fix:** Wire investment-level property_type and market data into the fund-level rollup. If property_type data is missing (P1 bug #2), fixing that will unblock this.
- **Files to check:**
  - `repo-b/src/app/lab/env/[envId]/re/funds/[fundId]/page.tsx` — sector/geo exposure panel data fetching
  - `backend/app/services/repe_finance.py` — fund portfolio rollup query

**7. Capital Activity Timeline on fund detail shows placeholder**
- **What's broken:** "Capital Activity Timeline" panel on fund detail shows "Capital activity will populate after quarter-close runs or capital ledger entries." Even though capital calls ($935M total) are seeded and showing in Capital Call Ops.
- **Fix:** Wire the capital call ledger data into the fund-level capital timeline chart.
- **Files to check:**
  - `repo-b/src/app/lab/env/[envId]/re/funds/[fundId]/page.tsx` — Capital Activity timeline data source
  - Capital call query in `backend/app/services/repe_finance.py`

**8. Dashboard templates "Monthly Operating Report", "Fund Quarterly Review", "Underwriting Dashboard" show 0 widgets**
- **What's broken:** `/re/dashboards` — three dashboard templates display "0 widgets" instead of a widget count, suggesting they are section-based shells without pre-configured widget definitions.
- **Fix:** Add default widget configurations to these three templates so they load with meaningful starting layouts.
- **Files to check:**
  - Dashboard template definitions (likely in `backend/app/services/` or a seed/config file)
  - `repo-b/src/app/lab/env/[envId]/re/dashboards/page.tsx`

---

### P3 — New Features (competitor-informed)

**9. Investor Communication Parsing (Juniper Square counter)**
- **What's shipping:** Juniper Square's JunieAI now includes AI-powered CRM with NLP parsing of investor emails and documents for sentiment, intent, and next-action prediction. This is live in production.
- **Winston gap:** LP investor tools exist, document pipeline exists, CRM core exists — but no inbound investor communication parsing.
- **Build target:** Add sentiment/intent extraction to the existing extraction engine service. Wire into CRM investor timeline. Estimated 2–3 hours per daily brief.
- **Files to start:**
  - `backend/app/services/` — extraction engine service
  - `backend/app/services/` — CRM services and LP investor record handlers

**10. Variance (Budget vs Actual) page data gap — seed UW versions**
- **What's broken:** `/re/variance` shows "No variance data" — "Total Actual" and "Total Plan" are blank. This is confirmed as Bug 5 in the META_PROMPT bug list.
- **Fix:** Seed underwriting versions and normalized accounting data for at least 3–5 assets so the variance table populates with meaningful demo data.
- **Files to check:**
  - `repo-b/db/schema/265_repe_object_model.sql` — underwriting version and budget tables
  - Variance seed script / Supabase seed data for env `a1b2c3d4-0001-0001-0003-000000000001`
  - `backend/app/services/repe_finance.py` — variance query

---

## Context for Coding Agent

- **Frontend files:** `repo-b/src/app/lab/env/[envId]/re/`
- **Backend AI gateway:** `backend/app/services/ai_gateway.py` (189KB — start here for P0)
- **REPE routes:** `backend/app/routes/nv_ai_copilot.py` — Lane F (repe_fast_path)
- **REPE finance service:** `backend/app/services/repe_finance.py` — fund/asset aggregation queries
- **Intent classifier:** `backend/app/services/repe_intent.py` — routing (confirmed working, verify stability)
- **Schema files:** `repo-b/db/schema/265_repe_object_model.sql`, `repo-b/db/schema/266_repe_env_business_binding.sql`
- **MCP tools:** `backend/app/mcp/` — tool response shapes (raw UUID exposure)
- **What to read first:** Start with `backend/app/services/ai_gateway.py` around the `repe_fast_path` handler and SQL generation prompt. The 39ms failure means SQL prompt is failing fast — look for the schema context injection and system prompt for the SQL model.

## Start Order
1. P0: Fix SQL generation in `repe_fast_path` → re-run AI test suite to verify
2. P1 #2: Seed investment sub-records (property_type, market, valuation, operating_data) for MRF III
3. P1 #3: Seed or trigger distribution data
4. P1 #4: Fix asset size display logic (0K SF)
5. P2 #6/#7: Wire sector/geo rollups and capital timeline once investment data is seeded
6. Run AI test suite to verify P0 fix landed
