# Meridian Capital Health — 2026-03-23

## Overall: DEGRADED

> Core pages load and display data correctly. The 2026-03-22 intent fix was partially successful — analytics queries no longer produce empty dashboard shells, but SQL generation is now failing. Distributions page has no data (known seeding issue). No console errors or failed API calls observed.

---

## Page Load Results

- Fund list: PASS — 3 funds displayed, Total Commitments $2.0B, Portfolio NAV $1.4B, AUM/NAV/DPI/TVPI all rendering
- Fund detail: PASS — Meridian RE Fund III shows NAV, Gross IRR 13.2%, Net IRR 11.7%, TVPI 1.21x, value creation chart rendering
- Asset pages: PASS — Assets page loads with Total NOI $24.7M, Avg Occupancy 87.2%, 22 investments in filter dropdown across multiple sectors/markets; note: individual investment rows show "No valuation" / "Pending" IRR on fund detail grid (known data gap)
- Capital calls: PASS — $935M total requested/received, 100% collection rate, investor breakdown rendering, collection progress by fund showing
- Distributions: FAIL — Page renders but all metric values are empty (Total Declared, Total Paid, Pending Amount all blank); all sub-panels show placeholder messages ("Recipient concentration appears here once payout rows exist"). Known Bug 2: pref return / carry data seeding issue.
- Dashboard charts: PASS — Dashboards page loads with AI builder, 7 layout templates (Executive Summary 5 widgets, Operating Review 6 widgets, Watchlist 4 widgets, Market Comparison 5 widgets showing correct widget counts). Fund detail value creation chart renders with quarterly data points.
- Period comparison: PARTIAL — Variance page loads but shows "No variance data" (Total Actual, Total Plan, Variance all $0). No underwriting versions seeded. Page structure intact.
- AI Chat: PARTIAL — See fix verification below.

---

## Today's Fix Verification

**What was fixed (2026-03-22):** Intent classification in `repe_intent.py` — chart keywords no longer boosted `INTENT_GENERATE_DASHBOARD` to 0.90, causing analytical queries like "Show me top 5 assets by NOI" to produce empty dashboard shells. Fix lowered chart-keyword dashboard score to 0.65, boosted analytics to 0.88 when chart keywords appear without explicit "dashboard" keyword.

**Verified working:** PARTIAL

**Test results:**
- `"What funds do we have?"` → PASS — Routed correctly, returned fund list in tabular format, 17,488ms, 35,252 tokens. Response: "You have 3 funds in Meridian Capital Management (environment snapshot — 2026Q1)". ✅
- `"Show me top 5 assets by NOI"` → PARTIAL — Intent now routes to `repe_fast_path` analytics lane (NOT dashboard) ✅. Response in 39ms. However, SQL generation fails: "I couldn't generate a SQL query from your request. Try rephrasing or use the SQL editor directly." ❌

**Assessment:** The fix successfully changed routing behavior. The query no longer produces an empty dashboard shell (previous symptom). However, the `repe_fast_path` analytics lane is failing to generate SQL from the NOI query. The fix resolved the routing regression but exposed a downstream SQL generation failure. Prior test history in the chat also showed pre-fix responses of "No response from Winston." still visible in chat history for this query type.

---

## New Issues Found

1. **SQL generation failure on NOI ranking queries** — "Show me top 5 assets by NOI" routes to analytics (fix working) but the fast-path SQL agent returns "I couldn't generate a SQL query from your request." in 39ms, suggesting a schema context or prompt failure in the SQL generation step. This is the next layer to fix now that routing is correct.

2. **Investment-level IRR "Pending" on fund detail** — Fund detail Portfolio Assets grid shows "No valuation" and "Pending" IRR for all investments (Equity Invested shows $34.2M and $25.4M, but Current Value is blank). This appears to be a data seeding gap — investment-level valuation records not populated.

3. **Distributions page empty** — All distribution metrics blank, all panels show placeholder text. Known seeding issue (Bug 2 from 2026-03-22 report: pref return / carry $0).

4. **Variance page no data** — "No variance data" for Budget vs Actual. No underwriting versions or budget plans seeded.

---

## Data Quality

- Funds displaying: 3 (Meridian RE Fund III, Meridian Credit Opportunities Fund I, Institutional Growth Fund VII)
- Assets with data: YES — 22 investments in filter, Total NOI $24.7M, Avg Occupancy 87.2%
- Financial metrics calculating: PARTIAL — Fund-level NAV/IRR/TVPI calculate correctly; investment-level IRR shows "Pending"; distributions and variance show no data

---

## Console & Network

- Console errors: NONE
- Failed API calls: NONE — All 6 observed API calls returned HTTP 200 (context, funds list, portfolio-kpis, per-fund quarter-state for all 3 funds)

---

## Recommendations for Tomorrow

**Priority 1 — SQL generation fix:** The `repe_fast_path` analytics lane now receives NOI/chart queries correctly but fails to generate SQL. Likely a schema context issue or a missing field-to-column mapping for "NOI" in the fast-path SQL prompt. Test with `repe.execute_sql` directly to confirm table/column names are accessible, then fix the prompt.

**Priority 2 — Seed distributions data:** Distributions page is completely empty. Seed pref return / carry / distribution records so the page has functional data for demos. This is Bug 2 from the prior session.

**Priority 3 — Investment-level valuations:** Fund detail grid shows "No valuation" and "Pending" IRR at the investment level. Seed current valuation records linked to investment IDs so IRR can be calculated.

**Priority 4 — Variance data:** Seed at least one fund's budget/underwriting version so the Variance page shows meaningful data.
