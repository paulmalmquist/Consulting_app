# Meridian Capital Health — 2026-03-24

## Overall: DEGRADED

> Core pages load with real data. Distribution seeding fix landed and 12 events now display correctly. Asset size display improved — most assets show proper unit counts. New debt fund tabs (Loan Book, Covenant Health, Maturity & Rate) are live and rendering data. SQL generation for analytics lane (repe_fast_path) remains broken in production despite today's commit — 0 Tools, 0 Tokens at 18.5s response time. No console errors or failed API calls observed across all pages.

---

## Page Load Results

- Fund list: PASS — 3 funds, Total Commitments $2.0B, Portfolio NAV $2.1B, Active Assets 32, AUM/NAV/DPI/TVPI all rendering
- Fund detail (equity): PASS — Institutional Growth Fund VII loads with sector allocation, geographic exposure, capital activity timeline, performance drivers, value creation chart all rendering
- Fund detail (MRF III): PASS — NAV, Gross IRR 12.4%, Net IRR 9.9%, TVPI 1.88x, sector exposure 100% classified (Value-Add Multifamily 77.1%, Senior Housing 22.9%); capital activity timeline placeholder remains (no call ledger entries)
- Fund detail (debt/new tabs): PASS — Meridian Credit Opportunities Fund I renders new Loan Book tab: Total UPB $534M, Weighted Avg Coupon 6.0%, DSCR 1.34, Portfolio LTV 77.4%, 8 loans, maturity profile by year; Covenant Health and Maturity & Rate tabs present
- Asset pages: PARTIAL — 34 assets load with Total NOI $24.7M, Avg Occupancy 87.2%; most multifamily assets now show correct unit counts; "Phoenix Gateway Medical Office" still shows "256,577 units" (exited asset, bad unit data persists); "Meridian Office Tower" shows "—" for Units/SF
- Capital calls: PASS — $935M total, 4 calls fully funded, 100% collection rate, investor breakdown rendering
- Distributions: PASS — 12 distribution events now displaying ($145.3M declared, 10 Paid + 2 Declared); seeding fix confirmed working. Minor issue: "Total Paid" shows $0 even though 10 events have Paid status (0 payout rows attached to paid events)
- Dashboard charts: PASS — 7 templates load; Executive Summary 5 widgets, Operating Review 6 widgets, Watchlist 4 widgets, Market Comparison 5 widgets render correctly; Monthly Operating Report, Fund Quarterly Review, Underwriting Dashboard still show 0 widgets (known P2)
- Variance: FAIL (data gap) — Page loads and API returns 200, but "No variance data" — no underwriting versions seeded
- AI Chat: PARTIAL — see fix verification below

---

## Today's Fix Verification

**What was fixed (2026-03-24, commit fa9372dc):**
1. P0 — Added `generate_sql()` to `combined_agent.py` (was missing, causing silent ImportError → "couldn't generate SQL")
2. P1 — Asset size display: added >0 guards for 0K SF, Math.max(1,...) for sub-1K rounding, `.toLocaleString()` for unit counts
3. P1 — Seeded `repe_property_asset` records for 8 CMBS assets
4. P1 — Seeded 12 `fin_distribution_event` records (2024Q1–2026Q1)
5. P1 — Fixed Phoenix Gateway Medical Office bad unit count (76294 set to NULL)

**Also today (commit 93286c5d):** Debt fund tab set — LoanBookTab, CovenantHealthTab, MaturityRateTab added to fund detail page with strategy-aware routing.

**Verified working:**
- Distribution seeding: YES — 12 events display correctly across both funds
- Asset size display: PARTIAL — most multifamily unit counts now correct; Phoenix Gateway still shows "256,577 units" (exited asset — NULL set in DB but still rendering from old value or cached data); "Meridian Office Tower" shows "—" for office SF
- Debt fund tabs: YES — Loan Book renders with full debt portfolio data
- SQL generation fix (P0): NO — "Show me top 5 assets by NOI" still returns "I couldn't generate a SQL query from your request" in ~18.5s (LaneF/repe_fast_path, 0 Tools, 0 Tokens). Fix committed but likely not deployed to Railway yet, or there is a residual import/runtime error in production.

---

## New Issues Found

1. **SQL generation still broken in production** — Today's `generate_sql()` fix either hasn't been deployed to Railway or there is a second-layer failure. LaneF/repe_fast_path is reached but SQL generation returns empty with no tokens consumed. Confirm Railway deployment status and check backend logs for ImportError or similar.

2. **Distributions "Total Paid" shows $0 despite 10 Paid events** — The distribution queue shows 10 events with "Paid" status but "0 payout rows" on each. The `Total Paid` KPI aggregates from payout row amounts, not event status, so it reads $0. Payout rows need to be seeded or generated for the paid events to make the KPI meaningful.

3. **Phoenix Gateway Medical Office still shows 256,577 units** — The bad unit data should have been set to NULL today, but the asset page still renders "256,577 units". This may require a page cache invalidation or the NULL-set only cleared the property_asset record while the asset record still has a value in another column.

4. **Investment-level data still missing across all funds** — Portfolio Assets grid on all fund detail pages shows "No type / No market / No valuation / Pending" for all investments. This was P1 bug #2 in today's task queue but appears not to have been addressed (only 8 CMBS assets were seeded with property_asset records; investment sub-records like property_type, market, valuation, operating_data still absent).

5. **Capital Activity Timeline placeholder on MRF III** — "Capital activity will populate after quarter-close runs or capital ledger entries" — capital call data seeded but not linked to the timeline chart.

---

## Data Quality

- Funds displaying: 3 (Institutional Growth Fund VII, Meridian Real Estate Fund III, Meridian Credit Opportunities Fund I)
- Assets with data: YES — 34 assets, Total NOI $24.7M, Avg Occupancy 87.2%
- Financial metrics calculating: PARTIAL — Fund-level NAV/IRR/TVPI calculate correctly; investment-level IRR shows "Pending" across all funds; distributions display but paid KPI is $0; variance has no data

---

## Console & Network

- Console errors: NONE
- Failed API calls: NONE — All observed API calls returned HTTP 200 (variance, environments, funds, capital calls, distributions all 200)
- AI chat trace: LaneF/repe_fast_path routed correctly but 0 Tools, 0 Tokens consumed — backend generate_sql() not executing

---

## Recommendations for Tomorrow

**Priority 1 — Verify Railway deployment of SQL fix:** Confirm backend deployed from commit fa9372dc. Check Railway logs for ImportError from `combined_agent.py`. If deployed, add backend logging around the `generate_sql()` call to pinpoint exact failure. This is still the highest-value unlock (blocks 4/6 AI test cases).

**Priority 2 — Fix distributions "Total Paid" KPI:** Seed or generate payout rows for the 10 Paid distribution events so the KPI reflects actual cash movement. The table data is good but the header KPI is misleading at $0.

**Priority 3 — Fix Phoenix Gateway Medical Office unit display:** The NULL was set but 256,577 still renders. Investigate whether the value is cached, stored in a second column, or if the asset page reads from a different field than what was updated.

**Priority 4 — Seed investment sub-records:** Property_type, market, valuation, and operating_data records are missing for all investments across all 3 funds. This blocks fund detail KPIs (Paid-In, Distributed, Markets, Occupancy, Avg LTV, investment-level IRR). This was P1 #2 from today's task queue — not addressed yet.

**Priority 5 — Variance seed data:** Seed at least 3–5 assets with underwriting versions and budget plans so the Variance page shows meaningful data for demos.
