# Creative Leapfrog Engine — 2026-03-23

> **Generated by:** creative-leapfrog-engine (scheduled, 9 AM)
> **Competitor intel sources:** daily-summary/2026-03-21.md, product-opportunities/dealpath-2026-03-20.md, cherre-2026-03-20.md, yardi-2026-03-19.md, juniper-square-2026-03-19.md, positioning-opportunities/2026-03-22.md, daily-intel/2026-03-22.md, feature-radar/2026-03-22.md + noon + context-aware-delegation.md
> **Repo state:** CAPABILITY_INVENTORY.md verified 2026-03-20 (258 pages, 208 services, 31 MCP categories, 32 lab environments)
> **Downstream consumer:** autonomous-coding-session (3 PM today) — top ideas are coded to be immediately actionable

---

## Intelligence Summary

**Competitor signals ingested today:**

- **Dealpath Connect** secured all three major global brokerages (CBRE, JLL, C&W) — creating a network moat Winston can't replicate with software alone
- **Juniper Square** launched AI-powered CRM using NLP + predictive modeling on investor emails/docs (post-$1.1B valuation)
- **Cherre Agent.STUDIO** — visual no-code agent builder where clients assemble custom AI workflows from pre-built parts
- **ARGUS Intelligence** extended scenario modeling to portfolio level — Winston already has this; they're catching up
- **Yardi** acquired 60% of WeWork — becoming an operations company, abandoning pure-play innovation
- **NVIDIA Agent Toolkit** signed 17 enterprise partners (Adobe, Salesforce, SAP) — the "bring your own agent domain" adoption model validated
- **Eragon** raised $100M valuation for prompt-first enterprise OS — direct external validation of Winston's core thesis
- **IBM/Confluent close** — real-time data pipelines becoming default for enterprise AI

**Key gap pattern across all competitors:** Every competitor is doing one of two things: (a) bolting AI onto legacy workflows, or (b) building point solutions. None has Winston's combination of AI-native architecture + full investment lifecycle coverage + unified data layer. The leapfrog opportunity is not in matching any individual competitor feature — it's in building the features that are only *possible* because Winston has all three simultaneously.

---

## #1 — Deal Signal Radar: Continuous Ambient Deal Intelligence

**Competitor Signal:** Dealpath Connect secured CBRE, JLL, and C&W — creating an institutional deal flow marketplace. Their pitch is "deals come to you." Juniper Square's CRM NLP extracts signals from investor communications.

**Source Competitor(s):** Dealpath, Juniper Square

**Creative Lens Applied:** Lens 1 (AI-Native Reimagination) + Lens 5 (Workflow Elimination)

**The Leapfrog Idea:**
What if Winston didn't wait for deals to arrive — it continuously monitored every signal that precedes a deal becoming available? Property transfers filed in county records, commercial loans coming due within 12-18 months, DSCR stress signals in asset reporting, owner behavioral patterns (key man departures, refinancing patterns, new debt layering). Winston surfaces a ranked "acquisition signal feed" — properties that haven't hit any marketplace yet but are statistically likely to transact in the next 6-18 months based on multi-signal convergence. You don't source deals from Dealpath Connect — you already own the pipeline by the time Dealpath lists it.

**Why This Is Better Than Copying:**
Dealpath Connect is a distribution network for deals that are *already for sale*. Deal Signal Radar is intelligence on deals that are *about to be for sale* — a full 6-18 month head start. No marketplace can replicate this because the signal lives in non-transactional data that requires AI synthesis, not a brokerage relationship.

**Prospect One-Liner:**
"While your competitors are bidding on deals that hit Dealpath, Winston shows you which properties are going to market before the owner even calls their broker."

**Connection to Existing Winston Code:**
- Relevant files/components: `backend/app/services/market_regime_engine.py` (signal processing pattern already exists), `backend/app/routes/market_regime.py`, MSA zone intelligence schema (`msa_zone`, `msa_zone_intel_brief` tables), `msa-rotation-engine` skill + `scoring_weights.json`, deal pipeline (Deal Radar in `repo-b/src/app/app/repe/` — confirmed 258 pages)
- Can extend existing: **Yes** — `market_regime_engine.py` already implements multi-signal composite scoring (equities/rates/credit/crypto weighted by pillar) — the same architecture applies to property-level distress signals. This is the pattern to extend, not replicate.
- Schema changes needed: **Yes** — new `property_signal_feed` table: `{property_id, signal_type, signal_value, signal_date, convergence_score, predicted_market_probability_6mo, predicted_market_probability_18mo}`. Extend `msa_zone` with signal aggregation. MSA feature cards 2 & 5 (county assessor connector + BLS/FRED connector) are prerequisite infrastructure.
- Related recent commits: Market regime engine just landed; MSA pipeline Phase 1 is the immediate predecessor.

**Implementation Complexity:** High
**Estimated Effort:** 3-4 weeks (data sources: 1 wk; signal engine: 1 wk; ranking + UI: 1 wk; AI interpretation layer: 1 wk)
**Dependencies:** MSA research sweep running (Phase 1, currently blocked), county assessor connector (MSA feature card #2), BLS/FRED connector (card #5), REPE deal data models

**Build Priority Score:** 9/10

---

## #2 — Portfolio Pulse: The Quarterly Report That Makes Quarterly Reports Obsolete

**Competitor Signal:** Juniper Square JunieAI offers quarterly report compilation automation. ARGUS Intelligence extended scenario simulation to portfolio level. Both are "make the existing reporting workflow faster."

**Source Competitor(s):** Juniper Square, Altus/ARGUS

**Creative Lens Applied:** Lens 5 (Workflow Elimination) + Lens 4 (Time Travel)

**The Leapfrog Idea:**
Kill the quarterly report entirely. Instead of automating the assembly of a document that gets emailed every 90 days, Winston runs a continuous portfolio health broadcast that LPs can query at any time with natural language. The quarterly LP report becomes an artifact generated *on demand* from a live snapshot — not a scheduled deliverable assembled by humans. Winston monitors every portfolio signal in real-time (NOI changes, lease expirations, covenant proximity, valuation shifts, capital deployment pace), surfaces an executive-level "Portfolio Pulse" that's always current, and generates a formatted LP report in 30 seconds on demand from any point in time. The quarterly cadence only exists because assembling data manually requires it. Eliminate the constraint, eliminate the cadence.

**Why This Is Better Than Copying:**
Juniper Square automated the report assembly workflow. Winston eliminates the distinction between "live intelligence" and "quarterly reporting" — the report is just a snapshot of the live portfolio intelligence that already exists. This is only possible because Winston has unified fund, asset, LP, and financial data in one place; Juniper Square doesn't.

**Prospect One-Liner:**
"Your LPs don't have to wait for your quarterly report — they can ask Winston for a portfolio snapshot any time, and get the same analysis you'd put in a report, generated in seconds."

**Connection to Existing Winston Code:**
- Relevant files/components: `finance.lp_summary`, `finance.nav_rollforward`, `finance.fund_metrics`, `finance.uw_vs_actual_waterfall`, `finance.noi_variance`, `ops.fund_quarter_detail`, `finance.generate_waterfall_memo` — all confirmed in MCP tool registry per CAPABILITY_INVENTORY.md. LP report auto-assembly is already in the BUILD-02 queue (MEGA_META_PROMPT_COMPETITOR_REVERSAL_2026-03-19.md) as a 3-5 day build.
- Can extend existing: **Yes** — BUILD-02 (quarterly LP report auto-assembly) is the foundation. The leapfrog is the *framing*: instead of shipping BUILD-02 as "faster report assembly," ship it as "the last quarterly report you'll ever manually prepare — from now on, the portfolio is always current." The LP portal becomes the always-on surface; the PDF report is an export.
- Schema changes needed: **Minimal** — existing financial tables support this. Need a `portfolio_pulse_snapshot` materialized view that refreshes hourly.
- Related recent commits: No specific commits visible; BUILD-02 is queued but not yet shipped.

**Implementation Complexity:** Medium
**Estimated Effort:** 1-2 weeks (BUILD-02 foundation: 3-5 days; continuous snapshot layer: 2-3 days; LP query interface in chat: 1-2 days)
**Dependencies:** BUILD-02 (LP report auto-assembly) must be shipped first — then this is a framing + infrastructure layer on top

**Build Priority Score:** 9/10

---

## #3 — Covenant Intelligence: The Lender Relationship OS

**Competitor Signal:** Yardi Debt Manager offers covenant tracking with breach alerts. Winston's capability inventory confirms covenant monitoring is already built (the BUILD-01 covenant breach engine from the competitor reversal meta prompt is queued). But both approaches are reactive — they tell you when you've breached.

**Source Competitor(s):** Yardi

**Creative Lens Applied:** Lens 4 (Time Travel) + Lens 1 (AI-Native Reimagination)

**The Leapfrog Idea:**
2028 version of covenant tracking isn't alerting you to breaches — it's running continuous lender relationship intelligence. Winston doesn't just monitor covenants; it models *lender behavior* in response to portfolio stress. It knows which lenders historically grant waivers vs. accelerate, which loan structures have hidden trip wires under specific market conditions (cap rate expansion + rising vacancy = DSCR cliff), and it proactively drafts waiver request packages *before* you need them. When a lender modifies terms, Winston cross-references against all other loans from the same institution to surface systemic exposure. It's not covenant monitoring — it's a lender relationship operating system that sits between your portfolio and your debt capital stack.

**Why This Is Better Than Copying:**
Yardi's Debt Manager is a compliance dashboard. Winston's Covenant Intelligence is a negotiation and relationship intelligence tool. The difference between "you're in breach" and "here's how to prevent breach and what to say to your lender" is billions of dollars in avoided workouts. This is only possible with AI because the synthesis across market conditions, lender behavior patterns, and cross-portfolio exposure requires reasoning, not reporting.

**Prospect One-Liner:**
"Every REPE firm gets covenant breach alerts. Winston tells you which covenants are going to be a problem in 90 days — and drafts the waiver request before you need it."

**Connection to Existing Winston Code:**
- Relevant files/components: BUILD-01 in `MEGA_META_PROMPT_COMPETITOR_REVERSAL_2026-03-19.md` (covenant breach alert engine — 3-5 days, data exists); `finance.check_covenant_compliance`, `finance.list_covenant_alerts` tools queued; debt surveillance + rate sensitivity tools already in MCP registry (confirmed: `rate_sensitivity` tool category); `market_regime_engine.py` provides the market condition input
- Can extend existing: **Yes** — BUILD-01 is the foundation. The leapfrog extensions are: (a) trajectory modeling (project DSCR/LTV forward 90/180 days using current market regime), (b) cross-loan lender exposure aggregation, (c) AI-drafted waiver package generation from Winston chat
- Schema changes needed: **Minimal** — `covenant_alert` table from BUILD-01 needs `projected_breach_date`, `waiver_likelihood` columns; new `lender_profile` table for behavioral patterns
- Related recent commits: `market_regime_engine.py` just landed — provides the market condition signals needed for forward-projection

**Implementation Complexity:** Medium
**Estimated Effort:** BUILD-01 (3-5 days) + trajectory engine (3 days) + waiver draft AI tool (2 days) = ~2 weeks total

**Dependencies:** BUILD-01 must be shipped first

**Build Priority Score:** 9/10

---

## #4 — Context-Aware Delegation Engine (Fully Specced — Ship It)

**Competitor Signal:** No competitor has this. The gap was self-identified from the ECC messy day queue. Cherre's Agent.STUDIO shows the market is moving toward user-orchestrated AI workflows. Juniper Square's NLP CRM shows AI extracting context from communications.

**Source Competitor(s):** Self-identified gap (ECC triage), validated by Cherre + Juniper Square direction

**Creative Lens Applied:** Lens 6 (Network Effects & Composability) + Lens 3 (Negative Space)

**The Leapfrog Idea:**
This idea is already fully specced in `docs/feature-radar/context-aware-delegation.md` — it's ready to build. The creative insight: every competitor builds *individual* AI capabilities (CRM AI, deal AI, reporting AI). Winston is the only platform where those capabilities can be *composed* for a handoff workflow: when a managing partner delegates a task, Winston reaches across the conversation history, fund data, LP records, capital activity, and CRM in a single context assembly pass — something architecturally impossible if those systems are separate products. The delegation brief is a compound output that proves Winston's unified architecture is irreplaceable.

**Why This Is Better Than Copying:**
No competitor can do context-aware delegation because they don't own the full data surface. Yardi has fund/asset data but no CRM. Juniper Square has LP data but no deal pipeline. Dealpath has deal data but no fund management. Only Winston has all of it — which means only Winston can assemble the full context brief for a handoff.

**Prospect One-Liner:**
"When your partner delegates a VIP LP call to an associate, Winston assembles the full context brief — prior conversations, fund positions, capital call history, and a pre-drafted response — in 10 seconds. No dropped context, ever."

**Connection to Existing Winston Code:**
- Relevant files/components: The spec in `docs/feature-radar/context-aware-delegation.md` names exact files: `backend/app/services/delegation_engine.py` (new), `backend/app/routes/ecc.py` (extend), `repo-b/src/app/lab/env/[envId]/ecc/` (delegate UI), `backend/app/services/ai_gateway.py` (context assembly). Data sources confirmed available: `ai_conversations`, `ai_messages`, `ai_decision_audit_log`, `actor`, `actor_role`, `fund`, `capital_call`, `distribution`, `attachment`, `cc_corpus_document`
- Can extend existing: **Yes** — AI gateway (189KB service) handles context assembly; existing RAG infrastructure handles document retrieval; SSE streaming handles real-time status updates; CRM service handles actor/role lookup
- Schema changes needed: **Yes** — delegation tracking table: `{delegation_id, queue_item_id, delegator_id, delegate_id, context_brief, draft_response, status, delegated_at, resolved_at, sla_deadline}`
- Related recent commits: ECC triage queue already exists per 32 lab environment types (confirmed)

**Implementation Complexity:** Medium
**Estimated Effort:** 1.5-2 weeks (delegation engine: 3 days; context assembly: 3 days; UI: 3 days; tracking/SLA: 2 days)
**Dependencies:** ECC environment must be healthy; AI gateway context assembly patterns from existing chat workspace

**Build Priority Score:** 9/10

---

## #5 — Investor Sentiment Radar: NLP Intelligence Layer on LP Communications

**Competitor Signal:** Juniper Square launched AI-powered CRM with NLP and predictive modeling on investor emails, docs, and communications — extracting sentiment and intent signals.

**Source Competitor(s):** Juniper Square

**Creative Lens Applied:** Lens 2 (Cross-Pollination) + Lens 4 (Time Travel)

**The Leapfrog Idea:**
Juniper Square is parsing LP emails to extract sentiment tags and CRM notes. Winston does something more valuable: it builds a continuous LP relationship graph that tracks not just what LPs say, but what they're *signaling* — re-up intent, redemption risk, co-investment interest, emerging concerns — by correlating communication patterns with capital behavior (are they responding to capital calls slower than their historical average? Did they ask about NAV right after a poor quarter?). The 2028 version isn't "tag this email as positive" — it's "GP, you have 4 LPs who are statistically likely not to re-up for Fund IX based on their engagement trajectory over the last 2 quarters, and here's the personalized campaign Winston drafted to re-engage each one." The AI doesn't just read the room — it predicts what's about to change in the room and acts before it does.

**Why This Is Better Than Copying:**
Juniper Square's NLP CRM retroactively classifies communications. Winston's Investor Sentiment Radar is prospective — it predicts LP behavior and triggers proactive outreach. The key insight is combining communication signals with *capital behavior* data (call response timing, distribution reinvestment rates, query frequency around NAV) — a cross-signal view only possible in a unified platform.

**Prospect One-Liner:**
"Winston told us an LP was showing redemption signals three months before they gave notice — we re-engaged, they stayed in, and we closed them for a larger commitment in Fund IX."

**Connection to Existing Winston Code:**
- Relevant files/components: CRM service suite (9 files confirmed in CAPABILITY_INVENTORY), engagement tracking service, `backend/app/services/extraction_engine.py` (existing extraction engine to extend with classifier), CRM routes, `finance.list_capital_calls` / `finance.list_capital_activity` (behavioral signals from capital data), RAG system (existing NLP infrastructure)
- Can extend existing: **Yes** — Feature radar 2026-03-22 already identified this as a top enhancement: "Add investor communication classifier to extraction engine, wire into CRM engagement tracking." The leapfrog is the behavioral correlation layer (capital timing + communication frequency → re-up probability model)
- Schema changes needed: **Yes** — `lp_sentiment_snapshot`: `{investor_id, period, sentiment_score, intent_signal, re_up_probability, behavioral_flags[], last_updated}`. Extend existing `engagement_tracking` table with temporal pattern fields
- Related recent commits: None specific; feature radar called this out as "coding session actionable today" for the base version

**Implementation Complexity:** Medium (base NLP classifier: Low; behavioral correlation: Medium)
**Estimated Effort:** Base classifier: 1 week; behavioral correlation + re-up probability model: 1 additional week
**Dependencies:** CRM service must have LP communication history seeded; extraction engine pipeline health

**Build Priority Score:** 8/10

---

## #6 — OM Autopilot: From Email Attachment to Ranked Deal in 90 Seconds

**Competitor Signal:** Dealpath AI Data Extract claims 95% accuracy on 90+ fields in under 1 minute. Dealpath Connect puts them at the top of every broker's distribution list. This is the most concrete competitive threat in the deal pipeline.

**Source Competitor(s):** Dealpath

**Creative Lens Applied:** Lens 1 (AI-Native Reimagination) + Lens 5 (Workflow Elimination)

**The Leapfrog Idea:**
Dealpath extracts data from OMs. Winston's AI-native version eliminates the act of forwarding the OM entirely. Winston monitors an email inbox (or broker distribution list) for new deal submissions, auto-ingests the OM attachment, runs 90+ field extraction, scores the deal against the fund's active investment criteria, computes a preliminary IRR range from extracted financials, and surfaces a ranked summary in the Winston deal pipeline before the analyst has even opened the email. The analyst's first interaction with the deal is a Winston summary card — not a 20-page PDF. At no point does an analyst manually extract data. The workflow is eliminated, not accelerated.

**Why This Is Better Than Copying:**
Dealpath's extraction is a tool the analyst uses. Winston's OM Autopilot is a pipeline that runs without the analyst. The difference: Dealpath reduces a 45-minute task to 5 minutes. Winston reduces it to zero. The analyst spends time deciding whether to pursue — not processing.

**Prospect One-Liner:**
"You forward an OM to deals@yourfirm.com and by the time you open Winston, the deal is already scored, ranked against your current pipeline, and a preliminary IRR model is ready for review."

**Connection to Existing Winston Code:**
- Relevant files/components: Document pipeline (extraction engine with profiles + writeback, OCR parser, text extractor — confirmed in CAPABILITY_INVENTORY); Deal Radar (deal pipeline + scoring, confirmed); `documents.init_upload` / `documents.complete_upload` MCP tools; `finance.pipeline_radar`; extraction profiles schema; Gmail MCP connector (available in Cowork environment)
- Can extend existing: **Yes** — document ingestion pipeline exists; needs OM-specific extraction schema (90+ fields as specced in `product-opportunities/dealpath-2026-03-20.md`); email-triggered pipeline needs a webhook/forwarding address handler (new: ~3 days); Deal Radar auto-population from extracted fields (new: ~2 days)
- Schema changes needed: **Yes** — `om_extraction_profile` table: field definitions, confidence thresholds, Deal Radar mapping. New `email_ingest_webhook` config
- Related recent commits: OM Data Extract already specced in `feature-radar/2026-03-20-competitor-derived.md` (priority 8/10) — this is the AI-native framing of that same build

**Implementation Complexity:** Medium
**Estimated Effort:** OM extraction schema: 3 days; email-triggered ingest handler: 3 days; Deal Radar auto-population: 2 days; scoring on ingest: 2 days = ~2 weeks

**Dependencies:** Document extraction pipeline (deployed); Deal Radar (deployed); email webhook routing (new infrastructure)

**Build Priority Score:** 8/10

---

## #7 — Winston MCP Plugin Registry: The Extensible Intelligence Layer

**Competitor Signal:** Cherre Agent.STUDIO lets clients build custom AI agents from their data without code. NVIDIA Agent Toolkit signed 17 enterprise partners by letting them build domain-specific agents on shared infrastructure. The noon feature radar (2026-03-22) scored this a 5/5 on differentiation: "no REPE competitor offers client-extensible AI tool registries."

**Source Competitor(s):** Cherre, NVIDIA Agent Toolkit

**Creative Lens Applied:** Lens 6 (Network Effects & Composability) + Lens 3 (Negative Space)

**The Leapfrog Idea:**
Cherre's Agent.STUDIO requires clients to build agents *inside* Cherre's data layer — you can only automate what Cherre has. Winston's plugin registry is different: firms register their own MCP tools that Winston executes alongside its first-party tools. A PE firm's proprietary waterfall model, their custom compliance checker, their internal market data API — all become callable by Winston's AI in the same conversation as `finance.run_waterfall` and `rag.search`. The network effect: as firms build and optionally share domain-specific tools (anonymized), Winston's collective intelligence compounds. A co-investment tool built by one firm becomes available to the whole network. This is the NVIDIA partner model applied to institutional real estate AI — a platform that gets smarter as more firms plug in.

**Why This Is Better Than Copying:**
Cherre Agent.STUDIO is a closed sandbox within Cherre's data. Winston's plugin model is open — firms bring their *own* data, models, and workflows and Winston becomes the orchestration layer that makes them all AI-callable. This is the architectural moat: Winston becomes the layer that connects *every* firm-specific intelligence asset, not just the ones Novendor builds.

**Prospect One-Liner:**
"Your proprietary underwriting model becomes an AI-callable tool in Winston — so instead of running it manually, you just ask Winston to run a deal through your model alongside our standard scoring."

**Connection to Existing Winston Code:**
- Relevant files/components: `backend/app/mcp/` (MCP registry — tool schemas, permissions, audit policy, confirmed 31 tool categories); `backend/app/mcp/tools/` directory (tool implementations); `agents/mcp.md` (MCP agent contract); noon feature radar 2026-03-22 specced this as: "enhancement to `backend/app/mcp/` tool registration API, new admin UI in MCP management pages, schema validation for third-party tool definitions"
- Can extend existing: **Yes** — MCP registry exists; needs (a) tool registration API endpoint for client-defined tools, (b) JSON schema validator for tool definitions, (c) sandboxed execution environment for third-party tool calls, (d) admin UI for tool approval workflow
- Schema changes needed: **Yes** — `mcp_plugin_registration`: `{plugin_id, firm_id, tool_name, tool_schema, endpoint_url, auth_config, approval_status, created_at}`
- Related recent commits: None specific; this is net-new feature but extends existing architecture

**Implementation Complexity:** High
**Estimated Effort:** Registration API + schema: 1 week; sandbox execution: 1 week; admin UI: 1 week; security/auth: 3 days = ~4 weeks
**Dependencies:** MCP registry (deployed); authentication system; sandboxed execution environment for third-party tools

**Build Priority Score:** 8/10

---

## #8 — Market Regime → Portfolio Action: Closing the Loop from Signal to Decision

**Competitor Signal:** ARGUS Intelligence extended scenario simulation to portfolio level. IBM/Confluent close signaling real-time data pipelines as table stakes. Cherre's data fabric enables cross-property signal aggregation.

**Source Competitor(s):** Altus/ARGUS, Cherre

**Creative Lens Applied:** Lens 2 (Cross-Pollination) + Lens 1 (AI-Native Reimagination)

**The Leapfrog Idea:**
`market_regime_engine.py` just landed in the repo — a multi-asset regime classifier (risk_on / risk_off / transitional / stress) that reads equities, rates, credit, and crypto signals. ARGUS does portfolio scenario modeling but requires the analyst to *manually* trigger scenarios. The AI-native version: Winston continuously runs the market regime engine, detects regime shifts, and *automatically propagates* the implications to the portfolio — re-running cap rate stress tests across all assets, projecting which loans move closer to covenant breach under the new rate assumptions, identifying which deals in the pipeline should be repriced. The analyst doesn't ask Winston "what's the impact of rising rates?" Winston has already answered that question before you ask it and is waiting to brief you.

**Why This Is Better Than Copying:**
ARGUS is a model you run. Winston is a system that runs itself. When the market regime shifts from `transitional` to `risk_off`, Winston doesn't wait for an analyst to open a scenario tool — it has already computed the portfolio-wide implications and surfaced a briefing. This is only possible because Winston owns the market regime engine, the portfolio data, and the covenant monitoring in one system.

**Prospect One-Liner:**
"When the market regime shifted to risk_off last Tuesday, Winston automatically re-ran our portfolio stress tests, flagged 3 assets with elevated covenant risk, and had the briefing ready before our morning call."

**Connection to Existing Winston Code:**
- Relevant files/components: `backend/app/services/market_regime_engine.py` (just built — regime classification logic), `backend/app/routes/market_regime.py`, `repo-b/src/components/market/RegimeClassifierWidget.tsx` (frontend widget already exists), `finance.stress_cap_rate`, `finance.sensitivity_matrix`, `finance.check_covenant_compliance` (queued in BUILD-01)
- Can extend existing: **Yes** — market regime engine outputs `regime_label` + `confidence` + `cross_vertical_implications`. Need to wire this output to a portfolio-wide cascade: (a) trigger `finance.stress_cap_rate` on regime shift, (b) re-evaluate `finance.check_covenant_compliance` with regime-adjusted assumptions, (c) surface a Portfolio Impact Brief in the Winston chat workspace as a proactive assistant message
- Schema changes needed: **Minimal** — add `triggered_by_regime_shift` flag to stress test runs; new `portfolio_regime_briefing` table for auto-generated impact summaries
- Related recent commits: `market_regime_engine.py` + `RegimeClassifierWidget.tsx` just landed (confirmed in file listing)

**Implementation Complexity:** Medium
**Estimated Effort:** Regime-to-portfolio cascade trigger: 3 days; proactive briefing generation: 2 days; UI notification: 2 days = ~1 week

**Dependencies:** BUILD-01 (covenant alerts) should be shipped first; market regime engine (just deployed ✓)

**Build Priority Score:** 8/10

---

## #9 — Acqui-Intelligence: The Pre-LOI Due Diligence Accelerator

**Competitor Signal:** Dealpath AI Data Extract + AI Recommended Comps (automated comp identification from MSCI/RCA data integration). Juniper Square's LP subscription document extraction for onboarding.

**Source Competitor(s):** Dealpath

**Creative Lens Applied:** Lens 4 (Time Travel) + Lens 2 (Cross-Pollination)

**The Leapfrog Idea:**
The 2028 version of deal due diligence doesn't start after LOI — it starts the moment the OM enters the pipeline. Winston runs a continuous due diligence engine on every deal in the pipeline: extracting OM data (90+ fields), cross-referencing the seller's ownership history from public records, running the market regime signal against the submarket, finding historical lease roll patterns for similar assets in the MSA, computing the spread between the asking cap rate and the current market cap rate for the asset class, and generating a "go/no-go brief" with conviction score. By the time an analyst is assigned, Winston has already done 80% of initial DD — not just data extraction, but synthesis and judgment. The analyst starts at the 20% that requires human insight: site visit read, sponsor relationship, local broker validation.

**Why This Is Better Than Copying:**
Dealpath extracts data from the OM. Winston's Acqui-Intelligence synthesizes data *from the OM, from public records, from market data, and from the firm's own portfolio history* — a level of cross-source synthesis that requires a unified data layer Dealpath doesn't have. The comp recommendation isn't just "here are similar recent sales" — it's "here are 3 comps and here's why this deal is priced 15 bps tighter than the market, which is the risk factor you should interrogate."

**Prospect One-Liner:**
"By the time your analyst opens the deal, Winston has already extracted the OM, found 3 market comps, run the cap rate spread analysis, and flagged the single biggest underwriting risk to investigate."

**Connection to Existing Winston Code:**
- Relevant files/components: Document extraction pipeline (deployed), Deal Radar + deal scoring (deployed), `finance.deal_geo_score` (geographic scoring), `backend/app/services/market_regime_engine.py` (market signal), MSA zone intelligence (Phase 1 queued), `finance.stress_cap_rate` (cap rate analysis), `finance.run_sale_scenario` (exit modeling)
- Can extend existing: **Yes** — this is OM Autopilot (Idea #6) + Deal Signal Radar (Idea #1) + market regime (Idea #8) composed. The individual pieces exist or are being built; the leapfrog is the *automated composition* of all three into a pre-LOI brief
- Schema changes needed: **Minimal** — new `acqui_intelligence_brief` table; extend Deal Radar records with `auto_dd_status`, `conviction_score`, `key_risk_flag`
- Related recent commits: Market regime engine just landed; provides the market context layer

**Implementation Complexity:** High (as standalone) / Medium (if Idea #6 and Idea #8 are built first)
**Estimated Effort:** 2-3 weeks after Ideas #6 and #8 are deployed
**Dependencies:** OM Autopilot (#6), Market Regime → Portfolio Action (#8), MSA data connectors

**Build Priority Score:** 7/10

---

## #10 — Winston for Lenders: The Credit Portfolio Surveillance Layer

**Competitor Signal:** Yardi Nova Credit integration for affordable housing — tenant eligibility screening bolted onto Voyager. Winston has a 1,427-line credit decisioning engine already deployed. The Nova/Yardi integration is point-to-point for a narrow use case; it doesn't generalize.

**Source Competitor(s):** Yardi

**Creative Lens Applied:** Lens 3 (Negative Space) + Lens 6 (Network Effects & Composability)

**The Leapfrog Idea:**
No REPE platform has a credit surveillance layer for *portfolio companies' debt* — the loans on your assets, not the LP credit. Winston's credit decisioning engine was built for consumer credit underwriting (full walled garden, 15-page environment, 1,427-line engine). The negative space: the exact same architecture — policy evaluation, risk assessment, exception handling, audit trail — applies to commercial real estate loan surveillance. A REPE CFO managing 30 loans across 20 assets needs to know which loans are approaching credit events (covenant breach, maturity, rate reset), which lenders are likely to be accommodating vs. adversarial, and which loan structures create cross-collateralization risk. Winston's credit engine is sitting dormant for this use case. Point it at the REPE debt stack and you've built something no competitor offers: AI-native credit surveillance for your entire loan portfolio, not just one-off covenant alerts.

**Why This Is Better Than Copying:**
Yardi's credit integration is tenant-facing (can this renter qualify?). Winston's credit surveillance is portfolio-facing (are my existing loans creating systemic risk?). The architectural advantage: Winston already has the credit decisioning framework, the policy evaluation engine, and the audit trail — this is a domain extension, not a greenfield build.

**Prospect One-Liner:**
"Winston runs the same AI credit analysis on your loan portfolio that banks run on borrowers — surfacing which of your 30 loans are the highest risk, and what to do about each one, before your next lender call."

**Connection to Existing Winston Code:**
- Relevant files/components: `backend/app/services/credit*.py` (1,427-line decisioning engine), `backend/app/routes/credit*.py`, credit schema (`credit_decision`, `credit_policy`, `credit_exception`, `credit_case`, `credit_portfolio` tables), `repo-b/src/app/lab/env/[envId]/credit/` (15-page credit environment), credit MCP tools; `finance.check_covenant_compliance` (BUILD-01); `backend/app/services/market_regime_engine.py` for market context
- Can extend existing: **Yes** — the credit decisioning architecture is the engine; it needs (a) a CRE loan data model (vs. the current consumer credit model), (b) policy definitions for commercial covenant compliance (DSCR, LTV, DSCR yield), (c) a portfolio-level surveillance view that aggregates individual loan risk scores
- Schema changes needed: **Yes** — extend credit schema with CRE-specific fields: `loan_type: commercial_re`, `collateral_asset_id`, `covenant_set`, `maturity_date`, `rate_type`, `cross_collateral_flags`
- Related recent commits: Credit decisioning environment confirmed FULLY BUILT per CAPABILITY_INVENTORY.md

**Implementation Complexity:** Medium
**Estimated Effort:** CRE loan data model: 3 days; policy adaptation: 3 days; portfolio surveillance view: 3 days; UI integration: 2 days = ~2 weeks

**Dependencies:** BUILD-01 (covenant alerts) as foundation; credit decisioning environment (already fully deployed ✓)

**Build Priority Score:** 7/10

---

## Summary: Priority Stack

| Rank | Idea | Lens(es) | Competitor Signal | Complexity | Build Priority |
|---|---|---|---|---|---|
| 1 | Deal Signal Radar (pre-market deal intelligence) | 1+5 | Dealpath Connect | High | **9** |
| 2 | Portfolio Pulse (continuous reporting, no quarterly cadence) | 5+4 | Juniper Square + ARGUS | Medium | **9** |
| 3 | Covenant Intelligence (lender relationship OS) | 4+1 | Yardi | Medium | **9** |
| 4 | Context-Aware Delegation Engine (fully specced) | 6+3 | Cherre + JS | Medium | **9** |
| 5 | Investor Sentiment Radar (behavioral NLP) | 2+4 | Juniper Square | Medium | **8** |
| 6 | OM Autopilot (zero-touch deal ingestion) | 1+5 | Dealpath | Medium | **8** |
| 7 | Winston MCP Plugin Registry (extensible tool layer) | 6+3 | Cherre + NVIDIA | High | **8** |
| 8 | Market Regime → Portfolio Action (signal-to-decision) | 2+1 | ARGUS + Cherre | Medium | **8** |
| 9 | Acqui-Intelligence (pre-LOI DD accelerator) | 4+2 | Dealpath | High | **7** |
| 10 | Winston for Lenders (CRE credit surveillance) | 3+6 | Yardi | Medium | **7** |

---

## Recommendations for 3 PM Coding Session

**Start immediately (data layer exists, architecture proven):**
- **Idea #8 — Market Regime → Portfolio Action** — `market_regime_engine.py` just landed. Wire regime output to `finance.stress_cap_rate` cascade. ~1 week. No blockers.
- **Idea #3 — Covenant Intelligence** — BUILD-01 (3-5 days) is the prerequisite. Schedule BUILD-01 first, then extend with trajectory modeling.

**Build this week (build queue items #2 and #4 confirm intent):**
- **Idea #2 — Portfolio Pulse** — BUILD-02 (LP report auto-assembly) is in the competitor reversal queue. Frame it as continuous reporting at shipping time, not just report automation.
- **Idea #4 — Context-Aware Delegation** — Fully specced in `context-aware-delegation.md`. All data sources confirmed available. No architectural unknowns.

**Phase for next sprint (needs MSA data pipeline or OM extraction first):**
- **Idea #5 — Investor Sentiment Radar** — Base NLP classifier can ship this week; behavioral correlation is next sprint
- **Idea #6 — OM Autopilot** — Email webhook is the only net-new infrastructure piece; extraction and Deal Radar exist

**Architectural bets (schedule for planning discussion):**
- **Idea #1 — Deal Signal Radar** — Depends on MSA pipeline Phase 1 unblocking. High impact, right direction, needs infrastructure.
- **Idea #7 — MCP Plugin Registry** — Correct strategic direction; 4-week build when a client specifically requests extensibility.
- **Ideas #9 & #10** — Correct direction; natural sequels after ideas #6 and #3 ship.

---

## What Makes This Output Different from Other Radar Files

The `feature-radar` and `product-opportunities` tasks identify *gaps* and *parity builds*. This output does something different:

1. **Reframes existing build queue items** (#2, #3, #4, #6) as category-defining narratives, not just feature completions. BUILD-02 shipped as "LP report automation" is a nice feature. BUILD-02 shipped as "Portfolio Pulse — the continuous intelligence layer that makes quarterly reports obsolete" is a category statement.

2. **Identifies compound features** (#8, #9) that emerge from *combining* recently-shipped capabilities (market regime engine) with existing ones — opportunities that didn't exist before the market regime engine landed.

3. **Names the architectural moat**: Every idea in this list is harder (or impossible) for Yardi, Dealpath, Juniper Square, or Cherre to build not because it requires a clever algorithm, but because it requires all of Winston's data layers to exist simultaneously in one system. That's the leapfrog.
