# Deploy Smoke Test — 2026-03-21 10:30 AM

## Deploy Activity

| Project | Deploys (last 12h) | Latest Status | Commit |
|---|---|---|---|
| consulting-app | 4 | ERROR (latest 2), READY (prior 2) | `5e6bc86` — fix(test): update repeNavigation test to include IR Review and AI Audit nav items |
| floyorker | 1 | ERROR | `b795865` — Merge PR #17: restructure floyorker site for championship authority |
| backend | 0 | N/A | No deployments configured |

### Build Failure Details

**consulting-app** (latest 2 deploys — both ERROR):
- **Error**: `Type error: Could not find a declaration file for module '../../../../../src/app/api/brief/route.js'. '/vercel/path0/repo-b/src/app/api/brief/route.js' implicitly has an 'any' type.`
- **Cause**: A `.js` file (`src/app/api/brief/route.js`) was added without a TypeScript declaration. The `dailybrief` commit introduced this file; the subsequent test fix commit didn't resolve the type error.
- **Production impact**: None — Vercel is still serving from the last successful deploy (`dpl_DoPyd6qiCpbLigwKq1Ddqunhy61W`, commit `a03764d`).
- **Fix**: Either rename `route.js` → `route.ts` or add a `.d.ts` declaration file, or add `// @ts-nocheck` at the top.

**floyorker** (all 4 deploys in history are ERROR):
- Separate project, not critical for Winston demos. All deploys have been failing.

## Smoke Test Results

| Test | Status | Method | Notes |
|---|---|---|---|
| Home page (paulmalmquist.com) | ✅ PASS | Browser | Renders correctly with "Winston" branding, Login as Admin / Login to Environment buttons, tagline visible |
| Admin login | ✅ PASS | Browser | Admin code accepted, redirected to /admin Control Tower |
| Control Tower (admin dashboard) | ✅ PASS | Browser | 6 active environments, AI Gateway ONLINE, System Status "All services operational", last check 9:58 AM |
| Lab environments (/lab/environments) | ✅ PASS | Browser | 6 environments listed, all HEALTHY status. Environments: Paul Malmquist Resume, StonePDS, Meridian Apex Holdings, Meridian Capital Management, Novendor, floyorker |
| Fund Portfolio (Meridian Capital Mgmt) | ✅ PASS | Browser | 3 funds rendered correctly. Dollar amounts properly formatted ($500.0M, $900.0M, $600.0M AUM). KPI strip: 3 funds, $2.0B total commitments, $1.4B portfolio NAV, 34 active assets |
| AI Chat — Winston response | ✅ PASS | Browser | Query "What funds do we have?" returned correct response listing all 3 funds with metadata. Response time: 1923ms, 1 tool call, 35,400 tokens, Lane B |
| AI Chat — Bug 0 (tool spam) | ✅ PASS | Browser | No raw tool names in conversation body. Tool trace (`repe.get_environment_snaps...`) visible only in right sidebar debug panel (intentional). Clean narrative in main response |
| Deal Radar (Pipeline) | ✅ PASS | Browser | Radar chart renders with 17 active deals ($1.15B pipeline value, $327.0M equity required, $67.4M avg deal). Sector labels visible (Multifamily, Industrial, Retail, Office, Medical Office, Hospitality). Stage counts: Sourced 2, Screening 5, LOI 3, Due Diligence 2 |
| novendor.ai | ✅ UPDATED | WebFetch | No longer placeholder — full marketing site live with "Put AI to Work" hero, service offerings (Operational Assessment, AI Concierge, Legacy SaaS Migration), industry pages (REPE, Consumer Credit, Medical, Legal), and "Book strategy call" CTA |

## Issues Found

### 1. Build Broken — consulting-app (HIGH)
The latest 2 Vercel deploys are failing due to a TypeScript declaration error in `src/app/api/brief/route.js`. Production is unaffected (serving last good deploy) but **no new code can ship until this is fixed**.

### 2. /login Route (LOW — retracted)
Earlier WebFetch testing showed `/login` returning a 404, but browser testing confirmed the login flow works correctly: Home → "Login as Admin" → `/login?loginType=admin` → enter code → redirects to `/admin`. The route works when accessed through the proper flow.

### 3. Operational Alerts (INFO)
Admin Control Tower shows 2 operational alerts:
- Meridian Apex Holdings: Inactive for 21 days
- StonePDS: Inactive (exact duration not captured)

These are expected for demo environments that aren't being actively used.

## novendor.ai Status Update
The site has been upgraded from a placeholder to a **full marketing website** with:
- Hero section: "Put AI to Work"
- Three service offerings
- Four industry verticals (REPE, Consumer Credit, Medical, Legal)
- CTA: "Book strategy call"

## Verdict: ✅ SAFE TO DEMO

**All authenticated pages pass.** Production is stable and rendering correctly across all tested surfaces (Fund Portfolio, AI Chat, Deal Radar, Lab Environments, Admin Control Tower). Bug 0 (tool spam) is NOT present — Winston chat returns clean narrative responses.

**One action needed:** Fix the TypeScript error in `src/app/api/brief/route.js` so new deploys can succeed. This does not affect the current production state but blocks future pushes.
