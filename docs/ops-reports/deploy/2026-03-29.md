# Deploy Smoke Test — 2026-03-29 5:30 AM

## Assumptions

- Vercel API is the source of truth for deploy status [HIGH]
- WebFetch cannot authenticate, so authenticated pages (admin, lab environments) are tested indirectly via backend health and gateway health [HIGH]
- Backend Railway health endpoint (`authentic-sparkle-production-7f37.up.railway.app/health`) is the source of truth for API health [HIGH]
- AI Gateway health endpoint is the source of truth for Winston AI status [HIGH]
- Last smoke test report was 2026-03-28; used as regression baseline [HIGH — verified via filesystem]

## Deploy Activity

| Project | Latest Status | Latest Commit | Deploy ID |
|---|---|---|---|
| consulting-app | READY (production) | `12d4dcc` — ops-digest: 2026-03-29 morning digest and LATEST.md refresh | `dpl_7fQ9rq8ALtsFXDfgV3ouTRTo9UZh` |
| backend (Vercel) | N/A | No Vercel deployments — deployed via Railway | — |

### consulting-app Deploy Detail

**Latest production deploy:** `dpl_7fQ9rq8ALtsFXDfgV3ouTRTo9UZh` — **READY** [method: Vercel API] [HIGH]
- Commit: `12d4dcc` — ops-digest: 2026-03-29 morning digest and LATEST.md refresh
- Author: Paul Malmquist (Co-Authored-By: Claude Opus 4.6)
- Note: Was BUILDING at start of smoke test run, transitioned to READY during checks. Confirmed READY via `get_project` API call.

**Recent deploy history (last ~24h on main):**
1. `12d4dcc` — ops-digest: 2026-03-29 morning digest → **READY** (current production)
2. `a31c286` — feat: trades execution layer, lab v1 API consolidation, repo-c removal → **READY** (after 4 ERROR attempts with dirty git state)
3. `a31c286` — same commit, 3 prior attempts → **ERROR** (gitDirty flag set)
4. `a31c286` — same commit, initial attempt → **ERROR** (gitDirty flag set)
5. `8e39a2e` — deploy2 → **READY**
6. `f1936b4` — deploy → **READY**
7. `944e535` — fix(trading-lab): fix hooks-before-return → **READY**

**Notable:** The `a31c286` commit (trades execution layer) had 4 consecutive ERROR deploys before succeeding on the 5th attempt. The errors were caused by `gitDirty: "1"` flag, suggesting uncommitted changes in the build environment. The final successful deploy did not have the gitDirty flag. [HIGH — source: Vercel API deploy metadata]

## Smoke Test Results

| # | Test | Status | Method | Confidence | Notes |
|---|---|---|---|---|---|
| 1 | Vercel Build (consulting-app) | **PASS** | Vercel API | HIGH | Latest production deploy `dpl_7fQ9rq8ALtsFXDfgV3ouTRTo9UZh` is READY |
| 2 | Homepage (www.paulmalmquist.com) | **PASS** | Vercel web_fetch | HIGH | HTTP 200. Winston branding, "Login as Admin" / "Login to Environment" buttons, "Request a walkthrough" CTA present. Build ID: `s10h9hM5DCYHW8Kzn26jD`. Non-www redirects to www (307) as expected. |
| 3 | App Shell (/app) | **PASS** | Vercel web_fetch | HIGH | HTTP 200. Redirects to /login as expected for unauthenticated access. Next.js app shell loads with CSS, fonts, JS chunks. Client-side rendering bailout present (expected for auth-gated page). |
| 4 | Lab Environments (/lab) | **PASS** | Vercel web_fetch | HIGH | HTTP 200. Redirects to /login as expected for unauthenticated access. Same app shell loads successfully. |
| 5 | Backend Health (/health) | **PASS** | WebFetch (Railway) | HIGH | Returns `{"ok": true}` |
| 6 | AI Gateway Health | **PASS** | WebFetch (Railway) | HIGH | Returns `enabled: true`, model: `gpt-5-mini`, embedding: `text-embedding-3-small`, `rag_available: true`, message: null (no errors) |
| 7 | Winston AI Response | **PASS (indirect)** | AI Gateway health | MEDIUM | Gateway reports enabled=true, RAG available, model configured. Cannot send live test prompt without auth |
| 8 | Auth Endpoint | **SKIPPED** | — | — | Auth handled via Supabase Auth, no standalone endpoint to test without credentials |

**Overall: 6/8 passed, 0 failed, 1 skipped, 1 indirect pass**

## Regression Check vs. Yesterday (2026-03-28)

| Issue from 2026-03-28 | Status Today |
|---|---|
| Authenticated page tests skipped | **PERSISTS** — same limitation, WebFetch cannot auth |
| Auth endpoint 404 | **DROPPED** — removed from test suite, not a real endpoint |
| Lab page returned 404-like redirect | **IMPROVED** — now returns HTTP 200 with login redirect (was previously showing 404 template) |

**New issues since 2026-03-28:**

### 1. Trades Execution Commit Had 4 ERROR Deploys (MEDIUM — resolved)
Commit `a31c286` (trades execution layer, lab v1 API consolidation, repo-c removal) failed 4 times before succeeding. All failures show `gitDirty: "1"` in metadata, indicating uncommitted changes were present during build. The 5th attempt succeeded without the dirty flag. This is resolved but suggests a CI hygiene issue with the build trigger process. [Source: Vercel API deploy metadata] [HIGH confidence on facts]

### 2. High Deploy Churn (LOW — informational)
7 deploys to production in the last ~18 hours, including 4 errors. This is higher churn than yesterday's 6 clean deploys. The errors were all on the same commit and are resolved. [Source: Vercel API]

## Verdict: PASS — PRODUCTION HEALTHY

Production consulting-app is READY on the latest commit (`12d4dcc` — deployed today). All 6 executable checks passed. Backend Railway instance is healthy (`{"ok": true}`). AI Gateway is fully operational with GPT-5-mini, embeddings, and RAG all enabled. No active regressions.

**Deploy velocity:** 3 successful production deploys + 4 transient ERROR deploys (now resolved) in the last 18 hours. Current production is stable.

**Comparison to yesterday:** Same overall health profile. Lab page check improved from SKIPPED to PASS (returns proper 200 with login redirect). No new failures.

## Self-Critique

- Winston AI test is indirect (gateway health, not a live prompt). Scored MEDIUM confidence. A live browser test would be more definitive but requires auth. [REVIEW — acceptable limitation, consistent with previous reports]
- Cannot verify admin dashboard, fund portfolio, or environment functionality without auth. [REVIEW — same limitation as every previous run]
- The 4 ERROR deploys on `a31c286` are concerning for CI hygiene but are resolved. Root cause (gitDirty flag) suggests build was triggered while uncommitted changes existed. [REVIEW — worth monitoring]
- All PASS items were verified via actual API calls or web fetches. No PASS was assumed without execution.
