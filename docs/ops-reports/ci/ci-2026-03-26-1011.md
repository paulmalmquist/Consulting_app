# CI Health Report — 2026-03-26 10:11 UTC

## Main Branch Status: GREEN

The main CI workflow is passing. Latest runs:

| Run ID | Workflow | Status | Timestamp |
|---|---|---|---|
| 23573185439 | CI | success | 2026-03-26 01:38 UTC |
| 23565459671 | CI | success | 2026-03-25 21:38 UTC |
| 23541469899 | CI | success | 2026-03-25 12:40 UTC |

### Perf Nightly: FAILED (infra, not code)

Run 23580721699 (`Perf Nightly`, 2026-03-26 06:27 UTC) failed at **Initialize containers** — Docker pull for `imresamu/postgis-pgvector:16-3.5` was denied (image may have been removed or renamed on Docker Hub). This is an infrastructure/config issue, not a code regression. No fix applied on main; the workflow YAML needs updating to a valid image tag.

## Auto Branch Processing

| Branch | Status | Action Taken |
|---|---|---|
| `auto/afternoon-2026-03-25` | Already merged into main | Deleted from remote |
| `auto/meridian-2026-03-25` | Already merged into main | Deleted from remote |

Both branches were confirmed ancestors of `main` (HEAD `5b5d1cc`). No CI runs were triggered on these branches independently — they were merged via manual commit. Remote refs cleaned up.

## Stale Branch Cleanup

No auto/* branches older than 3 days with failed CI found. All auto/* branches have been removed.

## Action Items

- [ ] Update `Perf Nightly` workflow to use a valid `postgis-pgvector` Docker image (the `imresamu/postgis-pgvector:16-3.5` tag is no longer accessible)

## Local Repo Note

The mounted repo at `/mnt/Consulting_app` has a corrupted git ref (`refs/heads/auto/meridian-2026-03-25.lock.bak`) with filesystem permissions preventing cleanup. This doesn't affect remote or CI but may cause `git fetch` failures in local sessions. Manual removal of the `.lock.bak` file on the host machine is recommended.
