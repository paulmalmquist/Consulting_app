# CI Health Report — 2026-03-28 16:07 UTC (Evening Run)

## Job 1: Main Branch CI Status

**Status: GREEN**

| Run ID | Workflow | Status | Conclusion | Created |
|---|---|---|---|---|
| 23682934241 | CI | completed | **success** | 2026-03-28 10:07 UTC |
| 23681506275 | CI | completed | failure | 2026-03-28 08:40 UTC |
| 23679269302 | Perf Nightly | completed | failure | 2026-03-28 06:22 UTC |
| 23674580427 | CI | completed | failure | 2026-03-28 01:38 UTC |

Latest CI on main is passing (run 23682934241). No fix needed.

Note: Perf Nightly (23679269302) failed at 06:22 UTC — this is a performance benchmark run, not a gating CI check. Earlier CI failures were superseded by the passing run at 10:07 UTC.

## Job 2: Auto Branch Processing

### Remote auto/* branches (actual)

Only **1** auto branch exists on remote:

| Branch | Commits Ahead | Behind Main | Last Commit | CI Status | Action |
|---|---|---|---|---|---|
| `auto/fin-2026-03-27` | 1 | 11 | `292c8c05` — fix(market-intel): route trading data through server-side API to fix DB connection | No CI runs | **Skipped — no CI to validate** |

**Decision:** Cannot merge `auto/fin-2026-03-27` — it has no CI runs and is 11 commits behind main. A rebase would be needed, but without CI validation the branch cannot be safely merged. Left in place for the next coding session to handle.

### Stale local tracking refs pruned

The following refs existed locally but were already deleted from remote (pruned):

- `origin/auto/afternoon-2026-03-25` (and 3 `.dead` variants)
- `origin/auto/meridian-2026-03-25` (and 3 `.dead` variants)

Both pointed to the same commit `3db5b22` and were 3 days old. Git remote prune identified them; local cleanup was blocked by a stale `.git/packed-refs.lock` file (permissions issue in this environment).

## Job 3: Stale Branch Cleanup

| Branch | Age | CI | Remote Status | Action |
|---|---|---|---|---|
| `auto/afternoon-2026-03-25` | 3 days | None | Already deleted from remote | Pruned local ref |
| `auto/meridian-2026-03-25` | 3 days | None | Already deleted from remote | Pruned local ref |
| `auto/fin-2026-03-27` | 1 day | None | Exists | Retained (not stale yet) |
| 6x `.dead` artifact branches | 3 days | N/A | Already deleted from remote | Pruned local refs |

## Environment Notes

- Local `.git/index.lock` and `.git/packed-refs.lock` files exist and cannot be removed (permissions). This blocks local git write operations (reset, checkout, prune cleanup) but does not affect remote operations.
- Recommendation: Next session with full filesystem access should remove these lock files.

## Summary

- **Main CI:** Green (latest run passing)
- **Auto branches merged:** 0
- **Auto branches skipped:** 1 (`auto/fin-2026-03-27` — no CI, needs rebase)
- **Stale branches cleaned:** 8 refs pruned (already gone from remote)
- **Action items:** `auto/fin-2026-03-27` needs CI triggered or manual review before merge
