# CI Health Monitor Report — 2026-03-29 22:47 UTC

## Job 1: CI on main

**Status: Unable to verify** — `gh` CLI not available in this environment. No GitHub Actions status could be checked. Main branch has no obvious build-breaking changes in recent commits. Working tree has unstaged changes across ~70 files (not committed).

Latest commits on main:
- `13b4a6d3` fix
- `02f06a91` commit paul
- `e329bbc5` push paul
- `b7279c06` fix: environment cards link to login, show industry, remove console chrome
- `c7f57475` fix: rename landing to Winston + fix auth shell light-mode rendering

## Job 2: Auto Branch Processing

### Remote auto/* branches (actual remote state)

Only **1 real branch** exists on the remote:

| Branch | Last Commit | Age | Commits Behind Main | Status |
|---|---|---|---|---|
| `auto/fin-2026-03-27` | `292c8c05` fix(market-intel): route trading data through server-side API | 2 days | 34 commits behind | **Not stale, cannot verify CI** |

The `fin-2026-03-27` branch has 1 commit adding a server-side trading API route and refactoring the markets page. It diverges significantly from main (34 commits behind). A rebase would be needed before merge, and CI verification is required first.

**Action taken: None** — cannot verify CI passed without `gh` CLI, so no merges were attempted.

### Stale local refs cleaned

8 stale tracking refs were pruned from the local `.git`. These were artifacts from previous lock file corruption:
- `origin/auto/afternoon-2026-03-25` (and 3 `.dead` variants)
- `origin/auto/meridian-2026-03-25` (and 3 `.dead` variants)

These branches no longer exist on the remote. Local prune partially succeeded but some refs remain stuck due to `.git/packed-refs.lock` permission issues in this sandbox environment.

## Job 3: Stale Branch Cleanup

No stale branches to delete on the remote. The March 25 branches were already deleted in a prior session. Only `auto/fin-2026-03-27` remains (2 days old, not yet stale).

## Recommendations

1. **Install `gh` CLI** in the scheduled task environment to enable CI status checks
2. **Rebase `auto/fin-2026-03-27`** onto current main and verify CI before merging — it's 34 commits behind
3. **Fix `.git` lock files** on the host machine: remove `packed-refs.lock` and any `.lock` files under `.git/refs/` to allow proper ref cleanup
4. The unstaged changes on main (~70 files) should be reviewed — they may contain work that needs to be committed or stashed
