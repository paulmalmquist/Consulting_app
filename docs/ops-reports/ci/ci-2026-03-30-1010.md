# CI Health Report — 2026-03-30 10:10 UTC

## Job 1: CI Status on Main

**Main CI: GREEN** — Last CI run (2026-03-30 02:10 UTC) passed successfully.

**Perf Nightly: FAILED** — Docker image pull failure (infrastructure, not code).
- Run: https://github.com/paulmalmquist/Consulting_app/actions/runs/23731427838
- Root cause: `imresamu/postgis-pgvector:16-3.5` pull denied — "repository does not exist or may require docker login"
- Impact: Perf benchmarks did not execute; no code changes needed
- Action: Docker Hub image may have been removed or renamed. Consider pinning to a verified tag or switching to an alternative PostGIS+pgvector image (e.g., `pgvector/pgvector:pg16` with PostGIS added, or `ankane/pgvector`).

## Job 2: Auto Branch Processing

| Branch | Age | CI Status | Merge Result |
|---|---|---|---|
| `auto/fin-2026-03-27` | 2 days | Only Vercel Preview (passed) — no CI workflow ran | **Skipped — rebase conflicts** |

### auto/fin-2026-03-27 Details
- Commit: `292c8c05` — "fix(market-intel): route trading data through server-side API to fix DB connection"
- 42 commits behind main, 1 ahead
- Rebase onto main produced conflicts in:
  - `repo-b/src/app/api/v1/trading/route.ts` (add/add)
  - `repo-b/src/app/lab/env/[envId]/markets/page.tsx` (content)
- No CI workflow ran on this branch (only Vercel Preview Comments check)
- Branch left in place for manual resolution or next autonomous session

## Job 3: Stale Branch Cleanup

No auto/* branches older than 3 days found. Nothing to clean up.

## Summary

- Main CI is healthy (green)
- Perf Nightly failure is infrastructure-only (Docker image unavailable)
- 1 auto branch exists but has rebase conflicts and no CI coverage — needs manual attention or a fresh attempt
