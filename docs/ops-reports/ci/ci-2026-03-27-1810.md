# CI Health Monitor — 2026-03-27 18:10 UTC (Evening Run)

## Job 1: Main Branch CI Status

**Status: GREEN**

| Run | Status | Conclusion | Time |
|---|---|---|---|
| CI | completed | success | 2026-03-27T20:57:49Z |
| CI | completed | success | 2026-03-27T19:06:53Z |
| CI | completed | success | 2026-03-27T08:42:29Z |
| CI | completed | success | 2026-03-27T01:08:59Z |
| Perf Nightly | completed | **failure** | 2026-03-27T06:28:56Z |

Main CI is passing. Perf Nightly failed at 6:28 AM but this is a non-blocking performance benchmark workflow — not the gating CI pipeline. No action needed.

## Job 2: Auto Branch Processing

### Branches Found on Remote

| Branch | Last Commit | Commits Ahead | Commits Behind | CI Status | Action |
|---|---|---|---|---|---|
| `auto/fin-2026-03-27` | `fix(market-intel): route trading data through server-side API to fix DB connection` (today) | 1 | 4 | No CI run (only Vercel Preview) | **Skipped** — no CI confirmation |

### Previously Existing (Now Deleted from Remote)

The following branches were already deleted from remote before this run. Local stale tracking refs were detected:

- `auto/afternoon-2026-03-25` — pruned (already deleted from remote)
- `auto/meridian-2026-03-25` — pruned (identical to afternoon, same HEAD `3db5b22`)
- 6x `.lock.bak.dead` / `.lock.dead` artifact refs — pruned

**Note:** Local `.git/packed-refs.lock` file is present, preventing full local ref cleanup. A future session should remove this lock file to fully clean local state.

### Merge Summary

- **Merged to main:** 0
- **Skipped (no CI):** 1 (`auto/fin-2026-03-27`)
- **Pruned stale refs:** 8

## Job 3: Stale Branch Cleanup

No auto/* branches older than 3 days with failed CI found. The March 25 branches (`afternoon`, `meridian`) were already deleted from remote by a prior process.

## Recommendations

1. **auto/fin-2026-03-27** — This branch has a useful fix (routing trading data through server-side API). CI should be triggered so it can be merged. It's 4 commits behind main and will need a rebase before merge.
2. **Lock file cleanup** — `/sessions/.../Consulting_app/.git/packed-refs.lock` should be removed to allow clean git operations.
3. **Perf Nightly failure** — Worth investigating if the market-intel DB connection fix in the fin branch is related to the perf nightly failure.
