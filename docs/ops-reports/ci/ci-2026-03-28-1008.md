# CI Health Report — 2026-03-28 10:08 UTC

## Job 1: Main Branch CI

**Status:** FIXED

Main was failing due to 3 Ruff lint errors in `backend/`:

| File | Error | Fix |
|---|---|---|
| `backend/app/services/cro_entity_detail.py` | F401 unused import `resolve_tenant_id` | Removed import |
| `backend/app/services/cro_next_actions.py` | F401 unused import `resolve_tenant_id` | Removed import |
| `backend/app/services/resume_workspace.py` | F841 unused variable `milestones_by_role` | Removed assignment |

Commit `a93e570` pushed to main with all 3 fixes. Ruff now passes clean.

## Job 2: Auto Branch Merge Status

| Branch | Age | CI | Merge Result |
|---|---|---|---|
| `auto/fin-2026-03-27` | 1 day | No CI runs | **Skipped** — rebase has merge conflict in `repo-b/src/app/lab/env/[envId]/markets/page.tsx` |

Note: The original repo showed several `auto/*.lock.bak.dead` and `auto/*.lock.dead` refs from 2026-03-25. These are corrupted local git refs from lock file issues, not real remote branches. Only `auto/fin-2026-03-27` exists on the remote.

## Job 3: Stale Branch Cleanup

No branches older than 3 days exist on the remote. No cleanup needed.

The corrupted local refs (`auto/afternoon-2026-03-25.lock.*`, `auto/meridian-2026-03-25.lock.*`) only appear in the mounted repo's local ref cache and should be cleaned up with `git remote prune origin` when the index.lock is cleared.
