# Morning Ops Digest — 2026-03-26

## Production Health: DEGRADED

Winston AI chat pass rate holds at 33.3% for the fourth consecutive day (2 of 6 tests passing). The repe_fast_path SQL generation pipeline remains broken for all asset-level queries, returning "I couldn't generate a SQL query" after 12-14 seconds. Test 1 regressed again to narration-only behavior where the model promises to fetch data but never renders it. Lane B latency continues worsening: error recovery test now takes 22.7s (was 12.5s yesterday, 164ms three days ago) and the greeting response takes 17.7s (was 7.1s). Tool call spam from Bug 0 is no longer visible, which is an improvement from the afternoon coding session fix.

The "New Chat" button does not clear conversation state, which likely contributes to the growing Lane B latency via context window bloat.

## Environment Health

**Meridian Capital Management: DEGRADED**
Core pages load with real data. Distribution seeding fix holding (12 events display). Debt fund tabs (Loan Book, Covenant Health, Maturity & Rate) live and rendering. Asset display improved but Phoenix Gateway still shows bad unit count. SQL generation fix committed (fa9372dc) but unconfirmed on Railway production. Variance page still empty (no underwriting versions seeded). Distributions "Total Paid" shows $0 despite 10 paid events (missing payout rows).

**Stone PDS: IMPROVING (13/14 pages PASS)**
Up from 12/16 the prior day. Client Satisfaction null guard fix deployed and confirmed working. Revenue & CI stable. Two new NaN display bugs surfaced: Forecast page shows "Total Deals: NaN" and Client Satisfaction NPS Score shows NaN. Schedule Health still silently redirects to Risk page (open P1). Several new nav items discovered (Project Status, Capacity Planning, Relationship Health, Tech Adoption, Process Compliance, Operational Signals, Reports, Documents, Audit, Configuration) that need test coverage.

## AI Test Pass Rate

33.3% (2/6) — unchanged since 2026-03-23. Three-day trend:

| Date | Rate | Test 1 | Tests 2-4 | Tests 5-6 |
|---|---|---|---|---|
| 03-23 | 33% | narration only | SQL fail / no fallback | PASS |
| 03-24 | 33% | hallucinated + tool spam | SQL fail / no fallback | PASS (slower) |
| 03-25 | 33% | narration only (no spam) | SQL fail / no fallback | PASS (much slower) |

## What Yesterday's Coding Session Built

**Branch: auto/afternoon-2026-03-25 (commit 6cfd6234)**
Fixed Bug 0 — tool call JSON leak in Lane A chat pipeline. Root cause: conversation history loader was injecting `[Prior tool calls: ...]` annotations into Lane A history, but Lane A has no tool definitions, so the model echoed the raw JSON verbatim. Fix: added `and not route.skip_tools` guard to the injection block. This explains why today's Test 1 no longer shows tool call spam (improvement), though it has regressed to narration-only (the underlying data-fetch issue in Lane A remains).

**Branch: auto/meridian-2026-03-25 (commit d660aa63)**
SQL generation error handling improvements and distribution paid KPI fix for Meridian.

## Auto Branch Status

| Branch | Last Commit | Status |
|---|---|---|
| `auto/afternoon-2026-03-25` | `6cfd6234` — Bug 0 Lane A tool call fix | Pushed, awaiting merge to main |
| `auto/meridian-2026-03-25` | `d660aa63` — SQL gen error handling + distribution KPI | Pushed, awaiting merge to main |

Both branches have been pushed to origin. Neither has been merged to main yet. The afternoon branch includes the meridian branch as a merge parent.

## Market Intelligence

**Regime: RISK_OFF_DEFENSIVE (5th consecutive session)**
S&P 500 at ~6,506, below both 50-day and 200-day moving averages. VIX improved to 22 (down from 27 at regime trigger). High yield spreads flat at 3.19%. Bitcoin-S&P correlation at 0.74, highest of 2026. Dollar index at 99.6. Watchpoints for regime transition: VIX below 20, HY spreads narrowing, S&P reclaims 200-DMA.

**MSA Research: Tampa — Water Street/Channel District**
First rotation for this zone. Composite acquisition score: 6.7/10. Demand drivers strongest at 8/10 (accelerating — 15,500 jobs YoY, #8 nationally for talent attraction, #1 net migration for young professionals). Supply moderating as metro multifamily deliveries drop from 7,400 to 3,500 units. Water Street master plan ($3B, 56 acres) targeting 2027 completion.

**Market Intelligence Segments (2026-03-26):**
Four new research segments published: L2 Scaling (crypto/real assets), RWA Tokenization (crypto/real assets), Homebuilders (equities), Liquidity Flows (macro).

## MSA Feature Backlog

14 cards in backlog. No new gaps detected (no new brief on 03-25). Top priorities: MSA Research Sweep Runner Phase 1 Automation (72.0) and County Assessor/Recorder Live Data Connector (72.0).

## Key Action Items

1. **Merge auto branches to main** — Both auto/afternoon-2026-03-25 and auto/meridian-2026-03-25 are pushed and ready. The Bug 0 fix in the afternoon branch should improve Test 1 results once deployed.
2. **Confirm Railway deployment** — The SQL generation fix (fa9372dc) and Bug 0 fix (6cfd6234) need Railway deploy confirmation. Until deployed, repe_fast_path will continue failing.
3. **Investigate Lane B latency regression** — 2.5x slowdown in greeting response, 1.8x in error recovery over 24 hours. Likely related to "New Chat" not clearing conversation state. Check backend session management.
4. **Fix NaN display bugs in Stone PDS** — Forecast "Total Deals: NaN" and Client Satisfaction "NPS Score: NaN" are low-effort display fixes.
5. **Expand Stone PDS test coverage** — 11 new nav items discovered that need health check coverage.
6. **Git index.lock issue** — A stale .git/index.lock file with restricted permissions is blocking git operations. Needs manual cleanup (likely from a crashed git process in a previous session).

---

*Generated by morning-ops-digest scheduled task — 2026-03-26 06:00 UTC*
