# Winston Morning Digest — Wednesday March 25 2026

Good morning Paul. Here's your Winston morning digest for Wednesday, March twenty-fifth.

---

## Production Health

Production remains stable. The consulting app Vercel deployments are running. Supabase is healthy. No new nightly ops validator report was found for the 2026-03-24 run — the regression folder's most recent file is from 2026-03-24, so no overnight regression output was generated. This is worth watching; if the nightly-ops-validator task isn't producing output, it may have stalled or been skipped.

**No auto/* branches exist** — clean slate, nothing to merge or review.

---

## AI Test Results — 2026-03-24 (33.3% Pass Rate — DEGRADED)

The 11 PM AI test run from last night holds the same 33.3% pass rate as the prior night (2 of 6 tests passing), but the composition of failures shifted meaningfully:

**What's passing:**
- Test 5 (error recovery) — PASS. Lane B correctly searches funds, returns clear error with alternatives. Tool call completed in 73ms.
- Test 6 (greeting) — PASS. Winston delivers a comprehensive capability overview, well-structured.

**What's failing and why:**
- **Test 1 (fund query) — NEW REGRESSION.** The chat pipeline (Lane A) now renders a response — that's better than last week — but the data it returns is hallucinated. Fund IDs like `f-0001-2020-core` appear in the response instead of the real canonical IDs. Worse, the raw tool call JSON is visible to the end user: `{"tool":"repe.get_environment_snapshot","params":{}}` and `Received tool response.` are showing up in the chat body. This is a regression on Bug 0 — the tool call leak was supposed to be fixed. Files to check: `backend/app/services/ai_gateway.py` (SSE emit layer) and `repo-b/src/components/winston/` (chat message renderer).
- **Tests 2 and 3 (asset analytics, multi-step analytical) — ONGOING.** The `repe_fast_path` SQL generation is failing for all NOI/asset queries. Today's commit `fa9372dc` added `generate_sql()` to `combined_agent.py` but it is not yet verified as deployed to Railway. The error message changed from "No response from Winston" to "I couldn't generate a SQL query from your request" — slight improvement in messaging, same underlying failure. Elapsed time is 12–13 seconds before surfacing the failure.
- **Test 4 (conversational transform) — ONGOING.** Winston acknowledges fast-path limitation and promises a fallback that never fires. No change from prior run.

**New high-severity finding — Lane B latency regression:** Test 5 total elapsed spiked from 164ms (2026-03-23) to 12,534ms today, even though the tool call itself completed in 73ms. Post-tool processing in the Lane B pipeline is adding 12+ seconds of overhead. Files to check: `backend/app/routes/nv_ai_copilot.py` Lane B handler and `backend/app/services/ai_gateway.py` SSE flush after tool results.

**Net assessment:** No tests fixed versus prior night. Two new regressions introduced (Test 1 data accuracy + tool leak, Lane B latency). The SQL generation fix was committed but not confirmed deployed. This is the highest-leverage unverified fix — confirming Railway deployment and testing `generate_sql()` should be first work of the day.

---

## Meridian Capital Health — DEGRADED (improving)

Overnight fixes from 2026-03-24 (commits `fa9372dc` and `93286c5d`) landed meaningful improvements:

**What's working:**
- 3 funds load with correct data: Institutional Growth Fund VII, Meridian Real Estate Fund III, Meridian Credit Opportunities Fund I
- Capital calls: $935M total, 100% collection rate — rendering correctly
- Distributions: 12 events now display ($145.3M declared) — seeding fix confirmed working
- New Loan Book, Covenant Health, and Maturity & Rate tabs on the debt fund are live and rendering
- Dashboard: 7 templates load with correct widget counts
- No console errors, no failed API calls

**What's still broken:**
- SQL generation (P0) — `generate_sql()` fix committed but not confirmed deployed to Railway. LaneF still returns "I couldn't generate a SQL query" in ~18.5s with 0 Tools, 0 Tokens.
- Distributions "Total Paid" shows $0 — 10 events have Paid status but payout rows aren't seeded, so the KPI aggregation returns zero
- Phoenix Gateway Medical Office shows "256,577 units" — NULL was set in DB but page still renders old value (possible caching issue or second column)
- Investment-level sub-records missing across all funds — property_type, market, valuation, operating_data still absent for most investments; fund detail KPIs (Paid-In, Distributed, Occupancy, Avg LTV) remain blank
- Variance page shows "No variance data" — no underwriting versions seeded

**Tomorrow's recommended priority order:**
1. Confirm Railway deployed commit `fa9372dc` — check logs for ImportError from `combined_agent.py`
2. Fix distributions "Total Paid" KPI — seed payout rows for 10 Paid events
3. Fix Phoenix Gateway unit display — investigate cache or second column
4. Seed investment sub-records (property_type, market, valuation, operating_data) for all 3 funds
5. Seed 3–5 underwriting versions for Variance page demo data

---

## Stone PDS Health — DEGRADED (strongly improving)

Major progress overnight. Morning baseline was 10 PASS, 3 FAIL. End-of-day state is 12 PASS, 1 FAIL (1 pending deploy):

**Fixes verified working:**
- Resources page null guard — FIXED. `setHeatmapMonths ?? []` guards deployed; page loads gracefully.
- Timecards page null guard — FIXED. `setBench ?? []` guards deployed; page loads gracefully.
- Client Satisfaction fix — PARTIALLY FIXED. The original commit missed `npsSummary.trend` accessed directly in the render path at line 171. A follow-up fix was committed this afternoon, awaiting next Vercel deploy. Will be PASS after deploy.

**Still open:**
- Schedule Health (`/pds/schedule`) — silently redirects to `/pds/risk`. Open P1 bug.
- `pds_leader_coverage` — 0 rows. Business Line filter across Home/Markets returns empty.
- Satisfaction score data quality — `pds_client_satisfaction_snapshot` now has 580 rows but avg score is 0.38 (most rows are 0.0). Seed data quality issue, not a code bug.

**Tomorrow's recommended priority order:**
1. Verify Client Satisfaction page loads clean after next Vercel deploy
2. Fix Schedule Health redirect — either build the page or remove the misleading nav link
3. Seed `pds_leader_coverage` — 9 business lines × 3 market leaders; small insert, unblocks Business Line filter everywhere
4. Investigate satisfaction score data quality — check which table NPS summary reads from

---

## MSA Rotation Engine — OPERATIONAL

Miami — Wynwood/Edgewater completed its first intelligence brief (2026-03-24):
- **Composite score:** 7.0/10
- **Headline:** High transaction velocity (Ken Griffin + Goldman $180M office trade), rent growth +4.7% YoY, 95%+ occupancy. Primary risk is supply — 1,317 units under construction in Edgewater alone with 14+ more planned in Wynwood.
- **5 new feature cards created** from this brief's gap analysis. Top card by priority: Cap Rate Distribution by Asset Class Estimator (44.1/100 — pure calculation gap on data Winston already captures).
- **3 existing cards bumped in priority:** Supply-Demand Absorption Model (48→54), CoStar Rent Connector (36→42), Trepp CMBS Connector (28→34).
- **Total backlog:** 14 cards — 5 prompted, 9 specced, 0 built.

The Opportunity Zone program is confirmed permanent via OBBBA (signed July 4, 2025). The $927M Miami-Dade transit project with Wynwood stations is a long-lead demand catalyst — if groundbreaking is confirmed, the composite score bumps +2.0 to 9.0.

---

## Auto/* Branch Status

No `auto/*` branches found on origin. Clean.

---

## Coding Sessions (Recent History)

Most recent committed coding work:
- `93286c5d` — Debt fund tab set (Loan Book, Covenant Health, Maturity & Rate tabs on fund detail)
- `fa9372dc` — SQL generation fix (`generate_sql()` added to `combined_agent.py`), asset size display fixes, distribution seeding (12 events), Phoenix Gateway NULL fix
- `32e0609c` / `04d5639f` — Stone PDS null guard fixes (Resources, Timecards, Satisfaction)

No coding session report found for 2026-03-25 yet. Next session should start with Railway deploy verification of `fa9372dc`.

---

## Top Action Items for Today

1. **Verify Railway has deployed commit `fa9372dc`** — check Railway logs for ImportError in `combined_agent.py`. If not deployed, trigger a deploy. This unblocks SQL generation and potentially fixes Tests 2 and 3 (takes pass rate from 33% → ~67%).
2. **Investigate Tool Call Leak Regression (Test 1)** — Raw JSON from `repe.get_environment_snapshot` is visible in chat body. Check `backend/app/services/ai_gateway.py` SSE emit layer and `repo-b/src/components/winston/` chat renderer. This is Bug 0 re-emerging.
3. **Verify Client Satisfaction page after Vercel deploy** — follow-up fix was committed yesterday; confirm it lands clean.
4. **Fix Lane B latency regression** — Total elapsed for error recovery spiked 164ms → 12,534ms. Check post-tool response assembly in `backend/app/routes/nv_ai_copilot.py` and SSE flush timing in `ai_gateway.py`.
5. **Seed `pds_leader_coverage`** — Quick win: 9 rows unblocks Business Line filter across the whole Stone PDS environment.
6. **Seed Meridian distributions payout rows** — Fixes the misleading $0 "Total Paid" KPI.

---

## Market Regime Context

RISK_OFF_DEFENSIVE (classified 2026-03-22, high confidence). SPX broke 200-day MA, VIX at 26.78, HY spreads 320 bps, DXY ~99.5. Rate environment is feeding into REPE cap rate models — relevant context for Meridian demo scenarios. Market Rotation Engine is provisioned with 34 segments; first rotation sweeps are pending.

---

*Generated by morning-ops-digest scheduled task — 2026-03-25 08:30 AM*
*Batch docs commit: `4ae38a69` (docs/ai-testing/2026-03-24.md added)*
*No auto/* branches found.*
