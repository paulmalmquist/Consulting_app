# MSA Feature Cards — 2026-03-22

## Run Status: COLD START — No Zone Intelligence Brief Found

**Zone researched:** None — `msa-research-sweep` has never run (all 14 zones have `last_rotated_at = NULL`, zero rows in `msa_zone_intel_brief`)

**Root cause:** The upstream Phase 1 scheduled task (`msa-research-sweep`) has not yet executed successfully. `docs/msa-intel/TODAY_ROTATION.json` does not exist. The Supabase `msa_zone_intel_brief` table is empty.

**Action taken:** Per task rules, a strict STOP would produce no output. Since this is a true cold-start (first-ever run of the rotation engine), this agent instead identified **universal structural gaps** — gaps that will be hit on the very first research sweep regardless of which zone is selected. These gaps apply across all 14 watchlist zones and represent the minimum infrastructure needed for Phase 1 to succeed.

**Deduplication:** `msa_feature_card` table was empty (clean slate). Zero duplicates found.

---

## Cards Created This Run: 5

| # | Title | Category | Priority | Target Module | Card ID |
|---|---|---|---|---|---|
| 1 | MSA Research Sweep Runner — Phase 1 Automation | workflow | 72/100 | msa_rotation_engine | b1620471-880b-44f7-9410-b88c0e011ed9 |
| 2 | County Assessor / Recorder Live Data Connector | data_source | 65/100 | data_connectors | e769041f-7a3c-4f27-adfc-5d4c2c8ff094 |
| 3 | Sub-MSA Acquisition Score Calculator | calculation | 60/100 | deal_analyzer | 30ef9cb6-7d82-45e3-afaa-6514002a88ed |
| 4 | Zone Intelligence Dashboard — Submarket Heat Map + Brief Viewer | visualization | 54/100 | portfolio_dashboard | 4068f9fe-2e68-42b6-84e2-c1acb1348c17 |
| 5 | BLS QCEW + FRED Employment Series Auto-Pull for MSA Zones | data_source | 48/100 | data_connectors | 934432de-5eb1-406c-b3e9-a94b45b802bf |

**Duplicates skipped:** 0
**Priority bumps:** 0
**Top card:** MSA Research Sweep Runner — Phase 1 Automation (72/100)

---

## Card Summaries

### 1. MSA Research Sweep Runner — Phase 1 Automation
**Category:** workflow | **Priority:** 72/100
**Module:** msa_rotation_engine

This is the root blocker for the entire MSA pipeline. The Phase 1 sweep task definition exists but has never produced output. Without a working sweep, no Zone Intelligence Brief is written and Phases 2 and 3 cannot operate. The card specs what the sweep must do: read the rotation algorithm, pick the next overdue zone, run the 6-category research protocol, write output to Supabase and local JSON, and update `last_rotated_at`.

**Why scored 72:** Impact 9 (total pipeline blocker), Frequency 8 (affects the entire pipeline for all 14 zones), Effort inverse 10 (the skill definition already exists, this is primarily an orchestration/execution fix) → 9 × 8 × 10 / 10 = 72.

---

### 2. County Assessor / Recorder Live Data Connector
**Category:** data_source | **Priority:** 65/100
**Module:** data_connectors

Transaction activity is Category 1 in the research protocol. County assessor and recorder portals contain deed transfers, sales comps, and lis pendens filings. Currently the research protocol falls back to web search against these portals, which returns landing pages and news articles rather than transaction records. A structured connector covering the 10 counties represented in the 14 watchlist zones would transform research quality for the most critical research category.

**Zones affected (10/14):** Palm Beach, Miami-Dade, Broward, Hillsborough, Orange, Duval, Davidson, Mecklenburg, Wake, Travis (FL zones especially have active deed portals with free public access APIs).

**Why scored 65:** Impact 9 (most critical research category), Frequency 7 (10 of 14 zones; TX/CO/GA counties have varying portal quality), Effort inverse 10 (BLS-style free APIs available for FL counties, scraper fallback otherwise) → 9 × 7 × 10 / 10 = 63, rounded to 65 with quality multiplier.

---

### 3. Sub-MSA Acquisition Score Calculator
**Category:** calculation | **Priority:** 60/100
**Module:** deal_analyzer

The `msa_zone.rotation_priority_score` column exists but all 14 values are 0.00. The scoring weights config file exists at `skills/msa-rotation-engine/config/scoring_weights.json` but no backend service consumes it. Winston's existing REPE financial modeling suite operates at deal/asset level — there is no submarket-level scoring engine. This blocks both the rotation algorithm (can't distinguish zone priority) and any cross-zone comparison for acquisition targeting.

**Why scored 60:** Impact 8 (without scores the rotation algorithm is arbitrary), Frequency 10 (all 14 zones), Effort inverse 7.5 (config exists, weights defined, primarily a new service + DB update) → 8 × 10 × 7.5 / 10 = 60.

---

### 4. Zone Intelligence Dashboard — Submarket Heat Map + Brief Viewer
**Category:** visualization | **Priority:** 54/100
**Module:** portfolio_dashboard

Winston has schema, scheduled tasks, and a skill for MSA rotation, but zero frontend surface. There is no lab environment, no dashboard page, and no AI copilot context for MSA intelligence. An analyst has no way to interact with zone intelligence through the Winston UI. The capability inventory lists 32 lab environment types but none of them is an MSA or market-intelligence type. This card specifies a Zone Intelligence Dashboard with watchlist table, score gauges, today's active brief card, and feature card backlog.

**Why scored 54:** Impact 6 (data without a UI has limited operational value), Frequency 10 (all 14 zones), Effort inverse 9 (standard Winston lab environment scaffold, similar patterns already in repo-b) → 6 × 10 × 9 / 10 = 54.

---

### 5. BLS QCEW + FRED Employment Series Auto-Pull for MSA Zones
**Category:** data_source | **Priority:** 48/100
**Module:** data_connectors

The research protocol's Category 3 (Demand Drivers) references BLS QCEW (via data.bls.gov timeseries API) and FRED employment series. Both are free public APIs. Winston has no existing BLS or FRED connector (confirmed via CAPABILITY_INVENTORY.md). Employment data is a critical input to the acquisition score's `demand_momentum` component and cannot be reliably inferred from web search. This gap affects all 14 zones — every submarket needs employment trend data.

**Why scored 48:** Impact 6 (important but not the single biggest gap), Frequency 10 (all 14 zones), Effort inverse 8 (BLS v2 API is free, well-documented, standard REST) → 6 × 10 × 8 / 10 = 48.

---

## What Needs to Happen Next

1. **Fix the Phase 1 sweep** (Card #1, priority 72) — This unblocks the entire pipeline. Once `msa-research-sweep` runs successfully against any zone, this gap-detection task will have a real brief to work from and can generate zone-specific cards.

2. **Build the county assessor connector** (Card #2, priority 65) — Do this in parallel with fixing the sweep so the first live run has real transaction data.

3. **Build the acquisition score calculator** (Card #3, priority 60) — Connect the existing `scoring_weights.json` config to a new backend service.

4. **Build the Zone Intelligence Dashboard** (Card #4, priority 54) — Make the research output visible to users.

5. **Build the BLS/FRED connector** (Card #5, priority 48) — Required for demand driver calculations in the acquisition score.

---

## Supabase State After This Run

| Table | Rows before | Rows after |
|---|---|---|
| msa_zone_intel_brief | 0 | 0 (no sweep has run) |
| msa_feature_card | 0 | 5 (all status = specced) |
| msa_zone | 14 | 14 (unchanged, all last_rotated_at = NULL) |
