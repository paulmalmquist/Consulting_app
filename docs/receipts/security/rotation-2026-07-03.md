# Credential Rotation Receipt — 2026-07-03

Redacted receipt. **No secret value appears here or in any command output, log, or commit.** ADO: Story #758. Follows the credential exposure remediated in PR #500 (values removed from tracked files; they remained in git history, so rotation was required).

## Scope

| Credential | System(s) updated | Read by |
|---|---|---|
| `NOVENDOR_ADMIN_PASSWORD` | Supabase Auth (user `info@novendor.ai`) + Vercel `consulting-app` production env (reference copy) | Reference only — no runtime code reads it (`grep` confirms only `ADMIN_INVITE_CODE` is read, in `repo-b/src/app/api/auth/login/route.ts`). The reference copy is kept truthful so the documented `vercel env pull` path returns the working Supabase password. |
| `ADMIN_INVITE_CODE` | Vercel `consulting-app` env — **all three targets** (production, preview, development) | `repo-b/src/app/api/auth/login/route.ts` (admin invite login) |

## Method

- **Supabase password**: rotated via the Supabase Admin API (`PUT /auth/v1/admin/users/{id}`) using a service-role key fetched fresh from the Supabase CLI (`supabase projects api-keys`). The Vercel-stored service-role key was stale (401) — a separate drift worth noting.
- **Vercel env**: set via the **Vercel REST API** (`POST /v10/projects/{id}/env`, value in JSON body). The `vercel env add` CLI proved unreliable for piped stdin in this environment — `< file`, `cat | …`, `printf | …`, and Python `subprocess(shell=True)` all stored an **empty** value while still printing `Added Environment Variable` (a false success). The REST API is deterministic and was the only reliable path. See the tips.md lesson.
- **Deploy**: `vercel redeploy` of the latest production deployment, aliased to `novendor.ai` — required because Vercel binds env vars to a deployment at creation; changing a project var does not affect existing deployments.

## Verification (timestamp 2026-07-03 ~17:59Z)

| Check | Result |
|---|---|
| Supabase grant with NEW password | HTTP 200 |
| Supabase grant with OLD password | HTTP 400 (rejected) |
| Vercel round-trip (`env pull`) NOVENDOR_ADMIN_PASSWORD == set value | true (len 25) |
| Vercel round-trip ADMIN_INVITE_CODE == set value | true (len 20) |
| Vercel-stored password grants Supabase | HTTP 200 |
| **Live** admin login on novendor.ai with NEW invite code | HTTP 200, `bos_session` cookie set |
| **Live** admin login with OLD (leaked) invite code | HTTP 401 |
| **Live** admin login with empty code | HTTP 401 |
| **Live** protected route `GET /app` with admin session | HTTP 200 |
| Production env var timestamps after rotation | both 2026-07-03T17:58–59Z |
| Leaked invite code in preview/development targets | replaced (HTTP 201 each) — leaked value now dead in all environments |

## Related findings (not changed)

- **Unlabeled Confluent key file** (`confluent)_kafka_api.json`, referenced by the pre-rotation ENV_KEYS.md): identified only — **does not exist on disk, is not tracked, and was never added in git history**; the `.gitignore` rule `*_api.json` (line 235) would catch any local copy. No git exposure; no action taken per instruction.
- **Stale Vercel service-role key**: the `SUPABASE_SERVICE_ROLE_KEY` stored on the `consulting-app` project returned 401 against Supabase; current keys come from the Supabase CLI. Flagged for a separate follow-up (not rotated here).
- **`repo-b` Vercel project is empty**: CLAUDE.md's "backend secrets live on the repo-b project" claim is stale — production secrets live on `consulting-app`. Corrected in CLAUDE.md by PR #500.

## Handling guarantees

- No secret value was printed to terminal, written to a committed file, or placed in a commit/PR body.
- All scratch files holding plaintext (env pulls, generated values, API keys, rotation scripts) were deleted; scratch verified clean of `password|invite|api_key|service_role` residue.
- The Supabase service-role key and Vercel token were used in-memory only.
