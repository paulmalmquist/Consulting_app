# Winston AI Feature Test Report — 2026-03-29

## Assumptions

- The Chrome extension is required to access the Winston AI chat interface for live testing
- The test URL remains `https://www.paulmalmquist.com/lab/env/a1b2c3d4-0001-0001-0003-000000000001/re/winston` (Meridian Capital Management environment)
- All 5 planned tests require browser automation to execute
- Without browser access, no test can be meaningfully executed — all must be marked SKIPPED
- Carryover issues from 2026-03-25 are assumed still present until verified by a successful test run
- Direct API fallback was attempted tonight but requires authentication tokens not available in the autonomous session

## Summary

| Metric | Value |
|---|---|
| Tests planned | 5 |
| Passed | 0 |
| Failed | 0 |
| Skipped | 5 |
| Pass rate | N/A — no tests executed |
| Environment | Meridian Capital Management (intended) |
| Chat URL | https://www.paulmalmquist.com/lab/env/a1b2c3d4-0001-0001-0003-000000000001/re/winston |
| Browser automation | ❌ Chrome extension not connected |
| API fallback attempted | ❌ Health endpoint returns 401 (auth required); lab page returns 307 (redirect to auth) |

**STATUS: SKIPPED — All 5 tests skipped. Chrome extension returned "Claude in Chrome is not connected" on two separate attempts. Direct API fallback was attempted via curl but blocked by authentication (401). This is the 4th consecutive night without test data.**

---

## API Fallback Attempt (New Tonight)

Per recommendations from 2026-03-27 and 2026-03-28, this run attempted a direct HTTP fallback before marking tests as SKIPPED.

| Endpoint | Method | Result | Evidence |
|---|---|---|---|
| `https://www.paulmalmquist.com` | GET | 200 OK | Site is live and responding [HIGH] |
| `https://www.paulmalmquist.com/api/ai/gateway/health` | GET | 401 Unauthorized | Auth required — no session token available in autonomous context [HIGH] |
| `https://www.paulmalmquist.com/lab/env/.../re/winston` | GET | 307 Redirect | Redirects to auth page — session cookie required [HIGH] |

**Conclusion:** The production site is healthy and responding. The AI gateway health endpoint exists and is rejecting unauthenticated requests (expected behavior). To enable an API-based test fallback, the autonomous task would need either: (a) a service account API key, or (b) a pre-authenticated session token injected into the task environment.

Source: curl HTTP calls from sandbox shell [HIGH]

---

## Test Results

### Test 1 — Basic Greeting/Response
**Status:** ⏭️ SKIPPED
**Prompt planned:** `Hello, what can you help me with?`
**Reason:** Chrome extension not connected; API fallback blocked by auth (401)
**Evidence:** `[mcp__Claude_in_Chrome__tabs_context_mcp returned "Claude in Chrome is not connected" at two attempts; curl to health endpoint returned HTTP 401]`
**Confidence:** `[HIGH]` — failure directly observed from tool error responses and HTTP status codes

---

### Test 2 — REPE-Specific Query (Fund Performance, Asset Data)
**Status:** ⏭️ SKIPPED
**Prompt planned:** `What funds do we have in this environment and what is their current performance?`
**Reason:** Chrome extension not connected; API fallback blocked by auth (401)
**Evidence:** `[same infrastructure failure as Test 1]`
**Confidence:** `[HIGH]` — failure directly observed

---

### Test 3 — Dashboard Generation Request
**Status:** ⏭️ SKIPPED
**Prompt planned:** `Generate a dashboard showing NOI trends across the top 5 assets`
**Reason:** Chrome extension not connected; API fallback blocked by auth (401)
**Evidence:** `[same infrastructure failure as Test 1]`
**Confidence:** `[HIGH]` — failure directly observed

---

### Test 4 — Document Processing Query
**Status:** ⏭️ SKIPPED
**Prompt planned:** `Show me a summary of the most recently uploaded documents in this environment`
**Reason:** Chrome extension not connected; API fallback blocked by auth (401)
**Evidence:** `[same infrastructure failure as Test 1]`
**Confidence:** `[HIGH]` — failure directly observed

---

### Test 5 — RAG/Context Retrieval
**Status:** ⏭️ SKIPPED
**Prompt planned:** `What does our investment policy say about maximum leverage ratios?`
**Reason:** Chrome extension not connected; API fallback blocked by auth (401)
**Evidence:** `[same infrastructure failure as Test 1]`
**Confidence:** `[HIGH]` — failure directly observed

---

## Infrastructure Issue

### Chrome Extension Not Connected — 4th Consecutive Night
**Error:** `Claude in Chrome is not connected. The Chrome extension isn't reachable.`
**Source:** `mcp__Claude_in_Chrome__tabs_context_mcp` tool call, attempted twice [HIGH]
**Impact:** All browser-based test execution blocked. This is the **fourth consecutive night** (2026-03-26, 2026-03-27, 2026-03-28, 2026-03-29) where Chrome connectivity has prevented test execution. The nightly AI tester has produced zero new test data in 4 days.
**Resolution:** This is no longer a transient issue. Structural fix required — see Recommendations.

---

## Carryover Issues from 2026-03-25 (Unverified — 4 Days Without Data)

The following issues were last documented with live test data on 2026-03-25. They remain **unverified** for the fourth consecutive night due to browser access failures. They should be assumed still present until the next successful test run confirms otherwise.

| Issue | Severity | Status as of 2026-03-25 | Days unverified |
|---|---|---|---|
| Chat pipeline returns narration only, no data (Test 1) | CRITICAL | Regression — oscillating between narration-only and hallucinated data | 4 |
| repe_fast_path SQL generation fails for all asset queries | CRITICAL | Ongoing — 0 tokens, 0 tools for all NOI/cap rate queries | 4 |
| Conversational transform fallback never executes | HIGH | Ongoing — fast-path limitation acknowledged, no fallback runs | 4 |
| Lane B total latency regression (worsening trend) | HIGH | Worsening — 164ms → 12534ms → 22695ms over 3 days | 4 |
| "New Chat" button does not clear conversation state | MEDIUM | New as of 2026-03-25 | 4 |
| Raw fund IDs exposed to users | LOW | Ongoing since 2026-03-21 | 4 |

Source: `docs/ai-testing/2026-03-25.md` [HIGH]

---

## Impact Statement

```
Tests planned:                5
Tests executed:               0
Tests skipped:                5 (Chrome extension not connected + API auth barrier)
Pass rate:                    N/A
Infrastructure blockers:      Chrome extension unreachable; API requires auth tokens
Consecutive skipped nights:   4 (2026-03-26 through 2026-03-29)
Carryover critical issues:    2 (unverified — assumed still present)
Carryover high issues:        2 (unverified — assumed still present)
API fallback attempted:       Yes — blocked by 401 (auth required)
```

### 7-Day Trend

| Date | Pass Rate | Test 1 | Test 2 | Test 3 | Test 4 | Test 5 |
|---|---|---|---|---|---|---|
| 2026-03-23 | 33.3% | ❌ narration only | ❌ SQL fail | ❌ SQL fail | ❌ no fallback | ✅ 164ms |
| 2026-03-24 | 33.3% | ❌ hallucinated + tool spam | ❌ SQL fail | ❌ SQL fail | ❌ no fallback | ✅ 12534ms |
| 2026-03-25 | 33.3% | ❌ narration only (no spam) | ❌ SQL fail | ❌ SQL fail | ❌ no fallback | ✅ 22695ms |
| 2026-03-26 | N/A | ⏭️ SKIP | ⏭️ SKIP | ⏭️ SKIP | ⏭️ SKIP | ⏭️ SKIP |
| 2026-03-27 | N/A | ⏭️ SKIP | ⏭️ SKIP | ⏭️ SKIP | ⏭️ SKIP | ⏭️ SKIP |
| 2026-03-28 | N/A | ⏭️ SKIP | ⏭️ SKIP | ⏭️ SKIP | ⏭️ SKIP | ⏭️ SKIP |
| 2026-03-29 | N/A | ⏭️ SKIP | ⏭️ SKIP | ⏭️ SKIP | ⏭️ SKIP | ⏭️ SKIP |

Pass rate was flat at 33.3% for 3 consecutive days followed by 4 consecutive skipped nights. No data on whether any fixes have shipped since 2026-03-25.

---

## Recommendations for Tomorrow's Session

1. **CRITICAL: Provision an API test token for the autonomous tester** — Four consecutive nights of Chrome failures proves browser-only testing is structurally unreliable for nightly autonomous runs. The recommended fix: create a service account or long-lived API token that can be stored as an environment variable for the scheduled task. This would allow `curl -H "Authorization: Bearer $TOKEN" https://www.paulmalmquist.com/api/ai/gateway/ask` to work without a browser. Tonight's run confirmed the endpoints exist and respond — only auth is missing. This is the highest-leverage fix for restoring nightly test coverage.

2. **Fix or replace the Chrome extension connectivity** — If browser-based testing is still desired (e.g., for UI validation), investigate why the extension has been disconnected for 4 consecutive nights. Likely causes: Chrome not running at 11 PM, extension signed out, or the machine is asleep/locked. Consider a keepalive mechanism or a cron job that verifies Chrome is running before the test window.

3. **Prioritize repe_fast_path SQL generation fix** — This has been the #1 functional blocker for an estimated 8+ days and blocks Tests 2, 3, and 4. Without live testing for 4 nights, it is unknown whether any progress has been made.

4. **Run a full verification of all 6 carryover issues on next successful test** — These have been unverified for 4 consecutive nights. Treat the next successful run as a baseline reset rather than assuming continuity from 2026-03-25 results.

5. **Consider expanding the scheduled task spec** — The current task spec is browser-only. Adding a hybrid approach (API calls for functional tests + browser for UI tests) would make the nightly tester resilient to Chrome outages. The backend routes are well-documented: `POST /api/ai/gateway/ask` for chat, `GET /api/ai/gateway/health` for health.

---

## Self-Critique

1. All 5 tests were SKIPPED due to infrastructure failure — this report contains zero new test data. [ACKNOWLEDGED — honest reporting per reliability protocol]
2. **Improvement over previous nights:** This run attempted an API-based fallback for the first time, confirming the endpoints are live (200 on main site, 401 on gateway health). This is actionable new information even though tests couldn't execute. [HIGH confidence — directly observed HTTP status codes]
3. Carryover issues are assumed still present but are UNVERIFIED for 4 consecutive nights. Any of them could have been fixed since 2026-03-25. The "days unverified" count reflects testing gap, not necessarily issue duration. [FLAGGED — recommend full re-verification on next successful run]
4. The Chrome extension connectivity issue is now a 4-night repeat failure, classified as CRITICAL infrastructure gap. Previous nights recommended an API fallback; tonight's run proved the concept is viable but needs an auth token provisioned. [HIGH confidence — directly observed]
5. No claims in this report are fabricated or assumed as fact. All HTTP status codes and tool responses are directly sourced from tool calls made during this run. [HIGH confidence]
