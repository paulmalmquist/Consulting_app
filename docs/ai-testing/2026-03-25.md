# Winston AI Feature Test Report — 2026-03-25

## Summary

| Metric | Value |
|---|---|
| Tests run | 6 |
| Passed | 2 |
| Failed | 4 |
| Pass rate | 33.3% |
| Environment | Meridian Capital Management |
| Chat URL tested | https://www.paulmalmquist.com/lab/env/a1b2c3d4-0001-0001-0003-000000000001/re/winston |
| Browser automation | ✅ Chrome extension connected |

**STATUS: DEGRADED — 4 of 6 tests failed. Core data-query pipeline remains broken. Test 1 regressed to narration-only (no data rendered at all).**

Compared to 2026-03-24 (33.3% pass rate), today is an identical pass rate with one notable regression: Test 1 now produces only a narration promise ("I'll fetch the environment snapshot to list the funds. One moment.") with no data rendered at all — worse than 2026-03-24 which at least returned hallucinated fund data. The tool call spam from 2026-03-24 is no longer visible (minor improvement), but the response is functionally empty. The `repe_fast_path` pipeline (Lane F) continues to fail for all data queries with the same SQL generation error. Tests 5 and 6 pass consistently. A new finding: the "New Chat" button does not properly clear conversation state — test messages were appended to the previous session's thread.

---

## Test Results

### Test 1 — Simple Fund Query
**Status:** ❌ FAIL (narration only — no data rendered)
**Prompt sent:** `What funds do we have in this environment?`
**Response summary:** "I'll fetch the environment snapshot to list the funds. One moment." — response ended there. No fund data was returned.
**Trace:** Path: chat, Lane: A, Elapsed: 3136ms, Tools: 0, Tokens: 1,439
**Tool call spam visible:** ❌ NO (improvement over 2026-03-24)
**Raw internal IDs visible:** N/A — no data rendered
**Correct data returned:** ❌ NO — no data returned at all
**Notes:** This is a regression from 2026-03-24. On 03-24, this test returned hallucinated fund data with tool call JSON visible. Today the tool call spam is gone (improvement), but the response stops after the narration promise without ever delivering fund data. This is functionally identical to the 2026-03-23 "narration only" behavior. The Lane A chat pipeline announces intent to call `repe.get_environment_snapshot` but either the tool call never executes or the response is never rendered. Tokens: 1,439 suggests the model did generate content, but it was not rendered to the user. This test is scored FAIL.

---

### Test 2 — Asset Analytics
**Status:** ❌ FAIL
**Prompt sent:** `Show me the top 5 assets by NOI`
**Response summary:** "I couldn't generate a SQL query from your request. Try rephrasing or use the SQL editor directly."
**Trace:** Path: repe_fast_path, Lane: F, Elapsed: 14231ms, Tools: 0, Tokens: 0
**Time to first meaningful response:** ~14.2s (waited on SQL generation, then failed)
**Tool call spam visible:** No
**Raw internal IDs visible:** No
**Notes:** Identical failure pattern to 2026-03-24. The `repe_fast_path` SQL generation stage fails for all asset NOI queries and returns 0 tokens, 0 tool calls. The 14.2s elapsed time is slightly higher than 2026-03-24's 12.3s. The error message is unchanged.

---

### Test 3 — Multi-Step Analytical
**Status:** ❌ FAIL
**Prompt sent:** `Compare the top 3 assets by NOI across all funds, and show me which ones have the highest cap rate improvement over the last 4 quarters.`
**Response summary:** "I couldn't generate a SQL query from your request. Try rephrasing or use the SQL editor directly."
**Trace:** Path: repe_fast_path, Lane: F, Elapsed: 12256ms, Tools: 0, Tokens: 0
**Time to first meaningful response:** ~12.3s (waited on SQL generation, then failed)
**Tool call spam visible:** No
**Raw internal IDs visible:** No
**Notes:** Identical failure pattern to Test 2 and to 2026-03-24. The complex multi-step query fares no better than the simple NOI query. Both are routed to the broken `repe_fast_path`. Elapsed time (12.3s) is comparable to 2026-03-24's 13.3s.

---

### Test 4 — Conversational Transform
**Status:** ❌ FAIL
**Prompt sent:** `Turn that into a bar chart`
**Response summary:** "I recognized this as a **transform result** request but the fast-path engine doesn't handle it yet. Let me use the full analysis pipeline instead."
**Trace:** Path: repe_fast_path, Lane: F, Elapsed: 14ms, Tools: 0, Tokens: 0
**Time to first meaningful response:** <1s (text appeared instantly)
**Tool call spam visible:** No
**Raw internal IDs visible:** No
**Notes:** Identical result to 2026-03-24 and 2026-03-23. Winston correctly identifies the conversational transform intent and acknowledges the fast-path limitation — but the promised fallback to the "full analysis pipeline" never executes. 14ms elapsed confirms no actual processing happened. Since Test 3 also returned no data, there is nothing to transform even if the fallback worked. Both the transform routing and the fallback mechanism remain unimplemented.

---

### Test 5 — Error Recovery
**Status:** ✅ PASS
**Prompt sent:** `Show me assets in a fund called XXXXXXXXNOTREAL`
**Response summary:** Clean, helpful error: "I couldn't find a fund named 'XXXXXXXXNOTREAL' in the Meridian Capital Management environment." Listed all 3 available funds with names, vintage, strategy, and fund IDs. Offered actionable next steps including fuzzy search suggestion — a new UX improvement.
**Trace:** Path: tool, Lane: B, Elapsed: 22695ms, Tools: 1 (Loading fund data — 116ms)
**Time to first meaningful response:** ~116ms (narration "Loading fund data" with checkmark)
**Tool call spam visible:** No
**Raw internal IDs visible:** ⚠️ YES — raw fund_ids (e.g., `a1b2c3d4-0003-0030-0001-000000000001`) remain visible in fund listing
**Notes:** The `tool` pipeline (Lane B) continues to work correctly for fund disambiguation queries. Response quality is high — clear error message, lists available alternatives, offers actionable next steps. The 116ms tool call time is fast. Total elapsed of 22695ms is notably higher than both 2026-03-24's 12534ms and 2026-03-23's 164ms — ongoing Lane B latency regression worsening. The fuzzy search suggestion ("Ask me to run a fuzzy search for names similar to...") is a new UX addition not present in previous runs. Ongoing concern: raw fund_ids remain exposed to end users.

---

### Test 6 — Simple Greeting
**Status:** ✅ PASS
**Prompt sent:** `Hello, what can you help me with?`
**Response summary:** Full, comprehensive capability overview: "Hi — I'm Winston, your in-app copilot for Meridian Capital Management (active environment)." Listed 5+ capability categories (Overview/diagnostics, Finance/modeling, Operations/risk, Reporting/IR, Pipeline/underwriting, Admin/docs) with concrete examples and usage instructions. Included a two-phase write flow explanation and offered to pull environment snapshot.
**Trace:** Path: chat, Lane: B, Elapsed: 17702ms, Tools: 0, Tokens: 9,342
**Time to first meaningful response:** ~17.7s (full response rendered)
**Tool call spam visible:** No
**Raw internal IDs visible:** No
**Notes:** Clean, professional, comprehensive greeting. Capability list is well-structured, accurate, and includes more detail than 2026-03-24's response (9,342 tokens). Notably this routed to Lane B (same as 2026-03-24) rather than Lane A. The 17.7s elapsed is significantly higher than 2026-03-24's 7.1s — a 2.5x latency regression for the greeting response. Response quality remains high despite the latency increase. No markdown rendering issues.

---

## Regressions vs Previous Run (2026-03-24)

| Test | 2026-03-24 | 2026-03-25 | Change |
|---|---|---|---|
| Test 1: Simple fund query | ❌ FAIL — response renders but data hallucinated + tool call JSON visible | ❌ FAIL — narration only, no data rendered at all | **Regression: narration-only (back to 03-23 behavior). Tool call spam gone (improvement).** |
| Test 2: Asset analytics | ❌ FAIL — "I couldn't generate a SQL query" (12.3s) | ❌ FAIL — "I couldn't generate a SQL query" (14.2s) | No change; slightly slower |
| Test 3: Multi-step analytical | ❌ FAIL — "I couldn't generate a SQL query" (13.3s) | ❌ FAIL — "I couldn't generate a SQL query" (12.3s) | No change |
| Test 4: Conversational transform | ❌ FAIL — fast-path limitation acknowledged, no fallback | ❌ FAIL — identical result | No change |
| Test 5: Error recovery | ✅ PASS — 73ms tool call, 12534ms total elapsed | ✅ PASS — 116ms tool call, 22695ms total elapsed | **Latency regression: total elapsed 12534ms → 22695ms (1.8x slower). Fuzzy search suggestion is new (improvement).** |
| Test 6: Simple greeting | ✅ PASS — 7140ms | ✅ PASS — 17702ms | **Latency regression: 7140ms → 17702ms (2.5x slower)** |

**Summary:** 0 new test fixes. 1 regression (Test 1 back to narration-only). Tool call spam in Test 1 is gone (minor improvement). Latency regressions across Lane B responses (Tests 5, 6). New fuzzy search UX addition in Test 5 error recovery.

---

## Issues Requiring Attention

### [CRITICAL — REGRESSION] Chat Pipeline Returns Narration Only, No Data (Test 1)
**Pattern:** The `chat` pipeline (Lane A) generates a narration promise ("I'll fetch the environment snapshot to list the funds. One moment.") but never renders the actual fund data. Tokens: 1,439 suggests content was generated by the model but not rendered to the UI.
**Status:** Regression from 2026-03-24. Behavior matches 2026-03-23's "narration only" pattern. The 2026-03-24 hallucinated-data-with-tool-spam behavior is no longer present — the tool call spam fix may have inadvertently broken response rendering entirely.
**Impact:** Users see a promise of data that never arrives — worse UX than even hallucinated data.
**Files to check:**
- `backend/app/services/ai_gateway.py` — SSE emit layer; response tokens may be filtered too aggressively after tool call spam fix
- `repo-b/src/components/winston/` — chat message rendering; check if content after narration blocks is being dropped
- `backend/app/services/ai_conversations.py` — tool call execution and response assembly for Lane A
**Priority:** **Critical** — core fund listing capability broken

### [CRITICAL — ONGOING] repe_fast_path SQL Generation Fails for All Asset Queries (Tests 2, 3)
**Pattern:** All data-oriented asset queries route to `repe_fast_path` (Lane F). The pipeline runs SQL generation for 12–14 seconds, then returns "I couldn't generate a SQL query from your request." with 0 tools, 0 tokens.
**Status:** Ongoing since 2026-03-23. No improvement across 3 consecutive test runs.
**Root cause:** The SQL generation model/prompt is unable to produce valid SQL for NOI/cap rate asset queries. Either the schema context is missing, the model prompt is wrong, or the SQL generation service is returning an error that surfaces as a user-facing failure.
**Files to check:**
- `backend/app/services/ai_gateway.py` — `repe_fast_path` SQL generation flow
- `backend/app/routes/nv_ai_copilot.py` — Lane F handler and SQL generation invocation
- The SQL generation prompt/schema context injected into the fast path
**Priority:** **Critical** — blocks majority of Winston's core REPE analytics functionality

### [HIGH — ONGOING] Conversational Transform Fallback Never Executes (Test 4)
**Pattern:** Winston acknowledges "the fast-path engine doesn't handle it yet" and promises to "use the full analysis pipeline instead" — but no fallback pipeline runs.
**Status:** Unresolved since 2026-03-23. No change across 3 consecutive runs.
**Files to check:**
- `backend/app/routes/nv_ai_copilot.py` — transform intent detection and fallback routing
- `backend/app/services/ai_gateway.py` — fallback pipeline trigger
**Priority:** **High** — conversational follow-up is a core UX capability

### [HIGH — WORSENING] Lane B Total Latency Regression (Tests 5, 6)
**Pattern:** Lane B response times are worsening across consecutive runs:
- Test 5 (error recovery): 164ms (03-23) → 12534ms (03-24) → 22695ms (03-25)
- Test 6 (greeting): 7140ms (03-24) → 17702ms (03-25)
**Status:** Worsening trend over 3 days.
**Root cause:** The actual tool execution is fast (116ms for Test 5). The 22.7s total implies increasing post-tool processing delay or LLM response generation overhead. Possible causes: growing conversation context (since New Chat doesn't clear state), or backend changes increasing response assembly time.
**Files to check:**
- `backend/app/routes/nv_ai_copilot.py` — Lane B post-tool response assembly
- `backend/app/services/ai_gateway.py` — SSE emit timing after tool results
- Chat state management — investigate whether "New Chat" actually clears conversation context on the backend
**Priority:** **High** — previously-fast Lane B path now has 17–22s latency

### [MEDIUM — NEW] "New Chat" Button Does Not Clear Conversation State
**Pattern:** Clicking "New Chat" appears to append new messages to the existing conversation thread rather than creating a fresh session. All 6 test prompts were appended to the previous night's test conversation. This may contribute to the Lane B latency regression (growing context window).
**Status:** New finding, not previously tracked.
**Files to check:**
- `repo-b/src/components/winston/` — New Chat button handler
- Backend conversation/session management
**Priority:** **Medium** — impacts test isolation and may cause context pollution + latency growth

### [LOW — ONGOING] Raw Fund IDs Exposed to Users (Test 5)
**Pattern:** Internal UUIDs (e.g., `a1b2c3d4-0003-0030-0001-000000000001`) appear in user-facing fund listings. Affects Lane B responses.
**Status:** Unresolved since 2026-03-21.
**Fix:** Strip or hide `fund_id` fields from MCP tool response rendering in the UI.
**Priority:** **Low** — cosmetic but unprofessional for client demos

---

## Impact Statement

```
Tests run:                    6
Pass rate:                    33.3% (2/6) — same as 2026-03-24 and 2026-03-23
Regressions vs 2026-03-24:   2 (Test 1 narration-only regression; Lane B latency worsening)
Improvements vs 2026-03-24:  1 (Test 1 tool call spam no longer visible)
New fixes vs 2026-03-24:     0
New critical issues:          1 (Test 1 narration-only: possible over-correction of tool spam fix)
Ongoing critical issues:      1 (repe_fast_path SQL generation broken — 3 consecutive days)
Ongoing high issues:          1 (conversational transform fallback not implemented)
Worsening high issues:        1 (Lane B latency regression — 3-day trend)
New medium issues:            1 ("New Chat" doesn't clear conversation state)
Ongoing low issues:           1 (raw fund IDs exposed)
```

### 3-Day Trend

| Date | Pass Rate | Test 1 | Test 2 | Test 3 | Test 4 | Test 5 | Test 6 |
|---|---|---|---|---|---|---|---|
| 2026-03-23 | 33.3% | ❌ narration only | ❌ SQL fail | ❌ SQL fail | ❌ no fallback | ✅ 164ms | ✅ PASS |
| 2026-03-24 | 33.3% | ❌ hallucinated + tool spam | ❌ SQL fail | ❌ SQL fail | ❌ no fallback | ✅ 12534ms | ✅ PASS |
| 2026-03-25 | 33.3% | ❌ narration only (no spam) | ❌ SQL fail | ❌ SQL fail | ❌ no fallback | ✅ 22695ms | ✅ PASS |

Pass rate is flat at 33.3% for 3 consecutive days. Test 1 is oscillating between failure modes (narration-only ↔ hallucinated data). Lane B latency is on a clear upward trend. No core data-query fixes have shipped in 3 days.
