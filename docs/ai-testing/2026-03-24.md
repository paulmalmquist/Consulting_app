# Winston AI Feature Test Report — 2026-03-24

## Summary

| Metric | Value |
|---|---|
| Tests run | 6 |
| Passed | 2 |
| Failed | 4 |
| Pass rate | 33.3% |
| Environment | Meridian Capital Management |
| Chat URL tested | https://www.paulmalmquist.com/lab/env/a1b2c3d4-0001-0001-0003-000000000001/re/winston |
| Browser automation | ✅ Chrome extension connected |

**STATUS: DEGRADED — 4 of 6 tests failed. Core data-query pipeline remains broken.**

Compared to 2026-03-23 (33.3% pass rate), today is an identical pass rate with two notable changes: Test 1 now renders a response (improvement from 2026-03-23 "narration only"), but introduces a **new regression** — the chat pipeline (Lane A) is returning hallucinated/stale fund IDs (`f-0001-2020-core`, `f-0002-2021-value-add`, `f-0003-2023-opportunistic`) that do not match the environment's canonical funds. The `repe_fast_path` pipeline (Lane F) continues to fail for all data queries, but its error message changed from "No response from Winston." to "I couldn't generate a SQL query from your request." — suggesting minor messaging improvement but same underlying failure. Tests 5 and 6 pass consistently.

---

## Test Results

### Test 1 — Simple Fund Query
**Status:** ⚠️ PARTIAL FAIL (response renders but data is wrong)
**Prompt sent:** `What funds do we have in this environment?`
**Response summary:** Response rendered with a fund list. However, the fund names and IDs returned are **incorrect** — hallucinated or stale data not matching the actual environment funds. Raw tool call JSON was visible in the response body.
**Trace:** Path: chat, Lane: A, Elapsed: 5154ms, Tools: 0, Tokens: 1,632
**Tool call spam visible:** ⚠️ YES — raw JSON block `{"tool":"repe.get_environment_snapshot","params":{}}` and `Received tool response.` text are visible to end user
**Raw internal IDs visible:** ⚠️ YES — hallucinated fund IDs shown (e.g., `f-0001-2020-core`, `f-0002-2021-value-add`, `f-0003-2023-opportunistic`)
**Correct data returned:** ❌ NO — real canonical fund IDs are `a1b2c3d4-0003-0030-0001-000000000001`, `a1b2c3d4-0001-0010-0001-000000000001`, `a1b2c3d4-0002-0020-0001-000000000001` (confirmed via Test 5's Lane B tool call)
**Notes:** This is a mixed result vs. 2026-03-23. On 03-23, this test produced "narration only" with no data rendered — today the response renders, which is a rendering fix. However the new behavior introduces a data accuracy regression: the chat pipeline (Lane A) is calling `repe.get_environment_snapshot` with 0 actual tool calls registered (Tools: 0 in trace), and returning hallucinated fund IDs rather than live environment data. The tool call narration leak (raw JSON shown to user) is a new visible regression for Bug 0. This test is scored FAIL.

---

### Test 2 — Asset Analytics
**Status:** ❌ FAIL
**Prompt sent:** `Show me the top 5 assets by NOI`
**Response summary:** "I couldn't generate a SQL query from your request. Try rephrasing or use the SQL editor directly."
**Trace:** Path: repe_fast_path, Lane: F, Elapsed: 12331ms, Tools: 0, Tokens: 0
**Time to first meaningful response:** ~12.3s (waited on SQL generation, then failed)
**Tool call spam visible:** No
**Raw internal IDs visible:** No
**Notes:** The error message changed from the 2026-03-23 "No response from Winston." to "I couldn't generate a SQL query from your request." This is slightly more informative, but the underlying failure is identical — the `repe_fast_path` SQL generation stage fails for all asset NOI queries and returns 0 tokens, 0 tool calls. The 12.3s elapsed time is high for a failure. The user is left with no actionable data and a redirect to the SQL editor (which most users won't use).

---

### Test 3 — Multi-Step Analytical
**Status:** ❌ FAIL
**Prompt sent:** `Compare the top 3 assets by NOI across all funds, and show me which ones have the highest cap rate improvement over the last 4 quarters.`
**Response summary:** "I couldn't generate a SQL query from your request. Try rephrasing or use the SQL editor directly."
**Trace:** Path: repe_fast_path, Lane: F, Elapsed: 13280ms, Tools: 0, Tokens: 0
**Time to first meaningful response:** ~13.3s (waited on SQL generation, then failed)
**Tool call spam visible:** No
**Raw internal IDs visible:** No
**Notes:** Identical failure pattern to Test 2 — the more complex multi-step query fares no better than the simple NOI query. Both are routed to the broken `repe_fast_path`. 13.3s elapsed for a failure is worse UX than the 2026-03-23 behavior (which failed faster with a dashboard shell). The SQL generation step is now the bottleneck before the failure message surfaces.

---

### Test 4 — Conversational Transform
**Status:** ❌ FAIL
**Prompt sent:** `Turn that into a bar chart`
**Response summary:** "I recognized this as a **transform result** request but the fast-path engine doesn't handle it yet. Let me use the full analysis pipeline instead."
**Trace:** Path: repe_fast_path, Lane: F, Elapsed: 19ms, Tools: 0, Tokens: 0
**Time to first meaningful response:** <1s (text appeared instantly)
**Tool call spam visible:** No
**Raw internal IDs visible:** No
**Notes:** Identical result to 2026-03-23. Winston correctly identifies the conversational transform intent and acknowledges the fast-path limitation — but the promised fallback to the "full analysis pipeline" never executes. 19ms elapsed confirms no actual processing happened. Since Test 3 also returned no data, there is nothing to transform even if the fallback worked. Both the transform routing and the fallback mechanism remain unimplemented.

---

### Test 5 — Error Recovery
**Status:** ✅ PASS
**Prompt sent:** `Show me assets in a fund called XXXXXXXXNOTREAL`
**Response summary:** Clean, helpful error: "I searched funds by name and did not find a fund called 'XXXXXXXXNOTREAL' in the Meridian Capital Management environment." Listed all 3 available funds with names, vintage, type, and status. Offered actionable next step.
**Trace:** Path: tool, Lane: B, Elapsed: 12534ms, Tools: 1 (Loading fund data — 73ms)
**Time to first meaningful response:** ~73ms (narration "Loading fund data" with checkmark)
**Tool call spam visible:** No
**Raw internal IDs visible:** ⚠️ YES — raw fund_ids (e.g., `a1b2c3d4-0003-0030-0001-000000000001`) remain visible in fund listing
**Notes:** The `tool` pipeline (Lane B) continues to work correctly for fund disambiguation queries. Response quality is high — clear error message, lists available alternatives, offers actionable next step. The 73ms tool call time is excellent. Total elapsed of 12534ms is notably higher than 2026-03-23's 164ms — possible regression in overall pipeline latency for Lane B, though the actual tool execution was fast. Ongoing concern: raw fund_ids remain exposed to end users. Notably, the canonical fund IDs returned here (`a1b2c3d4-*`) confirm the Test 1 fund IDs were hallucinated.

---

### Test 6 — Simple Greeting
**Status:** ✅ PASS
**Prompt sent:** `Hello, what can you help me with?`
**Response summary:** Full, comprehensive capability overview: "Hi — I'm Winston, your in-app copilot for Meridian Capital Management (current environment)." Listed 7+ capability categories (Portfolio & environment, Fund-level finance & reporting, Deal / asset analysis, Modeling & scenarios, Capital & investor operations, Pipeline & deal scoring, IR & docs) with concrete examples and a call to action.
**Trace:** Path: chat, Lane: B, Elapsed: 7140ms, Tools: 0, Tokens: 9,103
**Time to first meaningful response:** ~7.1s (full response rendered)
**Tool call spam visible:** No
**Raw internal IDs visible:** No
**Notes:** Clean, professional, comprehensive greeting. Capability list is well-structured, accurate, and includes more detail than 2026-03-23's response (9,103 tokens vs prior run). Notably this routed to Lane B (tool pipeline) rather than Lane A (chat pipeline) for the greeting — different from 2026-03-23 behavior but the result quality is the same. No markdown rendering issues.

---

## Regressions vs Previous Run (2026-03-23)

| Test | 2026-03-23 | 2026-03-24 | Change |
|---|---|---|---|
| Test 1: Simple fund query | ❌ FAIL — narration only, tokens not rendered | ❌ FAIL — response renders but data hallucinated + tool call JSON visible | **New regression: data accuracy + tool call leak** |
| Test 2: Asset analytics | ❌ FAIL — empty dashboard shell, "No response from Winston." | ❌ FAIL — "I couldn't generate a SQL query" (12.3s) | Error message improved; SQL gen timeout is slower |
| Test 3: Multi-step analytical | ❌ FAIL — empty dashboard shell, "No response from Winston." | ❌ FAIL — "I couldn't generate a SQL query" (13.3s) | Same pattern as Test 2 |
| Test 4: Conversational transform | ❌ FAIL — fast-path limitation acknowledged, no fallback | ❌ FAIL — identical result | No change |
| Test 5: Error recovery | ✅ PASS — 164ms | ✅ PASS — 73ms tool call, 12534ms total elapsed | **Latency regression: total elapsed 164ms → 12534ms** |
| Test 6: Simple greeting | ✅ PASS | ✅ PASS | No change — quality consistent |

**Summary:** 0 new test fixes. 2 new issues: (1) Test 1 chat pipeline now returns hallucinated fund data + exposes raw tool call JSON (Bug 0 regression); (2) Test 5 total elapsed time spiked from 164ms → 12534ms (Lane B latency regression). The `repe_fast_path` error message improved from "No response from Winston." to a more informative SQL failure message, but the core failure remains.

---

## Issues Requiring Attention

### [CRITICAL — NEW] Chat Pipeline Returns Hallucinated Fund Data + Exposes Tool Call JSON (Test 1)
**Pattern:** The `chat` pipeline (Lane A) generates a response with visible raw tool call JSON (`{"tool":"repe.get_environment_snapshot","params":{}}`, `Received tool response.`) and returns hallucinated fund IDs (`f-0001-2020-core` etc.) that don't match the real environment.
**Status:** New regression introduced since 2026-03-23.
**Impact:** Users see internal tool execution details AND incorrect data — both are critical UX failures. The correct fund IDs were verified via Lane B's tool call in Test 5.
**Files to check:**
- `backend/app/services/ai_gateway.py` — SSE emit layer; tool narration text being emitted as content tokens
- `repo-b/src/components/winston/` — chat message rendering; needs to filter internal tool call blocks
- `backend/app/services/ai_conversations.py` — possible stale context or wrong env binding for fund lookup
**Priority:** **Critical** — data accuracy failure + information exposure in chat pipeline

### [CRITICAL — ONGOING] repe_fast_path SQL Generation Fails for All Asset Queries (Tests 2, 3)
**Pattern:** All data-oriented asset queries route to `repe_fast_path` (Lane F). The pipeline runs SQL generation for 12–13 seconds, then returns "I couldn't generate a SQL query from your request." with 0 tools, 0 tokens.
**Status:** Ongoing since 2026-03-23. Error message changed from "No response from Winston." but underlying failure is the same.
**Root cause:** The SQL generation model/prompt is unable to produce valid SQL for NOI/cap rate asset queries. Either the schema context is missing, the model prompt is wrong, or the SQL generation service is returning an error that surfaces as a user-facing failure.
**Files to check:**
- `backend/app/services/ai_gateway.py` — `repe_fast_path` SQL generation flow
- `backend/app/routes/nv_ai_copilot.py` — Lane F handler and SQL generation invocation
- The SQL generation prompt/schema context injected into the fast path
**Priority:** **Critical** — blocks majority of Winston's core REPE analytics functionality

### [HIGH — ONGOING] Conversational Transform Fallback Never Executes (Test 4)
**Pattern:** Winston acknowledges "the fast-path engine doesn't handle it yet" and promises to "use the full analysis pipeline instead" — but no fallback pipeline runs.
**Status:** Unresolved since 2026-03-23.
**Files to check:**
- `backend/app/routes/nv_ai_copilot.py` — transform intent detection and fallback routing
- `backend/app/services/ai_gateway.py` — fallback pipeline trigger
**Priority:** **High** — conversational follow-up is a core UX capability

### [HIGH — NEW] Lane B Total Latency Regression (Test 5)
**Pattern:** Test 5 (error recovery, Lane B tool pipeline) total elapsed time jumped from 164ms (2026-03-23) to 12534ms today, even though the actual tool call completed in 73ms.
**Status:** New regression since 2026-03-23.
**Root cause:** The 73ms tool execution is fast; the 12.5s total implies a post-tool processing delay in the Lane B pipeline — possibly LLM response generation overhead or SSE flush timing.
**Files to check:**
- `backend/app/routes/nv_ai_copilot.py` — Lane B post-tool response assembly
- `backend/app/services/ai_gateway.py` — SSE emit timing after tool results
**Priority:** **High** — previously-fast Lane B path now has 12s latency

### [LOW — ONGOING] Raw Fund IDs Exposed to Users (Tests 1, 5)
**Pattern:** Internal UUIDs (e.g., `a1b2c3d4-0003-0030-0001-000000000001`) appear in user-facing fund listings. Affects both Lane A (hallucinated IDs) and Lane B (real but internal IDs).
**Status:** Unresolved since 2026-03-21.
**Fix:** Strip or hide `fund_id` fields from MCP tool response rendering in the UI.
**Priority:** **Low** — cosmetic but unprofessional for client demos

---

## Impact Statement

```
Tests run:                    6
Pass rate:                    33.3% (2/6) — same as 2026-03-23
Regressions vs 2026-03-23:   2 (Test 1 data accuracy/tool leak; Test 5 total latency 164ms → 12534ms)
New fixes vs 2026-03-23:     0
New critical issues:          1 (hallucinated fund data + tool call JSON visible in chat)
Ongoing critical issues:      1 (repe_fast_path SQL generation broken)
Ongoing high issues:          1 (conversational transform fallback not implemented)
New high issues:              1 (Lane B latency regression)
Ongoing low issues:           1 (raw fund IDs exposed)
```
