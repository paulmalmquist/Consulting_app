# Winston AI Feature Test Report — 2026-03-23

## Summary

| Metric | Value |
|---|---|
| Tests run | 6 |
| Passed | 2 |
| Failed | 4 |
| Pass rate | 33.3% |
| Environment | Meridian Capital Management |
| Chat URL tested | https://www.paulmalmquist.com/lab/env/a1b2c3d4-0001-0001-0003-000000000001/re/winston |
| Browser automation | ✅ Chrome extension connected |

**STATUS: DEGRADED — 4 of 6 tests failed. Core data-query pipeline remains broken.**

Compared to 2026-03-21 (the last successful run, 16.7% pass rate), there is a marginal improvement: Tests 5 (error recovery) and 6 (greeting) now pass consistently. However, the `repe_fast_path` pipeline (Lane F) continues to produce empty dashboard shells with "No response from Winston." for all data-oriented queries. The `chat` pipeline (Lane A) still generates tokens that don't render as actual response content — only the narration line appears. No regressions from 2026-03-21.

---

## Test Results

### Test 1 — Simple Fund Query
**Status:** ❌ FAIL
**Prompt sent:** `What funds do we have in this environment?`
**Response summary:** Narration line "I'll look up the active environment's funds now." appeared, but no fund data was rendered below it. Only trace metadata (Path, Lane, Elapsed, Tools, Tokens) visible.
**Trace:** Path: chat, Lane: A, Elapsed: 2873ms, Tools: 0, Tokens: 1,444
**Time to first meaningful response:** ~2.9s (narration only)
**Tool spam visible:** No
**Raw internal IDs visible:** No
**Notes:** Identical failure pattern to 2026-03-21. The chat pipeline (Lane A) generates ~1,444 tokens but they do not render in the UI — only the narration line appears. Zero tool calls despite the narration implying an environment lookup. Note: in the same chat session a prior query "What funds do we have?" (slightly different phrasing) DID return a full fund table with 3 funds (Lane A, 427ms, 0 tools), suggesting session state or prompt phrasing affects routing.

---

### Test 2 — Asset Analytics
**Status:** ❌ FAIL
**Prompt sent:** `Show me the top 5 assets by NOI`
**Response summary:** A Dashboard block rendered ("COMPLETED", "1 widgets • Custom • 2 KPI(s)") but no asset data, table, or chart appeared. Ended with explicit "No response from Winston." message.
**Trace:** Path: repe_fast_path, Lane: F (inferred from dashboard shell pattern)
**Time to first meaningful response:** <1s (shell appeared instantly)
**Tool spam visible:** No
**Raw internal IDs visible:** No
**Notes:** Same `repe_fast_path` failure as 2026-03-21. The dashboard container is created (suggesting intent parsing works) but no MCP tools are called to populate it with data. "No response from Winston." is the end state.

---

### Test 3 — Multi-Step Analytical
**Status:** ❌ FAIL
**Prompt sent:** `Compare the top 3 assets by NOI across all funds, and show me which ones have the highest cap rate improvement over the last 4 quarters.`
**Response summary:** Dashboard COMPLETED shell ("1 widgets • Custom • 2 KPI(s)") with "No response from Winston." Identical pattern to Test 2.
**Trace:** Path: repe_fast_path, Lane: F (inferred)
**Time to first meaningful response:** <1s (shell appeared instantly)
**Tool spam visible:** No
**Raw internal IDs visible:** No
**Notes:** The more complex multi-step query fares no better than the simple NOI query — both are routed to the broken `repe_fast_path`. No configuration error (unlike 2026-03-21 Test 4 with a multi-paragraph spec), suggesting the current query was correctly classified as a REPE data query rather than an architectural prompt.

---

### Test 4 — Conversational Transform
**Status:** ❌ FAIL
**Prompt sent:** `Turn that into a bar chart`
**Response summary:** "I recognized this as a **transform result** request but the fast-path engine doesn't handle it yet. Let me use the full analysis pipeline instead." — but no chart or data was produced. No fallback pipeline executed.
**Trace:** Path: repe_fast_path, Lane: F (inferred from pattern)
**Time to first meaningful response:** <1s (text appeared)
**Tool spam visible:** No
**Raw internal IDs visible:** No
**Notes:** Same result as 2026-03-21 Test 5. Winston correctly identifies the conversational transform intent and even acknowledges the fast-path limitation — but the promised fallback to the "full analysis pipeline" never materializes. Additionally, since Test 2 produced no data, there is nothing to transform. Both the transform routing and the fallback mechanism need fixing.

---

### Test 5 — Error Recovery
**Status:** ✅ PASS
**Prompt sent:** `Show me assets in a fund called XXXXXXXXNOTREAL`
**Response summary:** Clean, helpful error: "I searched Meridian Capital Management for a fund named 'XXXXXXXXNOTREAL' but found no exact match." Listed all 3 available funds with names, vintage, type, and strategy. Offered actionable next steps.
**Trace:** Path: tool, Lane: B, Elapsed: 164ms, Tools: ~1
**Time to first meaningful response:** ~0.2s (narration "Loading fund data" with checkmark)
**Tool spam visible:** No
**Raw internal IDs visible:** ⚠️ Yes — raw fund_ids (e.g., `a1b2c3d4-0003-0030-0001-000000000001`) are exposed to the user in the fund listing
**Notes:** The `tool` pipeline (Lane B) continues to work correctly for disambiguation queries. Response was fast (164ms vs 10.9s on 2026-03-21) — significant speed improvement. The narration layer ("Loading fund data") with a completion checkmark works well. Ongoing concern: internal UUIDs should not be shown to end users.

---

### Test 6 — Simple Greeting
**Status:** ✅ PASS
**Prompt sent:** `Hello, what can you help me with?`
**Response summary:** Full, detailed capability overview: "I'm Winston — your in-app copilot for Meridian Capital Management (active environment)." Listed 8 capability categories (Portfolio & navigation, Financial analytics & modeling, Performance/capital/LP reporting, Operations & compliance, Underwriting & pipeline, Document & IR support, Workflow / approvals, plus How I work section) with concrete examples.
**Trace:** Path: chat, Lane: A (inferred)
**Time to first meaningful response:** <2s (full response rendered)
**Tool spam visible:** No
**Raw internal IDs visible:** No
**Notes:** Clean, professional, comprehensive greeting. The capability list is well-structured and accurate. No markdown rendering issues. This is the healthiest test result of the run.

---

## Regressions vs Previous Run (2026-03-21)

| Test | 2026-03-21 | 2026-03-23 | Change |
|---|---|---|---|
| Test 1: Simple fund query | ❌ FAIL | ❌ FAIL | No change — narration only, tokens not rendered |
| Test 2: Asset analytics | ❌ FAIL | ❌ FAIL | No change — empty dashboard shell |
| Test 3: Multi-step analytical | ❌ FAIL | ❌ FAIL | No change — empty dashboard shell |
| Test 4: Conversational transform | ❌ FAIL | ❌ FAIL | No change — fast-path limitation acknowledged but unresolved |
| Test 5: Error recovery | ✅ PASS | ✅ PASS | **Consistent pass — speed improved from 10.9s → 164ms** |
| Test 6: Simple greeting | Not tested | ✅ PASS | New test — passes cleanly |

**Summary:** 0 new regressions. 0 new fixes for the broken pipeline tests. The error recovery test now runs dramatically faster (10.9s → 164ms). No new issues observed.

---

## Issues Requiring Attention

### [CRITICAL — ONGOING] repe_fast_path Pipeline Produces Empty Dashboards (Tests 2, 3, 4)
**Pattern:** All data-oriented queries route to `repe_fast_path` (Lane F). The pipeline generates a Dashboard container shell ("COMPLETED", "1 widgets") in <1s but calls 0 tools and generates 0 tokens. Ends with "No response from Winston."
**Status:** Unresolved since at least 2026-03-21 (likely earlier, given Chrome connectivity issues on 2026-03-19/20).
**Root cause:** The fast path creates UI scaffolding (dashboard metadata/widget count) based on query intent parsing, but the data-fetching stage never executes. The dashboard composition pipeline is not calling MCP tools to populate widgets.
**Files to check:**
- `backend/app/services/ai_gateway.py` — fast path routing and tool dispatch
- `backend/app/routes/nv_ai_copilot.py` — SSE lane routing, Lane F handler
- Dashboard widget data population pipeline
**Priority:** **Critical** — blocks the majority of Winston's core REPE functionality

### [HIGH — ONGOING] Chat Pipeline Tokens Not Rendered (Test 1)
**Pattern:** The `chat` pipeline (Lane A) generates ~1,444 tokens in ~2.9s but the response content never renders in the UI. Only the narration line is visible.
**Status:** Unresolved since 2026-03-21.
**Note:** A slightly different phrasing ("What funds do we have?" vs "What funds do we have in this environment?") DID return data in the same session — suggesting prompt phrasing, session state, or an intermittent rendering bug.
**Files to check:**
- `repo-b/src/components/winston/` — chat message rendering components
- `backend/app/services/ai_gateway.py` — SSE event emission order
- `repo-b/src/lib/commandbar/assistantApi.ts` — SSE stream consumer
**Priority:** **High** — core chat functionality broken for specific query patterns

### [LOW — ONGOING] Raw Fund IDs Exposed to Users (Test 5)
**Pattern:** Internal UUIDs (e.g., `a1b2c3d4-0003-0030-0001-000000000001`) appear in user-facing fund listings returned by the `tool` pipeline.
**Status:** Unresolved since 2026-03-21.
**Fix:** Strip or hide `fund_id` fields from MCP tool response rendering; show only human-readable fund names.
**Priority:** **Low** — cosmetic, but unprofessional for client demos

---

## Impact Statement

```
Tests run:                6
Pass rate:                33.3% (2/6)
Regressions vs 2026-03-21: 0
New fixes vs 2026-03-21:   0
New issues:               None
Ongoing critical issues:  1 (repe_fast_path broken)
Ongoing high issues:      1 (chat tokens not rendering)
Ongoing low issues:       1 (raw fund IDs exposed)
```
