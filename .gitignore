# macOS resource forks (external drives)
._*

# Per-deploy git SHA captured by scripts/deploy_backend.sh before `railway up`.
# Lives inside backend/app/ so the Dockerfile's `COPY app ./app` picks it up.
# Read at backend startup and exposed via /version. Not source.
backend/app/_git_sha.txt

# Stale path from an earlier deploy script revision.
backend/.git_sha

# Local AI-dispatch receipt mirrors written by `python -m scripts.ai_dispatch.cli ask`.
.ai_receipts/

# Loose research / outreach notes that landed at the repo root. Keep
# locally if useful, don't ship them as source.
/ai_remote_roles.md
/winston_ai_agent_dr.md
/resume_holes.md

# Outreach drafts (PII, per-campaign payloads).
scripts/drafts/

# Local-only utility scripts and smoke harnesses that should not enter
# CI or production. Move to docs/ or backend/scripts/ if they earn a
# place. The smoke for trading_analytics is included as long as it is
# tracked elsewhere; the standalone outlook integration is local-only.
scripts/outlook_service.py

# Angular specific
/dist/
/out-tsc/
/tmp/
/coverage/
/e2e/test-output/
/.angular/
.angular/

# Node modules and dependency files
node_modules/
/package-lock.json
/yarn.lock

# Environment files
/.env
/.env.*
!/.env.example

# Angular CLI and build artefacts
/.angular-cli.json
/.ng/

# TypeScript cache
*.tsbuildinfo

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Local secrets / scratch
godaddy.txt
godaddy2.txt
verceltoken.txt
openaikey.txt
*token*.txt
*pat*.txt
*key*.txt
trainingkey.txt

# Python virtualenvs and local env files (all subdirectories)
**/.venv/
**/.venv*/
**/.env
**/.env.*
!**/.env.example

# Python cache artifacts
**/__pycache__/
*.pyc

# Next.js build/export output
**/out/
**/.next/

# Test artifacts
**/test-results/
**/playwright-report/
**/coverage/

# Orchestration runtime artifacts
.orchestration/worktrees/
.orchestration/locks/
.orchestration/logs/*.json
.orchestration/logs/*.jsonl
.orchestration/sessions/*.json
claw-code-main/
# OS files
.DS_Store
.vercel

# Claude project instructions; keep machine-local files ignored.
.claude/*
!.claude/settings.json
!.claude/skills/
!.claude/skills/**
hf_toke.txt

# Local test/runtime artifacts
artifacts/
happy_co_invitecode.txt
repo-b/.next.bak*/
.openclaw/
scripts/.winston_managed_agent_state.json
.playwright-cli/

# Local scratch: daily dumps, outreach drafts, article scratch
outputs/
deal-opportunities-*.md

# Binary files and outputs
*.pdf
!backend/tests/fixtures/sample_text.pdf
!repo-b/public/assets/ai-roi/*.pdf
*.docx
*.pptx
*.xlsx
*.lock

# Brand assets and loose media — stored in docs/assets/ or ignored at root
docs/assets/*.png
*.HEIC
*.heic
Hall Boys*
docs/assets/*.html

# Generated/synthetic data
synthetic_repe_dataset.json

# Generated audit run outputs — timestamped run directories and per-entity
# authoritative-state snapshots produced by verification scripts and the
# Meridian surface drift / hierarchy trace runners. Keep hand-curated summary
# files (findings.md, audit_summary.json, etc.) but ignore bulk output.
audit/meridian_surface_drift/meridian-*/
audit/meridian_hierarchy_trace/meridian-*/
audit/meridian_hierarchy_trace/authoritative_period_state.*.json
audit/backfill/
audit/verification_run_*/
audit/ui_gross_to_net/

# Pitch Forge generated iteration outputs — critique JSON and final deck are
# produced on each run; committed intentionally only when curated.
demo_docs/**/pitch_forge/final/
demo_docs/**/pitch_forge/iteration_*/critique.json
demo_docs/**/pitch_forge/iteration_*/critique_summary.md
demo_docs/**/pitch_forge/iteration_3/
~$*
TaxHacker-main/
# Lock files
.~lock*
pretext-main/
mandalore/
client-hunting/
job-search/

# Font source archives
abadi-font-family.zip
abadi-font-family/

# Novendor wordmark source kit — track source files, ignore generated
# binaries (re-buildable via `python build_font.py`; production copies
# live in repo-b/public/fonts/)
novendor_type_source/**/dist/

# Altered Mind: private practice data — never commit
data/altered-mind/
Altered Mind Weekly Business Check In.xlsx

# Visual-direction design canvases — exploratory mockups for the
# /what-we-do hero infographic. Selected pieces are imported into
# repo-b/ on adoption; the canvases themselves stay local-only.
new_homepage/
nv_mark_transparent_files/
nv_mark_transparent_files.zip
novendor_business_to_ai_visual_directions.html

# Host runner output (per-run JSON, not worth versioning)
scripts/results/*.json
!scripts/results/.gitkeep

# Client-engagement scratch — receipts, drafts, gap-analysis, storyboards, and
# per-client work product that regenerates and shouldn't ship as source.
docs/happyco/
docs/outreach-rewrites-*.md
Hone_work/

# Loose root scratch: a stray running-lessons file (the tracked one is docs/tips.md);
# Option B UI reference mockups (already ported into repo-b/src/components/telemetry/);
# one-off outreach/draft automation scripts that belong in scripts/ if they earn a place.
/tips.md
/OptionB_*.jsx
/OptionB_handoff_prompt.md
/apply_firm_specific_drafts.py
/collect_bounces.py
/update_outlook_drafts.py
/update_quick_intro_drafts.py

# Outreach skill runtime state (SKILL.md is tracked; the bounce-resolution receipt regenerates).
skills/novendor-repe-outreach/resolved_bounces.json

# Client execution plans (root, per-client) and lead-gen research prompts — work product kept
# locally, not shipped as repo source.
/capgemini_tpm_microsite_execution_plan.md
/happyco_winston_sequential_execution_plan.md
docs/prompts/

# RS Factory generator artifacts (regenerated deterministically)
rs_factory_seed/output/

# Stargate streaming lane (PR 3+): local credentials and tooling venv stay out of git.
/confluent)_kafka_api.json
/aws_creds.json
scripts/streaming/stargate/.env
scripts/streaming/stargate/.venv/

# Local credential JSON class — never commit API keys / creds / secrets dumped to disk.
# Known-bad name plus the general class (no legit tracked files match these — checked).
confluent_api.json
confluent_kafka_api.json
*_api.json
*_creds.json
*_credentials.json
*credentials.json
*_secret.json
*_secrets.json
secrets.json
