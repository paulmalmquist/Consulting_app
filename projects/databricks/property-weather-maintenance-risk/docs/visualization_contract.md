# Visualization Contract

Charts must be generated from validated feature or prediction tables. Placeholder charts are not acceptable for a Databricks success claim.

The local contract writes minimal non-empty PNGs only to validate file plumbing. Databricks notebook execution must replace them with real charts before a live-run receipt can be marked successful.
